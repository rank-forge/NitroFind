# Requirements Archive: NitroFind v1.1

**Archived:** 2026-06-04
**Original document:** .planning/REQUIREMENTS.md (v1.1)
**Status:** ✅ ALL COMPLETE — 16/16 requirements delivered

---

# Requirements: NitroFind v1.1

**Defined:** 2026-06-03
**Core Value:** Instant, noise-free access to deep automotive knowledge — the entire database on your machine, searchable in milliseconds.
**Milestone goal:** Replace the PyQt6 desktop UI with a Flask web server accessible at http://localhost:5000. Single `python main.py` starts Elasticsearch and the web server together. Scraper and search engine from v1.0 are preserved without rewrite.

## v1.1 Requirements

### Server Lifecycle

- [x] **SRVR-01**: A single `python main.py` command starts Elasticsearch 8.18 and the Flask web server — no separate startup steps required
- [x] **SRVR-02**: Flask server listens on `http://localhost:5000` (port overridable via `PORT` environment variable)
- [x] **SRVR-03**: Flask server waits for Elasticsearch to pass a health check before accepting search requests; returns HTTP 503 with `{"status": "starting"}` during warmup
- [x] **SRVR-04**: Ctrl+C (SIGINT) terminates both the Flask server and the Elasticsearch subprocess cleanly — no orphaned JVM processes after exit

### REST API

- [x] **API-01**: `GET /api/search?q={query}` returns a JSON array of ranked results — each with title, url, source_domain, excerpt (with ES highlight tags), score, took_ms
- [x] **API-02**: `GET /api/search` accepts optional filter params `manufacturer`, `era_bucket`, `body_style` that narrow results using the existing `build_filter_clauses()` logic
- [x] **API-03**: `GET /api/status` returns JSON with Elasticsearch health (green/yellow/red), document count, and index size
- [x] **API-04**: `GET /` serves the main HTML search page (single-page app entry point)

### Search Interface (Browser)

- [x] **SRCH-01**: Search box updates results as the user types with a 300ms debounce — no button press required
- [x] **SRCH-02**: Each result row displays the article title, source domain, and an excerpt with matching query terms visually highlighted (bold)
- [x] **SRCH-03**: Clicking a result (or pressing Enter) displays the full article text in a right-side detail pane — no new browser tab opens
- [x] **SRCH-04**: Filter sidebar (manufacturer, era_bucket, body_style) narrows results without clearing the search query; filter state persists across query retypes

### UI Quality (Browser)

- [x] **UIPL-01**: UI ships with a dark theme as default (CSS variables — no Qt dependency)
- [x] **UIPL-02**: Result count and query time are displayed below the search box (e.g., "42 results (0.08s)")
- [x] **UIPL-03**: User can navigate results with arrow keys, open with Enter, and clear the search box with Escape

### Cleanup

- [x] **CLEN-01**: PyQt6, PyQt6-Qt6, PyQt6-sip, and qt-material removed from requirements.txt and all imports — app no longer requires a Qt install

## Requirement Outcomes

| Requirement | Phase | Delivered in | Outcome |
|-------------|-------|-------------|---------|
| SRVR-01 | Phase 6 | 06-03-PLAN.md | Validated — `python main.py` confirmed working |
| SRVR-02 | Phase 6 | 06-02-PLAN.md | Validated — localhost:5000, PORT env var |
| SRVR-03 | Phase 6 | 06-02-PLAN.md | Validated — 503 until state['ready'] |
| SRVR-04 | Phase 6 | 06-01-PLAN.md | Validated — SIGTERM + kill fallback |
| API-01 | Phase 7 | 07-01-PLAN.md | Validated — unit tested |
| API-02 | Phase 7 | 07-01-PLAN.md | Validated — unit tested |
| API-03 | Phase 6 | 06-02-PLAN.md | Validated — unit tested |
| API-04 | Phase 6/8 | 06-02 + 08-01 | Validated — render_template confirmed |
| SRCH-01 | Phase 8 | 08-02-PLAN.md | Validated — 300ms debounce + AbortController |
| SRCH-02 | Phase 8 | 08-02-PLAN.md | Validated — innerHTML excerpt rendering |
| SRCH-03 | Phase 8 | 08-02-PLAN.md | Validated — detail pane, no new tab |
| SRCH-04 | Phase 8 | 08-02-PLAN.md | Validated — filter state preserved |
| UIPL-01 | Phase 8 | 08-01-PLAN.md | Validated — dark teal CSS variables |
| UIPL-02 | Phase 8 | 08-02-PLAN.md | Validated — result count + took_ms display |
| UIPL-03 | Phase 8 | 08-02-PLAN.md | Validated — arrow/Enter/Escape keyboard nav |
| CLEN-01 | Phase 6 | 06-01 + 06-03 | Validated — PyQt6 removed from requirements.txt and codebase |

## Future Requirements (v1.2+)

- Full installer / startup shortcut (Windows .bat, macOS .command)
- Light/dark theme toggle in the browser UI
- Search history (last N queries, persisted in localStorage)
- Bookmarking — save articles to a favorites list
- `/api/article/{id}` endpoint for fetching individual articles by ES _id
- Filter by engine type, drivetrain, country of manufacture

---
*Archived: 2026-06-04 after v1.1 milestone completion*
