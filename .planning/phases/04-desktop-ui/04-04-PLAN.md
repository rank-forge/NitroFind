---
phase: 04-desktop-ui
plan: 04
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - main.py
autonomous: false
requirements:
  - UIPL-03
tags:
  - phase-4
  - desktop-ui
  - wiring
  - human-verify

must_haves:
  truths:
    - "main.py imports MainWindow from nitrofind.ui.main_window (no longer StubMainWindow)"
    - "on_es_ready constructs MainWindow(client) and shows it after LoadingWindow closes"
    - "apply_stylesheet(app, 'dark_teal.xml') still executes before window construction (UIPL-03 unchanged from Phase 1)"
    - "Running python main.py launches the app, reaches the search-ready state, and a user can type a query, see results with highlights, filter, click a result, see detail pane, navigate with arrows, press Enter, and press Escape"
    - "Quitting the app terminates the Elasticsearch subprocess cleanly (Phase 1 behavior preserved)"
  artifacts:
    - path: "main.py"
      provides: "MainWindow construction in on_es_ready"
      contains: "MainWindow(client)"
  key_links:
    - from: "main.py:on_es_ready"
      to: "nitrofind.ui.main_window.MainWindow"
      via: "main_window = MainWindow(client)"
      pattern: "MainWindow\\(client\\)"
---

<objective>
Land the W0-EXT-03 wiring change in main.py (swap StubMainWindow construction for MainWindow(client)) and run the end-to-end human-verification checkpoint that confirms Phase 4 success criteria 1-5 hold against a live, locally-indexed Elasticsearch instance. This is the final integration step — production code is otherwise complete after Plan 03; this plan binds it into the app entry point and verifies the user-facing behaviors that automated tests cannot prove (dark theme rendering, debounce feel, highlight visibility, keyboard navigation feel).

Purpose: The unit suite from Plans 01-03 proves contracts in isolation against mocked ES. Phase 4 success criteria require behavior against real ranked results — only a human can confirm that "typing in the search box updates results within 300ms of the user pausing" feels right, that highlights are visually visible against dark_teal, that arrow navigation feels responsive, and that the detail pane renders article body legibly.

Output: One-line code change in main.py, full non-integration test suite green, and a human-verification checkpoint confirming all five ROADMAP success criteria for Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-desktop-ui/04-RESEARCH.md
@.planning/phases/04-desktop-ui/04-PATTERNS.md
@.planning/phases/04-desktop-ui/04-UI-SPEC.md
@.planning/phases/04-desktop-ui/04-VALIDATION.md
@.planning/phases/04-desktop-ui/04-01-SUMMARY.md
@.planning/phases/04-desktop-ui/04-02-SUMMARY.md
@.planning/phases/04-desktop-ui/04-03-SUMMARY.md
@main.py
@nitrofind/ui/main_window.py

<interfaces>
<!-- main.py change scope — surgical, two-line diff. -->

Current (main.py line 34):
  from nitrofind.ui.main_window import StubMainWindow

Change to:
  from nitrofind.ui.main_window import MainWindow

Current (main.py lines 115-119, inside on_es_ready):
  main_window = StubMainWindow()
  main_window.show()
  state["main_window"] = main_window
  loading_window.close()

Change to:
  main_window = MainWindow(client)
  main_window.show()
  state["main_window"] = main_window
  loading_window.close()

NO other changes to main.py — DO NOT touch:
  - apply_stylesheet(app, "dark_teal.xml") on line 74 (UIPL-03 — must remain BEFORE window construction)
  - ESHealthWorker signal wiring on lines 206-211
  - shutdown_handler / aboutToQuit wiring
  - the state dict pattern from Phase 1 Plan 04
  - any logger calls
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update main.py to construct MainWindow(client) in place of StubMainWindow()</name>
  <files>main.py</files>
  <read_first>
    - main.py (the entire file — confirm the exact import line and on_es_ready function lines that need to change; note that `client` is constructed elsewhere in main.py before on_es_ready runs and is accessible via the existing capture pattern)
    - nitrofind/ui/main_window.py (confirm MainWindow constructor signature matches the W0-EXT-03 contract: MainWindow(client))
    - .planning/phases/04-desktop-ui/04-UI-SPEC.md (Phase 3 Integration Contracts: W0-EXT-03 section — exact change spec)
    - .planning/phases/04-desktop-ui/04-PATTERNS.md (main.py extend-in-place section — two-line change scope)
  </read_first>
  <action>
    Open main.py. Change the import statement that currently reads `from nitrofind.ui.main_window import StubMainWindow` to `from nitrofind.ui.main_window import MainWindow`. Inside the on_es_ready function, change the single line `main_window = StubMainWindow()` to `main_window = MainWindow(client)` — `client` must be the Elasticsearch client already constructed earlier in main.py and accessible at on_es_ready scope (verify by reading the file; if `client` is not in the closure scope, capture it from the existing scope or pass it through the same dict pattern Phase 1 uses for `worker`). Do NOT modify the apply_stylesheet call on line 74 — UIPL-03 requires it remain BEFORE any window construction. Do NOT modify ESHealthWorker signal wiring, the state dict pattern, shutdown_handler, aboutToQuit wiring, or any logger calls. After the change, run the full non-integration test suite to confirm no regression. Then run a syntax-only import smoke test: `python -c "import main"` should exit 0 without error (note: this does NOT launch the GUI, just imports the module to catch syntax errors).
  </action>
  <verify>
    <automated>pytest tests/ -x -m "not integration" && python -c "import importlib.util; spec = importlib.util.spec_from_file_location('main_smoke', 'main.py'); m = importlib.util.module_from_spec(spec); print('main.py imports clean')"</automated>
  </verify>
  <acceptance_criteria>
    - source: main.py contains the literal `from nitrofind.ui.main_window import MainWindow`
    - source: main.py does NOT contain the literal `StubMainWindow` anywhere (import or construction)
    - source: main.py contains the literal `MainWindow(client)` inside on_es_ready
    - source: main.py contains `apply_stylesheet(app, "dark_teal.xml")` BEFORE any reference to MainWindow (line-number ordering preserved — UIPL-03)
    - cli: `grep -c "StubMainWindow" main.py` returns 0
    - cli: `grep -c "MainWindow" main.py` returns >= 2 (one import, one construction)
    - cli: `grep -n "apply_stylesheet" main.py` reports a line number LESS than the line number where `MainWindow(client)` appears (ordering preserved)
    - behavior: pytest tests/ -x -m "not integration" exits 0 (full Phase 1-4 unit suite green)
    - behavior: python module-spec smoke test prints 'main.py imports clean' (no SyntaxError/ImportError)
  </acceptance_criteria>
  <done>main.py imports MainWindow and constructs it with the Elasticsearch client in on_es_ready; full non-integration test suite remains green; apply_stylesheet ordering preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end human verification of Phase 4 success criteria 1-5</name>
  <files>(no files modified)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 Success Criteria section — the 5 criteria this checkpoint verifies)
    - .planning/phases/04-desktop-ui/04-VALIDATION.md (Manual-Only Verifications table — dark theme, highlight visibility, 300ms debounce feel)
    - .planning/phases/04-desktop-ui/04-UI-SPEC.md (Copywriting Contract — exact strings the human will visually confirm; State Mapping — expected state transitions)
  </read_first>
  <what-built>
    Plans 01-03 + Task 1 of this plan delivered: MainWindow + SearchLineEdit + ResultDelegate + FilterSidebar wired into main.py, with the dark_teal theme applied at app startup. All automated tests are green. The app launches via `python main.py`, the Phase 1 loading window appears while ES warms up (~5-15s cold start), then the main window opens with the search bar focused and ready.
  </what-built>
  <how-to-verify>
    Prerequisites:
    1. Phase 2 scraper has been run against this machine at least once so the car_articles index contains real documents. If not, run the scraper first: `python -m nitrofind.scraper.run_scraper --limit 200` (or similar — check the scraper CLI from Phase 2 SUMMARY).
    2. Activate the venv: `source venv/bin/activate` (or the equivalent on Windows).

    Launch:
    3. Run `python main.py` from the repo root.
    4. Confirm the LoadingWindow appears with the spinner and "Starting Elasticsearch…" status (Phase 1 behavior unchanged).
    5. Wait for ES to become healthy (5-180s depending on machine). The loading window closes and the main window opens.

    Verify ROADMAP Phase 4 success criterion 1 — "Typing in the search box updates results within 300ms of the user pausing":
    6. Click the search bar (or confirm it has focus on open). Type "ferrari" quickly. Observe that results do NOT update on every keystroke. Pause typing. Within ~300ms of your pause, the result list populates with Ferrari-related articles. Repeat with a different query ("porsche") and confirm the same debounce feel.

    Verify ROADMAP Phase 4 success criterion 2 — "Each result row displays the article title, source domain, and an excerpt with matching query terms visually highlighted":
    7. Look at the result list. Each row should show: a bold title (11pt), a muted teal source domain line (e.g., "wikipedia.org"), and an excerpt line. Inside the excerpt, the search term ("ferrari" / "porsche") should appear in **bold** (rendered from ES `<b>` tags). Confirm the bold highlighting is visually obvious.

    Verify ROADMAP Phase 4 success criterion 3 — "Clicking a result (or pressing Enter) displays the full article text in a detail pane inside the app — no browser window opens":
    8. Click any result row. The right-hand detail pane should populate with the article title (14pt h2), source/url line in muted teal, and the article body text (full body via ArticleResult.body, not just the 300-char excerpt). Confirm NO external browser opens.
    9. Press Up/Down arrows in the result list and confirm the detail pane updates as you move.
    10. Press Enter on a row and confirm the detail pane shows that row's article (same handler).

    Verify ROADMAP Phase 4 success criterion 4 — "Filtering by manufacturer, era bucket, or body style in the sidebar narrows results without clearing the search query, and the filter state persists if the user types a new query":
    11. With a search query active, check "Ferrari" under Manufacturer in the sidebar. Observe the result list updates within ~300ms (filter triggers same debounce path) and the results narrow to Ferrari-only documents.
    12. Without unchecking the filter, clear the search bar and type a new query (e.g., "308"). Confirm: (a) the Ferrari checkbox is STILL checked, (b) new results are still filtered to Ferrari documents matching "308".
    13. Check an Era value (e.g., "1970s") alongside the manufacturer filter. Confirm results narrow further. Uncheck Ferrari; confirm only the era filter remains.

    Verify ROADMAP Phase 4 success criterion 5 — "The app renders with a dark theme by default, and the user can navigate results with arrow keys, open with Enter, and clear the search box with Escape":
    14. Visually confirm: background is dark (approximately #1a2327), text is light, selected row is teal (#6effe8 background, black text). No widget renders with white/default Qt styling.
    15. With the search bar focused and containing text, press Escape. Confirm the search bar clears immediately AND the result list clears AND the status label clears AND the window title resets to "NitroFind — Ready".
    16. Confirm arrow key navigation in the result list works (already tested in step 9). Confirm Enter activates a result (already tested in step 10). Confirm Escape clears (just tested in step 15).

    Verify status label copy (UIPL-02):
    17. Confirm the status label shows "N results (T:.2fs)" format with two decimal places of seconds — e.g., "12 results (0.08s)".
    18. Search for a nonsense query that returns zero results (e.g., "zzzqqxxyy"). Confirm status label shows exactly "No results".

    Verify error path (T-04-03 — optional but recommended):
    19. (Optional) Stop the local Elasticsearch process in a separate terminal, then attempt a search. Confirm the status label shows exactly "Search failed. Check Elasticsearch connection." (NOT a raw exception trace). Restart ES afterward.

    Verify clean shutdown:
    20. Close the app via window close button. Confirm in a terminal that no java/Elasticsearch processes remain orphaned (`pgrep -f Elasticsearch` should return nothing).

    If ALL of steps 6-20 behave as described, type `approved` to advance. Otherwise, describe which step(s) failed and what was observed.
  </how-to-verify>
  <acceptance_criteria>
    - human-check: All five ROADMAP Phase 4 Success Criteria confirmed (debounce feel, row content + highlights, detail pane on click/Enter, filter persistence, dark theme + arrow/Enter/Escape navigation)
    - human-check: Status label format matches "N results (T:.2fs)" with two decimal places
    - human-check: No external browser window opens on result selection (SRCH-03)
    - human-check: Dark theme applied uniformly — no widgets render with default Qt light styling (UIPL-03)
    - human-check: Escape clears search bar AND result list AND status label AND resets window title (UIPL-04)
    - human-check: App quit terminates ES subprocess cleanly (Phase 1 INFRA-03 still holds)
    - resume-signal: User responded "approved" or equivalent affirmation
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 20 verification steps passed, or describe which step failed and what was observed.</resume-signal>
  <done>Phase 4 success criteria fully verified against live ES with real indexed data; the user confirms the app feels and behaves as specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main.py imports → nitrofind.ui.main_window | New import surface; must not break Phase 1 startup flow |
| MainWindow(client) constructor → SearchEngine(client) → QThreadPool | New cross-thread surface introduced into the entry point |
| Live ES → app on user-visible queries | Real ES responses (including potentially malformed/missing fields) flow into the UI for the first time at integration scale |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W3-01 | Tampering | Import-time side effects from MainWindow module | mitigate | nitrofind.ui.main_window has no module-level side effects beyond class definitions and the `logger = logging.getLogger(__name__)` call (verified in Plan 03). main.py module-spec smoke test in Task 1 confirms no import-time crash. |
| T-04-W3-02 | Denial of Service | apply_stylesheet ordering broken by reordering | mitigate | acceptance_criteria includes grep assertion that `apply_stylesheet` line number is LESS than `MainWindow(client)` line number — guards against accidental reordering (Pitfall 4). |
| T-04-W3-03 | Information Disclosure | Live ES errors reaching the user on real-world failures | mitigate | _on_search_error (Plan 03) already maps to static string "Search failed. Check Elasticsearch connection." regardless of underlying exception. Verified by unit test and re-verified by optional human-verification step 19. |
| T-04-W3-04 | Spoofing | Orphaned Elasticsearch process after app quit | accept | Phase 1 INFRA-03 already enforces clean shutdown via shutdown_handler/aboutToQuit. Human-verification step 20 re-confirms after the MainWindow integration. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — only an import-statement swap in main.py. No package manager activity. |
</threat_model>

<verification>
- `pytest tests/ -x -m "not integration"` exits 0 (full Phase 1-4 unit suite green after main.py change)
- main.py imports without syntax errors
- StubMainWindow no longer referenced anywhere in main.py
- apply_stylesheet still ordered BEFORE MainWindow construction
- All 5 ROADMAP Phase 4 success criteria visually confirmed by the user against a live, populated ES instance
- App quit cleanly terminates the ES subprocess (no orphaned JVM processes)
</verification>

<success_criteria>
- main.py constructs MainWindow(client) in on_es_ready instead of StubMainWindow()
- Full non-integration test suite green
- All 5 Phase 4 ROADMAP success criteria human-verified end-to-end with live ES
- Dark theme renders correctly across all widgets (UIPL-03)
- Debounce feels right (~300ms pause yields results)
- Highlights visible in result excerpts (UIPL-01)
- Detail pane shows full article body (SRCH-03)
- Filter state persists across query retyping (SRCH-04)
- Escape clears, arrows navigate, Enter opens (UIPL-04)
- ES subprocess terminates cleanly on app quit (INFRA-03 preserved)
</success_criteria>

<output>
Create `.planning/phases/04-desktop-ui/04-04-SUMMARY.md` when done. Include:
- Confirmation of the main.py diff (import + construction line)
- Full test suite output snippet (pytest exit 0 confirmation)
- Human-verification step-by-step result log: which steps passed, any deviations noted
- Any issues observed during live-ES verification (e.g., real-world result quality, debounce timing feel, theme contrast issues) — these become Phase 5 or v2 candidates
- Phase 4 completion stamp: all 8 requirements (SRCH-01..04, UIPL-01..04) verified
</output>
</content>
</invoke>