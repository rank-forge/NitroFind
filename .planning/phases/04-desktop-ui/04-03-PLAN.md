---
phase: 04-desktop-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - nitrofind/ui/main_window.py
  - tests/test_ui/test_main_window.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - UIPL-01
  - UIPL-02
  - UIPL-04
tags:
  - phase-4
  - desktop-ui
  - main-window
  - search-line-edit

must_haves:
  truths:
    - "Typing in the search bar restarts a single-shot 300ms QTimer (SRCH-01)"
    - "After 300ms idle the timer fires _execute_search which calls SearchEngine.search with collected filters"
    - "Stale results from superseded searches are discarded via monotonic _current_seq guard"
    - "Status label updates to 'N results (T:.2fs)' on success, 'Searching…' during debounce, 'No results' for empty, 'Search failed. Check Elasticsearch connection.' on error (static string only — no raw exception)"
    - "Selecting (or arrow-key hovering) a result populates the detail pane with full body HTML"
    - "Escape on the SearchLineEdit clears the bar via keyReleaseEvent (not keyPressEvent)"
    - "Filter checkbox state survives query retyping (sidebar is read on every _execute_search call)"
  artifacts:
    - path: "nitrofind/ui/main_window.py"
      provides: "MainWindow(QMainWindow) replacing StubMainWindow; SearchLineEdit(QLineEdit) with Escape handler"
      contains: "class MainWindow(QMainWindow)"
    - path: "tests/test_ui/test_main_window.py"
      provides: "pytest-qt tests for SRCH-01/02/03/04 + UIPL-01/02/04"
  key_links:
    - from: "nitrofind/ui/main_window.py:MainWindow._execute_search"
      to: "nitrofind.search.engine.SearchEngine.search"
      via: "self._engine.search(query, filters=..., callback=..., error_callback=...)"
      pattern: "self\\._engine\\.search\\("
    - from: "nitrofind/ui/main_window.py:MainWindow._on_results"
      to: "QListWidget population + status label + detail pane"
      via: "seq guard, populate items, setText, optional detail pane reset"
      pattern: "if seq != self\\._current_seq"
    - from: "nitrofind/ui/main_window.py:SearchLineEdit.keyReleaseEvent"
      to: "QLineEdit.clear"
      via: "if event.key() == Qt.Key.Key_Escape: self.clear()"
      pattern: "Key_Escape"
---

<objective>
Replace StubMainWindow in nitrofind/ui/main_window.py with a full MainWindow(QMainWindow) that composes the SearchLineEdit, status label, FilterSidebar (from Plan 02), QListWidget + ResultDelegate (from Plan 02), and QTextBrowser detail pane into the layout defined in UI-SPEC. Wire the debounce timer, sequence-counter stale-result guard, filter persistence, keyboard navigation, and Escape clearing. Build the comprehensive pytest-qt test suite that proves SRCH-01..04 and UIPL-01/02/04 behaviors per 04-VALIDATION.md.

Purpose: This is the heart of Phase 4. Every search interaction described by the success criteria runs through MainWindow. Plan 02 already provides the visual surfaces and the filter sidebar contract; this plan binds them together with the SearchEngine signals from Plan 01 and the debounce/sequence/keyboard logic from UI-SPEC.

Output: One production file (nitrofind/ui/main_window.py) containing MainWindow + SearchLineEdit, and one test file (tests/test_ui/test_main_window.py) with the full 9-test grid from 04-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-desktop-ui/04-RESEARCH.md
@.planning/phases/04-desktop-ui/04-PATTERNS.md
@.planning/phases/04-desktop-ui/04-UI-SPEC.md
@.planning/phases/04-desktop-ui/04-VALIDATION.md
@.planning/phases/04-desktop-ui/04-01-SUMMARY.md
@.planning/phases/04-desktop-ui/04-02-SUMMARY.md
@nitrofind/ui/loading_window.py
@nitrofind/ui/main_window.py
@nitrofind/ui/result_delegate.py
@nitrofind/ui/filter_sidebar.py
@nitrofind/search/engine.py
@nitrofind/search/models.py
@tests/test_loading_window.py

<interfaces>
<!-- Contracts MainWindow depends on (all already exist after Plans 01-02). -->

SearchEngine (nitrofind/search/engine.py):
  __init__(self, client: Elasticsearch) -> None
  def search(
      self,
      query_text: str,
      *,
      filters: list[dict] | None = None,
      callback: Callable[[list, int], None] | None = None,  # after W0-EXT-02
      error_callback: Callable[[str], None] | None = None,
  ) -> None
  # Dispatches _SearchWorker on QThreadPool; returns immediately (non-blocking).
  # results_ready emits (list[ArticleResult], int took_ms) on success.
  # search_failed emits (str message) on failure.

ResultDelegate / _result_to_html (nitrofind/ui/result_delegate.py — from Plan 02):
  class ResultDelegate(QStyledItemDelegate)
  def _result_to_html(result: ArticleResult) -> str
  Data roles: UserRole=HTML string, UserRole+1=ArticleResult object

FilterSidebar (nitrofind/ui/filter_sidebar.py — from Plan 02):
  class FilterSidebar(QWidget)
  def collect_filters(self) -> list[dict]
  @property manufacturer_checks / era_checks / body_style_checks -> dict[str, QCheckBox]

ArticleResult (nitrofind/search/models.py — from Plan 01):
  Required: title, url, source_domain, score
  Optional: excerpt, body, highlight_title, highlight_body, manufacturer, era_bucket, body_style, ...

<!-- MainWindow contracts this plan creates (consumed by Plan 04). -->

MainWindow (nitrofind/ui/main_window.py):
  def __init__(self, engine_or_client) -> None
    # Accept either a SearchEngine instance OR an Elasticsearch client; document the chosen contract
    # in the docstring. Recommended: accept Elasticsearch client and construct SearchEngine internally
    # (this matches the main.py pattern in W0-EXT-03: MainWindow(client))
  @property search_bar / status_label / result_list / detail_pane / filter_sidebar / debounce_timer
  # All exposed for test introspection per loading_window.py convention.

SearchLineEdit (nitrofind/ui/main_window.py — defined alongside MainWindow):
  class SearchLineEdit(QLineEdit)
    def keyReleaseEvent(self, event: QKeyEvent) -> None
      # Escape -> self.clear() then super().keyReleaseEvent(event)

<!-- Copywriting contract (verbatim from UI-SPEC). -->

  Search bar placeholder:  "Search cars, manufacturers, models…"   (U+2026)
  Status idle:             ""
  Status loading:          "Searching…"
  Status results:          "{N} results ({T:.2f}s)"  (N=len(results), T=took_ms/1000)
  Status no results:       "No results"
  Status error:            "Search failed. Check Elasticsearch connection."
  Detail pane initial:     "Select a result to read the article."  (centered, color #80cbc4)
  Window title idle:       "NitroFind — Ready"
  Window title searching:  "NitroFind — {query}"   (em-dash U+2014)
  Empty result list label: "No articles match your search.\nTry different keywords or adjust filters."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SearchLineEdit + MainWindow with debounce, splitter layout, signal wiring, and stale-result guard</name>
  <files>nitrofind/ui/main_window.py</files>
  <read_first>
    - nitrofind/ui/main_window.py (current StubMainWindow content — must be replaced entirely; the title em-dash convention "NitroFind — Ready" carries forward)
    - nitrofind/ui/loading_window.py (QWidget construction pattern, signal-wiring order, property accessor convention, static error-string pattern lines 191-194)
    - nitrofind/ui/result_delegate.py (ResultDelegate and _result_to_html, both imported by MainWindow)
    - nitrofind/ui/filter_sidebar.py (FilterSidebar.collect_filters contract)
    - nitrofind/search/engine.py (SearchEngine.search signature; results_ready(list, int); search_failed(str); QueuedConnection convention; T-04-03 static error string pattern)
    - nitrofind/search/models.py (ArticleResult fields used in detail-pane HTML template)
    - main.py (current import of StubMainWindow on line 34 and construction on line 115 — informs the MainWindow(client) signature choice)
    - .planning/phases/04-desktop-ui/04-UI-SPEC.md (entire file — Layout Architecture, Spacing Scale, Typography, Color, every Component spec, Interaction Contracts, State Mapping, Copywriting Contract, Phase 3 Integration Contracts W0-EXT-03)
    - .planning/phases/04-desktop-ui/04-RESEARCH.md (Patterns 1/2/4/6/7/8; Pitfalls 1/2/4/5/7/8)
    - .planning/phases/04-desktop-ui/04-PATTERNS.md (main_window.py section — full pattern map and verbatim code blocks)
  </read_first>
  <action>
    Replace the contents of nitrofind/ui/main_window.py entirely. Open with a module docstring naming the class, listing every Copywriting Contract verbatim string from UI-SPEC (placeholder, status states, detail pane initial, window title, empty result list message), citing UI-SPEC Component sections, and noting that StubMainWindow is removed (or define an alias `StubMainWindow = MainWindow` for one wave if Plan 04 has not yet updated main.py — preferred: remove StubMainWindow and update main.py in Plan 04). Declare module logger via `logger = logging.getLogger(__name__)`. Import everything needed: QMainWindow, QWidget, QVBoxLayout, QHBoxLayout, QLineEdit, QLabel, QSplitter, QListWidget, QListWidgetItem, QAbstractItemView, QTextBrowser from PyQt6.QtWidgets; Qt, QTimer, pyqtSignal from PyQt6.QtCore; QKeyEvent from PyQt6.QtGui; SearchEngine from nitrofind.search.engine; ArticleResult from nitrofind.search.models; ResultDelegate and _result_to_html from nitrofind.ui.result_delegate; FilterSidebar from nitrofind.ui.filter_sidebar. Define class SearchLineEdit(QLineEdit) with only one override: keyReleaseEvent(self, event) that checks `if event.key() == Qt.Key.Key_Escape: self.clear()` then calls `super().keyReleaseEvent(event)` — placement BEFORE super() so the clear happens regardless of upstream handlers. Define class MainWindow(QMainWindow). Constructor signature: `def __init__(self, client) -> None` accepting an Elasticsearch client (matches the main.py W0-EXT-03 pattern `MainWindow(client)`); inside __init__ construct `self._engine = SearchEngine(client)` immediately. Initialize `self._current_seq: int = 0`. Set window title to "NitroFind — Ready" (em-dash U+2014, NOT hyphen) via setWindowTitle. Set minimum size to (1100, 700) via setMinimumSize. Build child widgets in this order (signal connections come AFTER all construction): self._search_bar = SearchLineEdit() with placeholderText="Search cars, manufacturers, models…" (U+2026), clearButtonEnabled=True, setFixedHeight(40); self._status_label = QLabel("") with setFixedHeight(20) and alignment Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter; self._debounce_timer = QTimer(self) with setSingleShot(True) and setInterval(300); self._filter_sidebar = FilterSidebar() with setMinimumWidth(160) and setMaximumWidth(240); self._result_list = QListWidget() with uniformItemSizes=False, setSelectionMode(QAbstractItemView.SelectionMode.SingleSelection), setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAlwaysOff), setItemDelegate(ResultDelegate(self._result_list)); self._detail_pane = QTextBrowser() with setOpenLinks(False), setOpenExternalLinks(False), and initial setHtml using the verbatim UI-SPEC empty-state HTML ("Select a result to read the article." inside a centered #80cbc4 paragraph). Build the layout: central QWidget with QVBoxLayout (setContentsMargins(8, 8, 8, 8), setSpacing(8)) holding the search row (QHBoxLayout containing self._search_bar only), then self._status_label, then a QSplitter(Qt.Orientation.Horizontal) with addWidget for sidebar/result_list/detail_pane in that order followed by setSizes([200, 320, 580]); call setCentralWidget(central_widget). Wire signals AFTER widget construction (mirroring engine.py T-03-06 and loading_window.py convention): self._search_bar.textChanged.connect(self._debounce_timer.start) — calling start() while running restarts the countdown; self._debounce_timer.timeout.connect(self._execute_search); for every checkbox in self._filter_sidebar.manufacturer_checks/era_checks/body_style_checks: connect cb.stateChanged to self._debounce_timer.start (so filter changes also trigger debounce); self._result_list.currentRowChanged.connect(self._on_result_hovered); self._result_list.itemActivated.connect(self._on_result_activated). Implement _execute_search(self): read query = self._search_bar.text().strip(); if query is empty, call self._result_list.clear(), self._status_label.setText(""), and reset window title to "NitroFind — Ready" — do NOT dispatch to ES; otherwise self._current_seq += 1, seq = self._current_seq, setWindowTitle(f"NitroFind — {query}"), setText("Searching…") on status, then call self._engine.search(query, filters=self._filter_sidebar.collect_filters(), callback=lambda results, took, s=seq: self._on_results(results, took, s), error_callback=self._on_search_error). Implement _on_results(self, results: list, took_ms: int, seq: int): early return if seq != self._current_seq (stale guard — Pitfall 1); self._result_list.clear(); if not results, setText("No results") and update detail pane with the empty-state HTML; else setText(f"{len(results)} results ({took_ms / 1000:.2f}s)") (UIPL-02 format); for each r in results, create QListWidgetItem(), setData(Qt.ItemDataRole.UserRole, _result_to_html(r)), setData(Qt.ItemDataRole.UserRole + 1, r), addItem(item); if results is non-empty, select row 0 (so arrow-key navigation has a starting position and detail pane shows first article). Implement _on_search_error(self, msg: str): logger.warning("Search failed: %s", msg) using % formatting (T-04-03); self._status_label.setText("Search failed. Check Elasticsearch connection.") — STATIC string only, never str(msg) in UI. Implement _on_result_hovered(self, row: int): early return if row < 0; item = self._result_list.item(row); if item is None return; result = item.data(Qt.ItemDataRole.UserRole + 1); update self._detail_pane.setHtml with the detail-pane template from UI-SPEC Component 6, preferring result.body if it is non-empty else result.excerpt. Implement _on_result_activated(self, item: QListWidgetItem): same body as _on_result_hovered but reads result from the item directly. Expose all six private widgets as @property accessors with type annotations (search_bar, status_label, result_list, detail_pane, filter_sidebar, debounce_timer) — mirrors loading_window.py lines 154-170 convention so tests can introspect without name-mangling. Use Qt6 fully-qualified enums only. All logger calls use % formatting — never f-strings inside logger.* calls (use f-strings only in setText/setHtml/setWindowTitle UI strings).
  </action>
  <verify>
    <automated>python -c "from nitrofind.ui.main_window import MainWindow, SearchLineEdit; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - source: nitrofind/ui/main_window.py contains `class MainWindow(QMainWindow)`
    - source: nitrofind/ui/main_window.py contains `class SearchLineEdit(QLineEdit)`
    - source: nitrofind/ui/main_window.py contains the literal `setInterval(300)`
    - source: nitrofind/ui/main_window.py contains the literal `setSingleShot(True)`
    - source: nitrofind/ui/main_window.py contains `Key_Escape` inside keyReleaseEvent method
    - source: nitrofind/ui/main_window.py contains `keyReleaseEvent` method definition (NOT keyPressEvent for Escape)
    - source: nitrofind/ui/main_window.py contains the literal `setSizes([200, 320, 580])`
    - source: nitrofind/ui/main_window.py contains the literal `"Search cars, manufacturers, models…"` placeholder
    - source: nitrofind/ui/main_window.py contains the literal `"Searching…"`
    - source: nitrofind/ui/main_window.py contains the literal `"No results"`
    - source: nitrofind/ui/main_window.py contains the literal `"Search failed. Check Elasticsearch connection."`
    - source: nitrofind/ui/main_window.py contains the literal `"NitroFind — Ready"` (em-dash U+2014)
    - source: nitrofind/ui/main_window.py contains `"Select a result to read the article."`
    - source: nitrofind/ui/main_window.py contains `if seq != self._current_seq` (stale guard)
    - source: nitrofind/ui/main_window.py imports from nitrofind.ui.result_delegate and nitrofind.ui.filter_sidebar
    - source: nitrofind/ui/main_window.py uses `logger = logging.getLogger(__name__)`
    - behavior: `python -c "from nitrofind.ui.main_window import MainWindow, SearchLineEdit"` exits 0
    - cli: `grep -n "keyPressEvent" nitrofind/ui/main_window.py | grep -i escape` returns no matches (Escape MUST use keyReleaseEvent per Pitfall 2)
    - cli: `grep -E "Qt\.AlignLeft\b|Qt\.Horizontal\b|Qt\.AlignCenter\b" nitrofind/ui/main_window.py` returns no matches (no Qt5 short-form enums)
    - cli: `grep -E "logger\.(debug|info|warning|error|critical).*f\"" nitrofind/ui/main_window.py` returns no matches (logger calls use % formatting only)
  </acceptance_criteria>
  <done>MainWindow imports cleanly, exposes all required widgets as properties, and contains every Copywriting Contract string verbatim. SearchLineEdit overrides keyReleaseEvent (not keyPressEvent) for Escape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build comprehensive pytest-qt suite covering SRCH-01/02/03/04 + UIPL-01/02/04</name>
  <files>tests/test_ui/test_main_window.py</files>
  <read_first>
    - nitrofind/ui/main_window.py (the just-implemented MainWindow + SearchLineEdit — confirm property names, signal targets, and method signatures the tests will assert against)
    - tests/test_loading_window.py (PyQt6 availability guard lines 23-30; qtbot widget construction pattern lines 33-51; signal waiting pattern lines 77-95)
    - tests/test_search/test_engine.py (mock client construction pattern; static MagicMock-based engine pattern)
    - .planning/phases/04-desktop-ui/04-VALIDATION.md (Per-Task Verification Map — 13 test functions listed; this file is the canonical test list)
    - .planning/phases/04-desktop-ui/04-PATTERNS.md (test_main_window.py section — exact qtbot+MagicMock idioms to copy)
    - .planning/phases/04-desktop-ui/04-RESEARCH.md (pytest-qt Test Pattern in Code Examples; Pitfall 6 — never instantiate QApplication directly in tests)
    - .planning/phases/04-desktop-ui/04-UI-SPEC.md (Interaction Contracts and State Mapping tables — define expected behaviors per requirement)
    - nitrofind/search/engine.py (SearchEngine.search signature so the MagicMock contract matches)
    - nitrofind/search/models.py (ArticleResult construction for fixture results)
  </read_first>
  <behavior>
    - Test 1 (SRCH-01 / test_debounce_timer_interval): window._debounce_timer.interval() == 300 AND window._debounce_timer.isSingleShot() is True
    - Test 2 (SRCH-01 / test_search_bar_triggers_debounce): qtbot.keyClicks(window._search_bar, "Ferrari") followed by qtbot.waitSignal(window._debounce_timer.timeout, timeout=500); engine_mock.search.assert_called_once() (debounce coalesced multiple keystrokes into one search)
    - Test 3 (SRCH-02 / test_result_list_populates): invoke the captured engine.search callback with a list of 3 ArticleResult fixtures and took_ms=42; assert window._result_list.count() == 3 AND status_label text equals "3 results (0.04s)"
    - Test 4 (SRCH-03 / test_result_click_updates_detail): with results populated, set window._result_list.setCurrentRow(0); assert window._detail_pane.toHtml() contains the title of the row-0 ArticleResult fixture
    - Test 5 (SRCH-04 / test_filter_preserved_on_retype): check a filter sidebar checkbox (e.g. manufacturer_checks["Ferrari"]); trigger a search; clear the search bar; type a new query; trigger second search; assert the second engine.search call's filters kwarg matches build_filter_clauses(manufacturer="Ferrari") (filters survived retyping)
    - Test 6 (UIPL-01 / test_highlight_tags_in_result_html): inject an ArticleResult with highlight_body=["The <b>Ferrari</b> 308 GTB"]; populate results; assert the QListWidgetItem at row 0's UserRole HTML string contains "<b>Ferrari</b> 308 GTB"
    - Test 7 (UIPL-02 / test_status_label_updated): trigger a callback with len=5 results and took_ms=80; assert status_label.text() == "5 results (0.08s)"
    - Test 8 (UIPL-02 / test_status_label_no_results): trigger callback with [] and took_ms=12; assert status_label.text() == "No results"
    - Test 9 (UIPL-04 / test_escape_clears_search): set search bar text to "Ferrari"; send Qt.Key.Key_Escape via qtbot.keyClick(window._search_bar, Qt.Key.Key_Escape); assert window._search_bar.text() == "" (Escape uses keyReleaseEvent — test must use keyClick which fires both press and release)
    - Test 10 (UIPL-04 / test_enter_opens_result): populate results; with row 0 selected, send Qt.Key.Key_Return via qtbot.keyClick on window._result_list; assert itemActivated handler ran (detail pane shows the row-0 article)
    - Test 11 (UIPL-04 / test_arrow_key_navigation): populate 3 results; window._result_list.setCurrentRow(0); send Qt.Key.Key_Down via qtbot.keyClick; assert window._result_list.currentRow() == 1; assert detail pane updated to row-1 article title
    - Test 12 (SRCH-01 stale guard / test_stale_results_discarded): trigger two searches in sequence (capturing both callbacks); invoke them in reverse order (newest first then older); assert the older callback's results were NOT applied to the result list (Pitfall 1)
    - Test 13 (T-04-03 / test_error_displays_static_string): invoke window._on_search_error("ConnectionError: 10061") manually; assert status_label.text() == "Search failed. Check Elasticsearch connection." (NOT containing "10061" — static string only)
    - Test 14 (T-04-03 / test_logger_uses_percent_formatting): grep the source file for `logger.*f"` patterns and assert no matches — copies the test_engine.py logger-formatting test
    - Test 15 (SRCH-01 empty query / test_empty_query_does_not_dispatch): with no text in search bar, manually call window._execute_search(); assert engine_mock.search NOT called AND status_label.text() == "" AND result_list count == 0
  </behavior>
  <action>
    Create tests/test_ui/test_main_window.py. Header section: a module docstring listing the requirement IDs covered, a comment block stating "Test strategy: qtbot fixture only — never instantiate QApplication (Pitfall 6); MagicMock SearchEngine — never live ES"; copy the PyQt6 availability guard block from tests/test_loading_window.py lines 23-30 verbatim (try import PyQt6 + PYQT6_AVAILABLE flag + pytestmark = pytest.mark.skipif). Implement a helper _make_window(qtbot, engine_mock=None) -> MainWindow that constructs MainWindow with a MagicMock client where MagicMock() side-effects monkeypatch SearchEngine to return engine_mock from the constructor — or alternatively, patch nitrofind.ui.main_window.SearchEngine to return a controllable mock via monkeypatch.setattr; document the chosen idiom in the file. Implement a helper _make_results(n: int = 3) -> list[ArticleResult] producing n ArticleResult instances with distinct titles ("Result 0", "Result 1", ...) and url/source_domain/score filled — used by tests 3,4,5,6,7,10,11. For each Behavior item, implement the test function with the same name in the Behavior list; use qtbot.addWidget(window) on every window construction so pytest-qt manages lifecycle. For test 12 (stale guard), invoke the callbacks directly (don't rely on QThreadPool ordering) — capture the callback argument from the engine.search MagicMock side_effect, store the seq id implicitly captured by the lambda, and call them in reverse order; assert the result list reflects only the more-recent search. For test 14 (logger formatting), implement: `def test_logger_uses_percent_formatting(): with open("nitrofind/ui/main_window.py", encoding="utf-8") as f: src = f.read(); assert not re.search(r'logger\.(debug|info|warning|error|critical)\(\s*f"', src), "logger calls must use % formatting"`. For test 9 (escape), prefer qtbot.keyClick(window._search_bar, Qt.Key.Key_Escape) which fires both press and release events — this exercises the keyReleaseEvent override. Use Qt6 fully-qualified enums throughout (Qt.Key.Key_Escape, Qt.Key.Key_Return, Qt.Key.Key_Down). Imports: `from unittest.mock import MagicMock`, `import pytest`, `import re` (for test 14), Qt and enums from PyQt6.QtCore, ArticleResult from nitrofind.search.models. Do NOT import or construct QApplication directly.
  </action>
  <verify>
    <automated>pytest tests/test_ui/test_main_window.py -x -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - source: tests/test_ui/test_main_window.py contains the literal `def test_debounce_timer_interval(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_search_bar_triggers_debounce(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_result_list_populates(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_result_click_updates_detail(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_filter_preserved_on_retype(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_highlight_tags_in_result_html(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_status_label_updated(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_escape_clears_search(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_enter_opens_result(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_arrow_key_navigation(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_stale_results_discarded(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_error_displays_static_string(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_logger_uses_percent_formatting(`
    - source: tests/test_ui/test_main_window.py contains the literal `def test_empty_query_does_not_dispatch(`
    - source: tests/test_ui/test_main_window.py contains `pytestmark = pytest.mark.skipif`
    - source: tests/test_ui/test_main_window.py does NOT contain `QApplication(sys.argv)` (Pitfall 6 — never construct QApplication directly)
    - source: tests/test_ui/test_main_window.py does NOT contain `keyPressEvent` (Escape test uses keyClick which routes through keyReleaseEvent)
    - behavior: pytest tests/test_ui/test_main_window.py exits 0
    - behavior: all 15 enumerated tests pass
    - cli: `grep -c "def test_" tests/test_ui/test_main_window.py` returns >= 15
    - cli: `pytest tests/ -x -m "not integration"` exits 0 (full Phase 1-4 non-integration suite green)
  </acceptance_criteria>
  <done>All 15 test functions covering SRCH-01/02/03/04, UIPL-01/02/04, T-04-03 static error string, stale-result guard, empty-query short-circuit, and logger formatting pass. Full non-integration test suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard input → SearchEngine.search query_text | Search bar text crosses from user into ES via Phase 3's multi_match.query string |
| ES search_failed message → status label | Worker thread error string crosses to main thread; must NOT be surfaced verbatim |
| Filter checkbox state → ES term query | Hardcoded filter values (Phase 4 v1) cross to ES via build_filter_clauses |
| ArticleResult body/excerpt → QTextBrowser detail pane | ES document body text crosses to Qt Rich Text rendering |
| _SearchWorker results → main thread | Cross-thread signal delivery (QueuedConnection) — must respect stale-result guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Query injection via search bar | mitigate | Already enforced in Phase 3 (T-03-01): SearchEngine.search places query_text inside multi_match.query string value only — never as DSL key. MainWindow passes user text through unmodified; Phase 3 boundary protects ES. |
| T-04-02 | Tampering (XSS) | QTextBrowser rendering of body/excerpt | mitigate | QTextBrowser renders Qt Rich Text only — no QtWebEngine, no <script> execution. setOpenLinks(False) AND setOpenExternalLinks(False) explicit per UI-SPEC Component 6 — prevents navigation if HTML contains anchors. |
| T-04-03 | Information Disclosure | Raw exception text in UI status label | mitigate | _on_search_error logs raw msg via logger.warning("Search failed: %s", msg) but sets status label ONLY to the static literal "Search failed. Check Elasticsearch connection." Tested by test_error_displays_static_string. |
| T-04-04 | Denial of Service | ES call from main thread blocking event loop | mitigate | MainWindow NEVER calls client.search() directly. All ES I/O routed through self._engine.search() which dispatches to _SearchWorker on QThreadPool (Phase 3 contract). MainWindow only connects callbacks via QueuedConnection. |
| T-04-05 | Tampering | Stale result overwriting current display | mitigate | _current_seq monotonic counter incremented before every dispatch; _on_results early-return if seq mismatch (Pattern 2 from RESEARCH.md, Pitfall 1). Tested by test_stale_results_discarded. |
| T-04-06 | Spoofing | itemActivated firing from synthetic input | accept | Single-user local desktop app — no untrusted UI input source. itemActivated handler reads only the ArticleResult stored in UserRole+1 which originates from a trusted local ES instance. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — uses existing PyQt6 6.11.0 and pytest-qt 4.5.0. RESEARCH.md Package Legitimacy Audit confirms no new installs required for Phase 4. |
</threat_model>

<verification>
- `pytest tests/test_ui/test_main_window.py -x -m "not integration"` exits 0
- `pytest tests/ -x -m "not integration"` exits 0 (full Phase 1-4 unit suite green)
- `python -c "from nitrofind.ui.main_window import MainWindow, SearchLineEdit"` exits 0
- nitrofind/ui/main_window.py contains every Copywriting Contract string verbatim
- SearchLineEdit uses keyReleaseEvent for Escape (NOT keyPressEvent)
- All Qt enums use Qt6 fully-qualified form
- Logger calls use % formatting only
- No raw exception text reaches the status label (T-04-03 enforced)
</verification>

<success_criteria>
- MainWindow replaces StubMainWindow with full search/filter/detail UI
- Debounce timer (300ms single-shot) restarts on textChanged and on filter stateChanged
- _execute_search increments _current_seq, formats window title, dispatches via SearchEngine.search with collected filters
- _on_results enforces stale-result guard, populates QListWidget via _result_to_html + ArticleResult data role, updates status label with "N results (T:.2fs)" / "No results"
- _on_search_error sets static error string only, logs raw via % formatting
- _on_result_hovered and _on_result_activated update detail pane preferring body over excerpt
- Empty query short-circuits before ES dispatch
- All 15 pytest-qt unit tests pass
- Full non-integration test suite remains green
</success_criteria>

<output>
Create `.planning/phases/04-desktop-ui/04-03-SUMMARY.md` when done. Include:
- Final method signatures of MainWindow public surface (search, _execute_search, _on_results, etc.)
- The chosen MagicMock-SearchEngine injection idiom in tests (monkeypatch vs constructor injection) for Plan 04 reference
- Test counts (expect ~15 tests, all green)
- Any deviations from UI-SPEC Copywriting Contract (e.g., minor copy adjustments) with rationale
- Confirmation that Plan 04 can swap `StubMainWindow()` for `MainWindow(client)` in main.py with no other changes required
</output>
</content>
</invoke>