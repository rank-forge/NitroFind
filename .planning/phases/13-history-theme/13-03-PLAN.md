---
phase: 13-history-theme
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified: []
autonomous: false
requirements: [HIST-01, HIST-02, THME-01]
nyquist_compliant: true
must_haves:
  truths:
    - "A human confirms the history list shows the last 10 unique queries, most recent first, after several searches"
    - "A human confirms clicking a history item repopulates the search box and re-executes the query"
    - "A human confirms the theme toggle switches dark/light without a page reload"
    - "A human confirms the light theme persists across a hard refresh with no flash of dark theme (no FOUC)"
    - "A human confirms search, pagination, filters, and sort still work after the Phase 13 additions"
  artifacts: []
  key_links: []
---

<objective>
Human UI verification of the Phase 13 features that cannot be tested server-side. The project has no browser automation (no Playwright/Selenium), so actual localStorage persistence, CSS-variable theme switching, history click re-execution, and FOUC-free reload are verified by a person driving the app in a real browser. This mirrors the closing human-verification plan used in Phases 10, 11, and 12.

Purpose: Prove the JS behavior of HIST-01, HIST-02, and THME-01 works end-to-end in a browser and that no existing search/pagination/filter/sort flow regressed.
Output: Human approval (or a defect list) recorded when the checkpoint resumes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-history-theme/13-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm full automated suite is green before human verification</name>
  <files></files>
  <read_first>
    - .planning/phases/13-history-theme/13-VALIDATION.md (Sampling Rate — full suite must be green before verification)
  </read_first>
  <action>
    Run the full non-integration test suite to confirm no regressions before handing off to the human. If any test fails, stop and report — do not proceed to the human checkpoint until the suite is green.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/ -m "not integration"` exits 0
    - The three Phase 13 template tests (`test_template_has_history_list`, `test_template_has_theme_toggle`, `test_template_has_fouc_prevention_script`) are among the passing tests
  </acceptance_criteria>
  <done>Full non-integration suite passes, including the three Phase 13 template-structure tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UI verification of history + theme in a browser</name>
  <action>This is a human-verification checkpoint — no autonomous implementation. The executor must start the Flask dev server (and Elasticsearch, so search/pagination/filter regression checks can run against the indexed corpus), present the verification steps below to the user, and pause for the resume signal before marking the phase complete.</action>
  <what-built>
    Search history (HIST-01/02): the home view now shows a recent-searches list under the search box; entries are the last 10 unique queries, most recent first; clicking one re-runs it.
    Theme toggle (THME-01): a "Light"/"Dark" button in the home view top-right and in the results-view top bar switches the whole UI between dark and light themes; the choice persists in localStorage across reloads with no flash of the wrong theme.
  </what-built>
  <how-to-verify>
    1. Start the app and open it in a browser (the usual `localhost:5000` dev flow). Wait for the search box to enable.
    2. HIST-01 — run several distinct searches (e.g. "ferrari", "mustang", "porsche 911", then "ferrari" again). Return to the home view (clear the box or press Escape). Confirm the history list under the search box shows the queries most-recent-first, that "ferrari" appears only once and has moved to the top, and that no partial/typed fragments (e.g. "ferr") appear.
    3. HIST-02 — click a history entry. Confirm both the home and results search inputs show that query text and the results update to that search.
    4. THME-01 (toggle) — click the theme toggle. Confirm the entire UI switches dark↔light instantly with NO page reload, that text/accent remain readable in light mode (the teal accent is legible on white — not washed out), and that the button label flips between "Light" and "Dark".
    5. THME-01 (persist + FOUC) — with light theme active, hard-refresh (Ctrl+Shift+R). Confirm the page loads directly in light theme with NO brief flash of dark theme before settling.
    6. Regression — confirm search-as-you-type, Previous/Next pagination, the manufacturer/era/body/year/country filters, and the sort buttons all still work with the new elements present.
  </how-to-verify>
  <verify>
    <human-check>All six browser checks in how-to-verify pass against a live server (HIST-01 history population/order/dedup, HIST-02 click re-execute, THME-01 live theme switch, THME-01 persistence with no FOUC, and no regression in search/pagination/filter/sort).</human-check>
  </verify>
  <resume-signal>Type "approved" if all six checks pass, or describe any issues (which check, what happened).</resume-signal>
  <done>The user confirms HIST-01, HIST-02, and THME-01 work end-to-end in the browser and that no existing search/pagination/filter/sort flow regressed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

This plan creates no new source symbols. It verifies the symbols introduced in 13-02 (`history-list`, `theme-toggle`, `theme-toggle-results`, `html[data-theme="light"]`, `nitrofind-history`, `nitrofind-theme`, and the history/theme JS functions).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human tester → running app | Manual verification session against the local dev server; no new code or input path introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Repudiation | manual verification outcome | mitigate | Human approval or defect list is recorded verbatim in the plan SUMMARY when the checkpoint resumes, creating a traceable sign-off record. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (RESEARCH Package Legitimacy Audit: none). No supply-chain surface. |

The threats mitigated in code (T-13-01 textContent, T-13-02/04 try/catch, T-13-03 namespaced keys) are exercised behaviorally here: step 2/3 confirm history renders safely, and the app must not crash on empty/corrupt storage.
</threat_model>

<verification>
- Full non-integration suite green (Task 1).
- All six human-verification checks pass (Task 2) — history population/order/dedup, history click re-execute, live theme switch, persistence + no FOUC, and no regression in search/pagination/filter/sort.
</verification>

<success_criteria>
A human confirms in a real browser that HIST-01, HIST-02, and THME-01 behave per the Phase 13 success criteria and that no existing flow regressed. Approval (or a defect list) is recorded on resume.
</success_criteria>

<output>
Create `.planning/phases/13-history-theme/13-03-SUMMARY.md` when done, recording the human approval or defect list verbatim.
</output>
