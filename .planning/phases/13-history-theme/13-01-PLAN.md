---
phase: 13-history-theme
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_server.py
autonomous: true
requirements: [HIST-01, HIST-02, THME-01]
nyquist_compliant: true
must_haves:
  truths:
    - "Three new template-structure tests exist in tests/test_server.py and are collected by pytest"
    - "All three new tests are RED (fail) against the current templates/index.html (which lacks history-list, theme-toggle, and the FOUC script)"
    - "The three tests use the existing client_not_ready fixture — no new fixture or test file is created"
  artifacts:
    - path: "tests/test_server.py"
      provides: "RED template-structure scaffold for HIST-01/02 (history-list container) and THME-01 (theme-toggle button + FOUC script)"
      contains: "def test_template_has_history_list"
  key_links:
    - from: "tests/test_server.py"
      to: "nitrofind.server.app (GET /)"
      via: "client_not_ready.get('/') asserting rendered HTML byte substrings"
      pattern: "client_not_ready.get"
---

<objective>
Create the Wave 0 failing-test scaffold for Phase 13. Add three new template-structure tests to `tests/test_server.py` that assert the rendered `GET /` HTML contains the DOM surface Phase 13 introduces: the `id="history-list"` container (HIST-01/02), the `id="theme-toggle"` button (THME-01), and the inline FOUC-prevention script markers `nitrofind-theme` + `dataset.theme` (THME-01).

Purpose: Lock the DOM-structure contract in tests BEFORE `templates/index.html` is modified (RED), so the implementation plan (13-02) has an executable definition of done. These are the only automatable checks for this phase — actual localStorage/theme/history JS behavior is manual-only (no browser automation in the project).
Output: `tests/test_server.py` with three new test functions appended after the existing `test_root_*` block, all RED against the current template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-history-theme/13-VALIDATION.md
@tests/test_server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add three RED template-structure tests to test_server.py</name>
  <files>tests/test_server.py</files>
  <read_first>
    - tests/test_server.py (see the `client_not_ready` fixture at lines 27-32 and the existing `test_root_returns_html` / `test_root_uses_template` pattern at lines 115-128 — copy the fixture usage and byte-substring assertion style exactly)
    - .planning/phases/13-history-theme/13-PATTERNS.md (section `tests/test_server.py` — the three target test bodies are specified verbatim there)
    - .planning/phases/13-history-theme/13-RESEARCH.md (Phase Requirements → Test Map — confirms the exact byte markers each test asserts)
  </read_first>
  <action>
    Append three new test functions to the end of tests/test_server.py, after `test_root_uses_template` (currently ends at line 128). Each uses the existing `client_not_ready` fixture, issues `client_not_ready.get("/")`, asserts `resp.status_code == 200`, then asserts byte-substring presence:
    - `test_template_has_history_list` — asserts `b'id="history-list"'` in `resp.data` (HIST-01/02 DOM container).
    - `test_template_has_theme_toggle` — asserts `b'id="theme-toggle"'` in `resp.data` (THME-01 toggle button).
    - `test_template_has_fouc_prevention_script` — asserts BOTH `b'nitrofind-theme'` in `resp.data` AND `b'dataset.theme'` in `resp.data` (THME-01 inline FOUC script markers).
    Each function gets a one-line docstring naming its requirement (e.g. "THME-01 DOM structure"). Do NOT modify templates/index.html in this plan — these tests MUST fail now. Do not create a new test file or fixture.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_server.py::test_template_has_history_list tests/test_server.py::test_template_has_theme_toggle tests/test_server.py::test_template_has_fouc_prevention_script -x 2>&1 | grep -q "3 failed\|failed" && echo RED_CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_server.py` contains `def test_template_has_history_list(client_not_ready):`
    - `tests/test_server.py` contains `def test_template_has_theme_toggle(client_not_ready):`
    - `tests/test_server.py` contains `def test_template_has_fouc_prevention_script(client_not_ready):`
    - `tests/test_server.py` contains the substrings `b'id="history-list"'`, `b'id="theme-toggle"'`, `b'nitrofind-theme'`, and `b'dataset.theme'`
    - Running the three tests: all three FAIL (RED) — the current template has none of these markers
    - The pre-existing tests still pass: `python3 -m pytest tests/test_server.py::test_root_returns_html tests/test_server.py::test_root_uses_template -x` exits 0
    - No new pytest fixture or test file is created (grep confirms only additions to test_server.py)
  </acceptance_criteria>
  <done>Three new template-structure tests are collected by pytest and are RED against the current template; the existing test_root_* tests remain green; no new file or fixture added.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

New symbols created by THIS plan (exclude from source-drift verification — they do not exist yet):

- Test functions: `test_template_has_history_list`, `test_template_has_theme_toggle`, `test_template_has_fouc_prevention_script`
- Byte-marker assertions referencing symbols created later in 13-02: `id="history-list"`, `id="theme-toggle"`, `nitrofind-theme`, `dataset.theme`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → Flask app | Tests exercise the rendered template via the in-process Flask test_client; no untrusted external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | tests/test_server.py assertions | accept | Test-only file; asserts static byte substrings against rendered HTML. No runtime attack surface; no user input reaches this code. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan or phase (RESEARCH Package Legitimacy Audit: zero packages). No supply-chain surface introduced. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_server.py -x` collects the three new tests (they appear in the run).
- The three new tests are RED; `test_root_returns_html` and `test_root_uses_template` remain green.
</verification>

<success_criteria>
Three RED template-structure tests exist in `tests/test_server.py`, using the existing `client_not_ready` fixture, asserting the `history-list` container, `theme-toggle` button, and FOUC-script markers. They fail against the current template and provide the executable definition of done for plan 13-02.
</success_criteria>

<output>
Create `.planning/phases/13-history-theme/13-01-SUMMARY.md` when done.
</output>
