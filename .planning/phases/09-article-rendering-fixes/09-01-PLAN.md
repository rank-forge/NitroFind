---
phase: 09-article-rendering-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scraper/test_cleaner.py
  - tests/test_scraper/test_wikipedia.py
  - tests/test_scraper/test_blogs.py
  - tests/test_search/test_models.py
  - tests/test_search/test_api_search.py
  - tests/test_es_schema.py
autonomous: true
requirements: [BUG-01, BUG-02]

must_haves:
  truths:
    - "Every Phase 9 behavior has a failing test that exists before implementation (RED state)"
    - "Test commands from 09-VALIDATION.md resolve to real test functions"
  artifacts:
    - path: "tests/test_scraper/test_cleaner.py"
      provides: "strip_nav_sections() tests"
      contains: "def test_strip_nav_sections_removes_references"
    - path: "tests/test_scraper/test_wikipedia.py"
      provides: "_clean_wikipedia_html() and body_html doc tests"
      contains: "def test_clean_wikipedia_html_preserves_tables"
    - path: "tests/test_scraper/test_blogs.py"
      provides: "blog body_html and noise-exclusion tests"
      contains: "def test_doc_has_body_html_field"
    - path: "tests/test_search/test_models.py"
      provides: "ArticleResult.body_html tests"
      contains: "def test_body_html_field_default"
    - path: "tests/test_es_schema.py"
      provides: "body_html mapping test"
      contains: "def test_body_html_field_present"
    - path: "tests/test_search/test_api_search.py"
      provides: "updated result-shape test including body_html"
      contains: "body_html"
  key_links:
    - from: "tests/test_scraper/test_cleaner.py"
      to: "nitrofind.scraper.cleaner.strip_nav_sections"
      via: "import"
      pattern: "from nitrofind.scraper.cleaner import .*strip_nav_sections"
    - from: "tests/test_scraper/test_wikipedia.py"
      to: "nitrofind.scraper.wikipedia._clean_wikipedia_html"
      via: "import"
      pattern: "from nitrofind.scraper.wikipedia import _clean_wikipedia_html"
---

<objective>
Create the Wave 0 failing-test scaffold for Phase 9. Per 09-VALIDATION.md, every Phase 9 behavior (BUG-01 table rendering + body_html field, BUG-02 nav-text exclusion) must have an automated test that exists and fails BEFORE the production code is written. This plan writes ONLY test code. No production files are touched.

Purpose: Establish RED state so the implementation plans (09-02, 09-03) have concrete acceptance gates. Without this, the Nyquist sampling contract in 09-VALIDATION.md has no targets.
Output: 6 test files updated/created with new failing tests that import symbols not yet defined (strip_nav_sections, _clean_wikipedia_html, body_html field).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-article-rendering-fixes/09-RESEARCH.md
@.planning/phases/09-article-rendering-fixes/09-PATTERNS.md
@.planning/phases/09-article-rendering-fixes/09-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols introduced across Phase 9 (excluded from drift verification — these are newly created):
- `nitrofind.scraper.cleaner.strip_nav_sections(content: str) -> str` (function)
- `nitrofind.scraper.cleaner._WIKIPEDIA_NAV_SECTIONS` (module constant, frozenset)
- `nitrofind.scraper.wikipedia._clean_wikipedia_html(raw_html: str) -> str` (function)
- `nitrofind.scraper.wikipedia._WIKIPEDIA_NOISE_SELECTORS` (module constant, list)
- `nitrofind.scraper.wikipedia.WikipediaScraper._fetch_html_body(self, pageid: int) -> str` (method)
- `nitrofind.scraper.blogs._BLOG_NOISE_SELECTORS` (module constant, str)
- `body_html` document key (scraper doc dict, both wikipedia.py and blogs.py)
- `body_html` ES mapping field in `CAR_ARTICLES_MAPPING["properties"]` (type: text, index: False)
- `ArticleResult.body_html` (dataclass field, str default "")
- `body_html` key in API response from `_result_to_api_dict()` in server.py
- `--recreate` CLI flag in `scripts/scraper.py`
- Test functions: `test_strip_nav_sections_removes_references`, `test_strip_nav_sections_removes_external_links`, `test_strip_nav_sections_preserves_content_headings`, `test_strip_nav_sections_empty_input`, `test_clean_wikipedia_html_preserves_tables`, `test_clean_wikipedia_html_removes_navboxes`, `test_clean_wikipedia_html_empty_on_no_mw_parser_output`, `test_fetch_and_build_doc_returns_body_html`, `test_doc_has_body_html_field`, `test_breadcrumb_excluded_from_body`, `test_related_articles_excluded_from_body`, `test_body_html_field_default`, `test_article_result_body_html_from_es_hit`, `test_body_html_field_present`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scraper test scaffold (cleaner, wikipedia, blogs)</name>
  <files>tests/test_scraper/test_cleaner.py, tests/test_scraper/test_wikipedia.py, tests/test_scraper/test_blogs.py</files>
  <read_first>
    - tests/test_scraper/test_cleaner.py (existing pure-function test patterns, import line ~16)
    - tests/test_scraper/test_wikipedia.py (existing MagicMock page patterns, SAMPLE_CONFIG, mock_state)
    - tests/test_scraper/test_blogs.py (existing _make_scraper, _make_mock_response, _SAMPLE_CONFIG, _VALID_ARTICLE_HTML helpers)
    - .planning/phases/09-article-rendering-fixes/09-PATTERNS.md (Test File Patterns section — exact test bodies)
  </read_first>
  <action>
    Add the failing tests defined verbatim in 09-PATTERNS.md "Test File Patterns" section to the three scraper test files.

    test_cleaner.py: update import to `from nitrofind.scraper.cleaner import make_excerpt, compute_era_bucket, parse_year, strip_nav_sections`. Add four tests: `test_strip_nav_sections_removes_references`, `test_strip_nav_sections_removes_external_links`, `test_strip_nav_sections_preserves_content_headings`, `test_strip_nav_sections_empty_input`.

    test_wikipedia.py: add `from nitrofind.scraper.wikipedia import _clean_wikipedia_html` and four tests: `test_clean_wikipedia_html_preserves_tables`, `test_clean_wikipedia_html_removes_navboxes`, `test_clean_wikipedia_html_empty_on_no_mw_parser_output`, `test_fetch_and_build_doc_returns_body_html`. The last test patches `WikipediaScraper._fetch_html_body` to return `"<div>html</div>"` and asserts `"body_html" in doc`. Reuse the existing MagicMock page construction pattern and SAMPLE_CONFIG/mock_state from the file.

    test_blogs.py: add module-level fixture `_ARTICLE_WITH_NOISE_HTML` (containing a `.breadcrumb`, `.related-articles`, `.newsletter-signup` div per 09-PATTERNS.md) and three tests: `test_doc_has_body_html_field`, `test_breadcrumb_excluded_from_body`, `test_related_articles_excluded_from_body`. Use the existing `_make_scraper`, `_make_mock_response`, `_SAMPLE_CONFIG` helpers and the first configured blog target.

    These tests MUST fail at this stage (strip_nav_sections, _clean_wikipedia_html, body_html do not exist yet) — that is the intended RED state. Do NOT add any production code to nitrofind/.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_scraper/test_cleaner.py tests/test_scraper/test_wikipedia.py tests/test_scraper/test_blogs.py -q 2>&1 | grep -E "error|failed|ImportError|collected" | head</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_scraper/test_cleaner.py imports strip_nav_sections and contains `def test_strip_nav_sections_removes_references`
    - tests/test_scraper/test_wikipedia.py contains `def test_clean_wikipedia_html_preserves_tables` and `def test_fetch_and_build_doc_returns_body_html`
    - tests/test_scraper/test_blogs.py contains `def test_doc_has_body_html_field` and `def test_breadcrumb_excluded_from_body`
    - Running the three files shows collection errors or failures (RED) caused by missing strip_nav_sections / _clean_wikipedia_html / body_html — NOT by syntax errors in the test files
    - No file under nitrofind/ is modified by this task
  </acceptance_criteria>
  <done>The three scraper test files contain the new tests and they fail because the referenced production symbols do not exist yet.</done>
</task>

<task type="auto">
  <name>Task 2: Search/API/schema test scaffold (models, api_search, es_schema)</name>
  <files>tests/test_search/test_models.py, tests/test_search/test_api_search.py, tests/test_es_schema.py</files>
  <read_first>
    - tests/test_search/test_models.py (existing `test_article_result_all_fields` expected_fields set ~line 57, `test_article_result_body_default_empty_string` ~line 160)
    - tests/test_search/test_api_search.py (existing `test_search_result_shape` ~line 111, `client_with_search` mock ES hit fixture ~lines 40-56)
    - tests/test_es_schema.py (existing `test_mapping_has_required_fields` assertion block ~lines 41-44)
    - .planning/phases/09-article-rendering-fixes/09-PATTERNS.md (Test File Patterns — test_models, test_api_search, test_es_schema subsections)
  </read_first>
  <action>
    Add the failing tests defined verbatim in 09-PATTERNS.md to the three files.

    test_models.py: add `"body_html"` to the `expected_fields` set in `test_article_result_all_fields`. Add `test_body_html_field_default` (asserts default `""`) and `test_article_result_body_html_from_es_hit` (asserts from_es_hit populates body_html from _source and falls back to `""` when missing).

    test_api_search.py: update the `test_search_result_shape` key-set assertion to `{"title", "url", "source_domain", "excerpt", "body", "body_html", "score", "took_ms"}` — this adds the Phase 9 `body_html` key AND fixes the pre-existing missing `body` key (Pitfall 7). Add `"body_html": "<p>Full text.</p>"` (and `"body": "Full text."` if not already present) to the mock ES hit `_source` in the `client_with_search` fixture so the shape assertion can pass once production code lands.

    test_es_schema.py: add `test_body_html_field_present` asserting `"body_html" in CAR_ARTICLES_MAPPING["properties"]`, `props["body_html"]["type"] == "text"`, and `props["body_html"]["index"] is False`.

    These tests MUST fail now (body_html field does not exist on ArticleResult, in the API response, or in the ES mapping). Do NOT add production code.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_search/test_models.py tests/test_search/test_api_search.py tests/test_es_schema.py -q 2>&1 | grep -E "error|failed|collected" | head</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_search/test_models.py contains `def test_body_html_field_default` and `def test_article_result_body_html_from_es_hit`, and `body_html` appears in the `expected_fields` set
    - tests/test_search/test_api_search.py `test_search_result_shape` asserts a key set containing both `body` and `body_html`
    - tests/test_es_schema.py contains `def test_body_html_field_present` asserting `index is False`
    - Running the three files shows failures (RED) attributable to the missing body_html field — NOT to test-file syntax errors
    - No file under nitrofind/ is modified by this task
  </acceptance_criteria>
  <done>The three search/schema test files contain the new tests and they fail because the body_html field is not yet implemented in production code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none for this plan) | Test-only plan; no runtime trust boundary crossed. The XSS/innerHTML boundary is handled in 09-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | pip installs | accept | Phase 9 introduces zero new packages (09-RESEARCH.md Package Legitimacy Audit: none). No install task exists. |
| T-09-T1 | Tampering | test fixtures | accept | Test HTML fixtures are static literals authored in-repo; no external fetch in unit tests. |
</threat_model>

<verification>
Run the full Phase 9 test target — expect RED (failures), confirming the scaffold targets unimplemented behavior:
`python3 -m pytest tests/test_scraper/ tests/test_search/ tests/test_es_schema.py -m "not integration" -q`
All new Phase 9 tests should appear as failed/errored, NOT as passed or skipped.
</verification>

<success_criteria>
- All 14 new/updated Phase 9 tests exist and are collected by pytest without test-file syntax errors
- The new tests fail (RED) because strip_nav_sections, _clean_wikipedia_html, the body_html field, and the body_html mapping do not yet exist
- No production file under nitrofind/ or static/ or scripts/ is modified
</success_criteria>

<output>
Create `.planning/phases/09-article-rendering-fixes/09-01-SUMMARY.md` when done
</output>
