---
phase: 09-article-rendering-fixes
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - nitrofind/search/models.py
  - nitrofind/search/query_builder.py
  - nitrofind/server.py
  - static/js/app.js
  - static/css/style.css
autonomous: true
requirements: [BUG-01]

must_haves:
  truths:
    - "ArticleResult exposes body_html populated from the ES _source"
    - "GET /api/search responses include a body_html key for every result"
    - "The article detail view renders body_html as HTML (tables visible), falling back to plain-text body when body_html is empty"
    - "Rendered article tables have visible borders and cell padding"
  artifacts:
    - path: "nitrofind/search/models.py"
      provides: "ArticleResult.body_html field + from_es_hit mapping"
      contains: "body_html"
    - path: "nitrofind/search/query_builder.py"
      provides: "body_html in _source list"
      contains: "body_html"
    - path: "nitrofind/server.py"
      provides: "body_html in _result_to_api_dict()"
      contains: "body_html"
    - path: "static/js/app.js"
      provides: "innerHTML article rendering with body fallback"
      contains: "innerHTML"
    - path: "static/css/style.css"
      provides: "#article-body table styling"
      contains: "#article-body table"
  key_links:
    - from: "nitrofind/server.py"
      to: "ArticleResult.body_html"
      via: "_result_to_api_dict body_html key"
      pattern: "body_html.*result\\.body_html"
    - from: "static/js/app.js"
      to: "result.body_html"
      via: "articleBody.innerHTML assignment"
      pattern: "innerHTML\\s*=\\s*htmlContent"
    - from: "nitrofind/search/query_builder.py"
      to: "ES _source"
      via: "body_html in _source list"
      pattern: "body_html"
---

<objective>
Implement the display half of Phase 9 BUG-01: thread the `body_html` field from the ES `_source` through the result model, query `_source` list, API response, and into the browser article view via `innerHTML`, plus minimal table CSS so rendered Wikipedia/blog tables are legible. The plain-text `body` remains the fallback for articles without HTML.

Purpose: The scraper (09-02) produces body_html, but it is invisible until the read path returns it and the frontend renders it as HTML rather than text. This plan makes tables actually appear in the detail pane.
Output: GREEN for the models and api_search shape tests from 09-01; article view renders HTML tables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-article-rendering-fixes/09-RESEARCH.md
@.planning/phases/09-article-rendering-fixes/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
See 09-01-PLAN.md `<artifacts_this_phase_produces>` for the full Phase 9 symbol list. Symbols this plan creates: `ArticleResult.body_html` (dataclass field + from_es_hit mapping); `body_html` in query_builder `_source` list; `body_html` key in `_result_to_api_dict()`; `innerHTML` article rendering in app.js `openArticle()`; `#article-body table` CSS rules in style.css.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: body_html through model + query _source + API response</name>
  <files>nitrofind/search/models.py, nitrofind/search/query_builder.py, nitrofind/server.py</files>
  <read_first>
    - nitrofind/search/models.py (ArticleResult dataclass fields incl. `body: str = ""` ~line 74, from_es_hit `body=src.get("body", "")` ~line 115)
    - nitrofind/search/query_builder.py (`build_search_body` `_source` list ~lines 241-245)
    - nitrofind/server.py (`_result_to_api_dict()` return dict ~lines 95-120)
    - .planning/phases/09-article-rendering-fixes/09-PATTERNS.md (models.py, query_builder.py, server.py Pattern Assignments — exact lines)
    - .planning/phases/09-article-rendering-fixes/09-RESEARCH.md (Patterns 6-7, Pitfall 6)
  </read_first>
  <behavior>
    - ArticleResult().body_html defaults to "" when constructed without it
    - ArticleResult.from_es_hit({"_source": {"body_html": "<div>...</div>"}}) sets body_html to that HTML; missing key yields ""
    - GET /api/search result items include a body_html key (test_search_result_shape expects {title, url, source_domain, excerpt, body, body_html, score, took_ms})
  </behavior>
  <action>
    In models.py: add `body_html: str = ""` to the ArticleResult dataclass immediately after the `body` field (with a Phase 9 comment), and add `body_html=src.get("body_html", "")` to the `from_es_hit` constructor call immediately after the `body=` line (Pitfall 6 — without this, server.py's `result.body_html` raises AttributeError).

    In query_builder.py: append `"body_html"` to the `_source` list in `build_search_body()` (after `"body"` on the first line of the list).

    In server.py: add `"body_html": result.body_html` to the dict returned by `_result_to_api_dict()`, immediately after the `"body": result.body` entry.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_search/test_models.py tests/test_search/test_api_search.py -m "not integration" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/test_search/test_models.py tests/test_search/test_api_search.py -m "not integration" -q` exits 0
    - models.py ArticleResult has a `body_html: str = ""` field and from_es_hit calls `src.get("body_html", "")`
    - query_builder.py `_source` list contains `"body_html"`
    - server.py `_result_to_api_dict` return dict contains `"body_html": result.body_html`
  </acceptance_criteria>
  <done>body_html is read from ES into ArticleResult and returned by GET /api/search; model + api_search shape tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: article view innerHTML rendering + table CSS</name>
  <files>static/js/app.js, static/css/style.css</files>
  <read_first>
    - static/js/app.js (`openArticle()` ~lines 160-165, existing safe `excerpt.innerHTML` precedent ~line 146)
    - static/css/style.css (`#article-body` block ~lines 302-308)
    - .planning/phases/09-article-rendering-fixes/09-PATTERNS.md (app.js Pattern Assignment — exact openArticle replacement)
    - .planning/phases/09-article-rendering-fixes/09-RESEARCH.md (Pattern 8, Open Question 3 — table CSS recommendation)
  </read_first>
  <action>
    In app.js `openArticle()`: keep `articleTitle.textContent` and `articleSource.textContent` as-is. Replace the `articleBody.textContent = result.body || "No content available."` line with: read `const htmlContent = result.body_html || "";` then if `htmlContent` is truthy set `articleBody.innerHTML = htmlContent;` else `articleBody.textContent = result.body || "No content available.";`. Add a comment noting innerHTML is intentional and matches the existing `excerpt.innerHTML` rationale (scraper strips script/style/on* — local single-user offline app). Leave the `transitionTo("article")` call in place.

    In style.css: add rules scoped to `#article-body table`, `#article-body th`, `#article-body td`. Give the table `border-collapse: collapse;` and a sensible `margin` (e.g. `1rem 0`); give th/td a `border: 1px solid var(--border)` (use the existing border color token if one exists in :root, otherwise `#444`/a theme-consistent value found in style.css) and `padding: 0.4rem 0.6rem;`. Give `th` `text-align: left;` and a subtle header background using an existing surface token. Keep the change minimal — this is legibility, not redesign. Note: the design-taste-frontend skill does not apply here (it is scoped to landing pages/portfolios, explicitly not data tables / product UI); standard table-legibility hygiene is sufficient.
  </action>
  <verify>
    <automated>grep -q "articleBody.innerHTML" static/js/app.js && grep -q "result.body_html" static/js/app.js && grep -q "#article-body table" static/css/style.css && echo FRONTEND_OK</automated>
  </verify>
  <acceptance_criteria>
    - app.js `openArticle` reads `result.body_html`, sets `articleBody.innerHTML` when present, and falls back to `articleBody.textContent = result.body || "No content available."` when body_html is empty
    - app.js still sets `articleTitle.textContent` and `articleSource.textContent` (no change to those)
    - style.css contains a `#article-body table` rule with `border-collapse: collapse`
    - style.css gives `#article-body th, #article-body td` a border and padding
    - grep -c "articleBody.textContent" static/js/app.js shows the fallback line is still present (one occurrence inside the else branch)
  </acceptance_criteria>
  <done>The article view renders body_html as HTML with legible tables and falls back to plain text when body_html is absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elasticsearch _source → browser DOM | stored body_html is injected into the page via innerHTML |
| client → API | search query params reach GET /api/search (unchanged this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-T1 | Tampering | articleBody.innerHTML (stored XSS) | mitigate | Source content is sanitized scraper-side in 09-02 (`<script>`/`style`/`on*`/`data:` stripped). innerHTML matches the existing reviewed `excerpt.innerHTML` precedent (D-10). Local single-user offline app — no remote submitter, no server-side execution (09-RESEARCH.md Security Domain: XSS risk near-zero). |
| T-09-03-I1 | Information Disclosure | body_html in API response | accept | body_html is the same article content already exposed via `body`; no new data class exposed. |
| T-09-SC | Tampering | pip installs | accept | Zero new packages this phase (09-RESEARCH.md audit: none). |
</threat_model>

<verification>
Search read-path unit tests green:
`python3 -m pytest tests/test_search/ -m "not integration" -q`
Frontend wiring present:
`grep "articleBody.innerHTML" static/js/app.js && grep "#article-body table" static/css/style.css`
</verification>

<success_criteria>
- ArticleResult.body_html populated from ES and returned by the API
- Article view renders body_html via innerHTML with plain-text fallback
- Table CSS makes rendered tables legible
- models + api_search shape tests from 09-01 are GREEN
</success_criteria>

<output>
Create `.planning/phases/09-article-rendering-fixes/09-03-SUMMARY.md` when done
</output>
