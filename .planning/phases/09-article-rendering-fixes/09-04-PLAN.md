---
phase: 09-article-rendering-fixes
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified: []
autonomous: false
requirements: [BUG-01, BUG-02]

must_haves:
  truths:
    - "The car_articles index is re-created with the body_html field present in its mapping"
    - "Re-scraped Wikipedia and blog articles carry non-empty body_html and noise-free body"
    - "A human confirms the detail pane renders tables (BUG-01) and shows no navigation text (BUG-02) for both a Wikipedia and a Hagerty article"
  artifacts: []
  key_links:
    - from: "scripts/scraper.py --recreate"
      to: "car_articles index mapping"
      via: "indices.delete + ensure_index"
      pattern: "body_html"
---

<objective>
Apply the schema change to the live index and verify the end-to-end fix. The full Phase 9 test suite is green from 09-02/09-03, but BUG-01 and BUG-02 are only truly closed once the index is re-created with the body_html mapping, the corpus is re-scraped through the fixed pipeline, and a human confirms tables render and navigation text is gone in the actual UI. The validation strategy lists both visual checks as manual-only (require a live ES + UI session).

Purpose: Pitfall 5 — ensure_index is idempotent, so an existing index silently drops the new body_html field. The index must be dropped and rebuilt, then re-scraped, before the display fix is observable.
Output: A re-created, re-populated index and human sign-off on BUG-01 and BUG-02 in the running app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-article-rendering-fixes/09-RESEARCH.md
@.planning/phases/09-article-rendering-fixes/09-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan produces no new code symbols. It exercises the `--recreate` flag and body_html pipeline created in 09-02/09-03. See 09-01-PLAN.md for the full Phase 9 symbol list.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full suite green + re-create index + re-scrape corpus</name>
  <files></files>
  <read_first>
    - scripts/scraper.py (--recreate flag and main() wiring from 09-02; INDEX_NAME constant; _create_client; ES_HOME / config expectations)
    - nitrofind/es_schema.py (body_html field; ensure_index)
    - .planning/phases/09-article-rendering-fixes/09-RESEARCH.md (Pitfall 5; Open Question 1 — fallback handles any stragglers)
    - .planning/phases/09-article-rendering-fixes/09-VALIDATION.md (full suite command, manual-only verifications)
  </read_first>
  <action>
    First confirm the full non-integration suite is green: run `python3 -m pytest tests/ -m "not integration" -q`. If any Phase 9 test is red, stop and report — do not re-scrape against a broken pipeline.

    Then apply the schema change to the live index. Requires a running Elasticsearch node on localhost:9200 (start it the same way the app does — via the ES_HOME the project uses, or `python3 main.py` in a separate process, or the operator's existing ES). Run `python3 scripts/scraper.py --recreate --all`. This drops the car_articles index, rebuilds it via ensure_index with the body_html mapping, and re-scrapes Wikipedia (via the Parse API for HTML + nav-stripped plain text) and the configured blogs (with expanded noise removal + body_html capture).

    After the run, verify body_html landed: query the index for one document and confirm a non-empty body_html field is present, e.g. `curl -s 'localhost:9200/car_articles/_search?size=1&_source=title,body_html' | python3 -c "import sys,json; d=json.load(sys.stdin); src=d['hits']['hits'][0]['_source']; print('HAS_BODY_HTML' if src.get('body_html') else 'MISSING'); print(src.get('title'))"`. Also confirm the mapping contains body_html: `curl -s 'localhost:9200/car_articles/_mapping' | grep -q body_html && echo MAPPING_OK`.

    If ES cannot be started in this environment, report that and convert this task to operator instructions in the SUMMARY — the human checkpoint (Task 2) then covers live verification.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -m "not integration" -q</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -m pytest tests/ -m "not integration" -q` exits 0 (full suite green, including all Phase 9 tests)
    - `scripts/scraper.py --recreate --all` completes without an unhandled exception (return code 0)
    - The car_articles _mapping contains a body_html field after re-creation (curl ... | grep body_html succeeds), OR the SUMMARY documents that ES was unavailable and re-scrape is an operator step
    - At least one re-scraped document has a non-empty body_html field (HAS_BODY_HTML), OR ES-unavailable is documented
  </acceptance_criteria>
  <done>The full test suite is green and the index has been re-created with body_html and re-populated (or the ES-unavailable path is documented for the operator).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of BUG-01 and BUG-02 in the running UI</name>
  <what-built>
    Phase 9 fixes the detail pane: Wikipedia/blog articles now store cleaned HTML (body_html) with tables preserved and navigation sections stripped from the searchable body. The article view renders body_html via innerHTML with table CSS; the searchable body excludes References/External links (Wikipedia) and breadcrumb/related/newsletter text (blogs).
  </what-built>
  <how-to-verify>
    1. Ensure Elasticsearch is running and the corpus was re-scraped (Task 1). Start the app: `python3 main.py`.
    2. Open the browser UI (default http://127.0.0.1:5000).
    3. BUG-01 (Wikipedia tables): search `Ferrari 308`, click a Wikipedia result. Confirm the detail pane shows a rendered specs/infobox TABLE (rows of make/model/production data with visible cell borders) — not stripped or missing.
    4. BUG-02 (Wikipedia nav text): in that same article body, confirm there is NO trailing "References", "External links", or "See also" list of citation/link text mixed into the prose.
    5. BUG-01 (blog tables) + BUG-02 (blog nav): search a term that returns a Hagerty result, open it. Confirm any embedded table renders, and the body shows NO breadcrumb ("Home > ..."), related-article cards, newsletter signup, or author-bio text.
    6. Confirm a result that has no body_html still shows plain-text body (fallback) rather than an empty pane.
  </how-to-verify>
  <resume-signal>Type "approved" if tables render and no navigation text appears for both a Wikipedia and a Hagerty article; otherwise describe what is still wrong (which source, missing tables vs. leftover nav text) so a gap-closure plan can target it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live index | --recreate deletes and rebuilds the car_articles index (destructive) |
| external web → scraper → index | re-scrape pulls fresh untrusted HTML into storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-D1 | Denial of Service | --recreate drops the index | accept | Single-user local tool; the index is rebuildable from the scraper (the corpus is derived, not authoritative). Operator runs the destructive command knowingly behind an explicit flag. |
| T-09-04-T1 | Tampering | re-scraped body_html (stored XSS) | mitigate | Sanitization handled in 09-02 (script/style/on*/data: stripped). The human checkpoint also visually confirms no script execution / broken rendering. |
| T-09-SC | Tampering | pip installs | accept | Zero new packages this phase (09-RESEARCH.md audit: none). |
</threat_model>

<verification>
Full suite green: `python3 -m pytest tests/ -m "not integration" -q`
Mapping has body_html: `curl -s 'localhost:9200/car_articles/_mapping' | grep body_html`
Human checkpoint approved for BUG-01 (tables render) and BUG-02 (no nav text) on both a Wikipedia and a Hagerty article.
</verification>

<success_criteria>
- Full non-integration test suite green
- car_articles index re-created with body_html and re-scraped (or operator path documented)
- Human-approved: tables render in the detail pane and navigation text is absent, for both a Wikipedia and a Hagerty article
</success_criteria>

<output>
Create `.planning/phases/09-article-rendering-fixes/09-04-SUMMARY.md` when done
</output>
