---
phase: 11-extended-filtering
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - nitrofind/search/query_builder.py
  - nitrofind/server.py
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03]

must_haves:
  truths:
    - "build_filter_clauses accepts year_from, year_to, and country and emits the correct range/term clauses (FILT-01, FILT-02)"
    - "GET /api/search?year_from=&year_to=&country= parses the three params, coerces year params to int safely, and forwards them as ES filter clauses (FILT-03)"
    - "A non-integer year param is coerced to None and emits no range clause"
    - "All six filters (manufacturer, era_bucket, body_style, year_from, year_to, country) combine into one bool.filter list"
  artifacts:
    - path: "nitrofind/search/query_builder.py"
      provides: "Extended build_filter_clauses with year_from/year_to/country params emitting range + term clauses"
      contains: "country_of_origin"
    - path: "nitrofind/server.py"
      provides: "_safe_int_param helper + api_search reading year_from/year_to/country"
      contains: "_safe_int_param"
  key_links:
    - from: "nitrofind/server.py"
      to: "nitrofind.search.query_builder.build_filter_clauses"
      via: "keyword args year_from/year_to/country in the api_search call"
      pattern: "year_from=_safe_int_param"
    - from: "nitrofind/search/query_builder.py build_filter_clauses"
      to: "Elasticsearch bool.filter context"
      via: "range/term dicts appended to the filters list consumed by build_search_body"
      pattern: "range.*production_(end|start)"
---

<objective>
Implement the backend for Phase 11 extended filtering. Extend `build_filter_clauses` (FILT-01 year-range overlap, FILT-02 country term) and extend the Flask `api_search` route to read and safely coerce the three new query params (FILT-03). This turns the Plan 01 backend tests GREEN.

Purpose: Delivers the server-side filter logic that the UI (Plan 03) and the public `GET /api/search` contract (FILT-03) depend on. Year filtering uses interval-overlap semantics (`production_end >= year_from AND production_start <= year_to`) per RESEARCH.md Pattern 1; country uses a `term` clause on the `country_of_origin` keyword field per Pattern 2.
Output: Updated `query_builder.py` and `server.py`; Plan 01's query-builder and API tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extended-filtering/11-RESEARCH.md
@.planning/phases/11-extended-filtering/11-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces

This plan (11-02) creates these new symbols:
- `nitrofind/search/query_builder.py`: three new keyword parameters on `build_filter_clauses` — `year_from: int | None`, `year_to: int | None`, `country: str | None`. New emitted clause shapes: `{"range": {"production_end": {"gte": year_from}}}`, `{"range": {"production_start": {"lte": year_to}}}`, `{"term": {"country_of_origin": country}}`.
- `nitrofind/server.py`: new module-level helper `_safe_int_param(raw: str | None) -> int | None`; three new `request.args` reads (`year_from`, `year_to`, `country`) wired into the `build_filter_clauses(...)` call inside `api_search`.

The ES fields `production_start`, `production_end` (integer) and `country_of_origin` (keyword) already exist in `nitrofind/es_schema.py` — this phase does NOT create or alter the index mapping.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend build_filter_clauses with year_from / year_to / country</name>
  <files>nitrofind/search/query_builder.py</files>
  <behavior>
    - build_filter_clauses(year_from=1960) → [{"range": {"production_end": {"gte": 1960}}}]
    - build_filter_clauses(year_to=1975) → [{"range": {"production_start": {"lte": 1975}}}]
    - build_filter_clauses(year_from=1960, year_to=1975) → two range clauses
    - build_filter_clauses(year_from=None, year_to=None) → []
    - build_filter_clauses(country="Germany") → [{"term": {"country_of_origin": "Germany"}}]
    - build_filter_clauses(country="") → []
    - All six params together → six clauses
    - year_from=0 (edge: valid integer year) → emits a range clause (must use `is not None`, not truthiness)
  </behavior>
  <read_first>
    - nitrofind/search/query_builder.py (current `build_filter_clauses` signature at line 158 and body around lines 180-187 — the existing manufacturer/era_bucket/body_style term pattern to extend)
    - tests/test_search/test_query_builder.py (the Plan 01 tests this task must turn GREEN — they define the exact expected clause dicts)
    - .planning/phases/11-extended-filtering/11-PATTERNS.md (query_builder.py section — exact range/term clause shapes and the None-guard rule)
    - nitrofind/es_schema.py lines 46-50 (confirm production_start/production_end are integer and country_of_origin is keyword — these field names must match exactly)
  </read_first>
  <action>
    Add three optional keyword parameters to the `build_filter_clauses` signature after `body_style`: `year_from: int | None = None`, `year_to: int | None = None`, `country: str | None = None`.

    In the function body, after the existing three term-filter branches and before `return filters`, append:
    - FILT-01 year-range overlap (two independent optional clauses): when `year_from is not None`, append `{"range": {"production_end": {"gte": year_from}}}`; when `year_to is not None`, append `{"range": {"production_start": {"lte": year_to}}}`. Use `is not None` (NOT truthiness) so that integer `0` is a valid year. Add a comment noting the interval-overlap rationale: an article's production period overlaps `[year_from, year_to]` iff `production_end >= year_from AND production_start <= year_to`.
    - FILT-02 country exact match: when `country` is truthy (`if country:`, matching the existing manufacturer guard so empty string is skipped), append `{"term": {"country_of_origin": country}}`.

    Field names are fixed by the ES schema: `production_end` (range gte for year_from), `production_start` (range lte for year_to), `country_of_origin` (term). Do not use `match` on country (keyword field — `term` only). Do not touch published_at. Do not alter the existing manufacturer/era_bucket/body_style branches.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && python -m pytest tests/test_search/test_query_builder.py -q -k "year or country or all_filters" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `nitrofind/search/query_builder.py` `build_filter_clauses` signature contains `year_from: int | None = None`, `year_to: int | None = None`, `country: str | None = None`.
    - File contains the substring `"production_end"` and the substring `"production_start"` inside range clauses and `"country_of_origin"` inside a term clause.
    - `pytest tests/test_search/test_query_builder.py -q -k "year or country or all_filters"` exits 0 (all seven Plan 01 query-builder tests GREEN).
    - Year guard uses `is not None` (so a year of 0 still emits a clause); country guard uses truthiness (empty string emits nothing).
  </acceptance_criteria>
  <done>build_filter_clauses emits correct range/term clauses for the new params and the seven query-builder tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire year_from / year_to / country into api_search with safe int coercion</name>
  <files>nitrofind/server.py</files>
  <behavior>
    - GET /api/search?q=test&year_from=1960 → ES bool.filter contains {"range": {"production_end": {"gte": 1960}}}
    - GET /api/search?q=test&year_to=1975 → ES bool.filter contains {"range": {"production_start": {"lte": 1975}}}
    - GET /api/search?q=test&country=Germany → ES bool.filter contains {"term": {"country_of_origin": "Germany"}}
    - GET /api/search?q=test&year_from=abc → no production_end clause (coerced to None), status 200
    - Empty/absent params → no extra clauses, existing behavior unchanged
  </behavior>
  <read_first>
    - nitrofind/server.py (the `api_search` route at line 128 and the `build_filter_clauses(...)` call at lines 151-155; also read the surrounding param-extraction and the T-07-02 docstring at line 138 to preserve the security note convention)
    - nitrofind/search/query_builder.py (the signature updated in Task 1 — confirm the kwarg names year_from/year_to/country)
    - tests/test_search/test_api_search.py (the Plan 01 API tests this task must turn GREEN — they define the exact param names and expected forwarded clauses)
    - .planning/phases/11-extended-filtering/11-PATTERNS.md (server.py section — `_safe_int_param` body and the extended build_filter_clauses call block)
  </read_first>
  <action>
    Add a module-level helper before `api_search`: `_safe_int_param(raw: str | None) -> int | None` that returns `None` when `raw` is falsy, attempts `int(raw)`, and returns `None` on `ValueError`. This guards against non-integer year input (Tampering mitigation — raw user strings never reach an ES range clause).

    Extend the existing `build_filter_clauses(...)` call inside `api_search` (currently passing manufacturer/era_bucket/body_style) to also pass:
    - `year_from=_safe_int_param(request.args.get("year_from"))`
    - `year_to=_safe_int_param(request.args.get("year_to"))`
    - `country=request.args.get("country") or None`

    Keep the existing `or None` pattern for the string params. The country param follows the manufacturer convention exactly (value placed in a `term` filter value field, never in a DSL key — T-07-02 precedent). Do not change the response shape, the sort handling, or any other part of the route.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && python -m pytest tests/test_search/test_api_search.py -q -k "year or country" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `nitrofind/server.py` contains `def _safe_int_param(`.
    - The `build_filter_clauses(` call in `api_search` contains `year_from=_safe_int_param(request.args.get("year_from"))`, `year_to=_safe_int_param(request.args.get("year_to"))`, and `country=request.args.get("country") or None`.
    - `pytest tests/test_search/test_api_search.py -q -k "year or country"` exits 0 (all four Plan 01 API tests GREEN).
    - `GET /api/search?q=test&year_from=abc` returns 200 and emits no production_end clause (covered by test_year_invalid_string_coerced_to_none).
  </acceptance_criteria>
  <done>api_search reads and safely coerces the three new params, forwards them to build_filter_clauses, and the four API forwarding tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/search | Untrusted query-string params (year_from, year_to, country) cross into the Flask route |
| Flask route → Elasticsearch | Coerced/guarded values placed into ES DSL term/range value fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | year_from / year_to params | mitigate | `_safe_int_param` coerces via `int()`; non-integer input becomes None and emits no clause — raw user string never enters an ES range clause (V5 input validation) |
| T-11-03 | Tampering | country param | mitigate | Value placed only in `term` filter value position via build_filter_clauses, never as a DSL key — extends existing T-07-02 mitigation; `or None` guard strips empty string |
| T-11-04 | Denial of Service | extreme year values (e.g. year_from=99999999) | accept | `int()` succeeds but an ES range on an integer field with an out-of-range value is a no-op returning zero results — no performance risk on a 2 GB local single-node index |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs — additive edits to two existing Python files; RESEARCH.md Package Legitimacy Audit confirms zero new packages |
</threat_model>

<verification>
- `pytest tests/test_search/test_query_builder.py -q -k "year or country or all_filters"` exits 0.
- `pytest tests/test_search/test_api_search.py -q -k "year or country"` exits 0.
- `pytest tests/test_search/ -q` is fully green — all new FILT tests pass and no pre-existing tests regress.
- Manual DSL check (optional): `build_filter_clauses(year_from=1960, year_to=1975, country="Germany")` returns exactly three clauses with the documented shapes.
</verification>

<success_criteria>
- All 11 Plan 01 tests are GREEN.
- `build_filter_clauses` and `api_search` handle the new params with the exact clause shapes from RESEARCH.md.
- No regressions in the existing test suite.
- Year coercion rejects non-integer input gracefully (None, no clause).
</success_criteria>

<output>
Create `.planning/phases/11-extended-filtering/11-02-SUMMARY.md` when done.
</output>
