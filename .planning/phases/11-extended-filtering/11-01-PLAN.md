---
phase: 11-extended-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_search/test_query_builder.py
  - tests/test_search/test_api_search.py
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03]

must_haves:
  truths:
    - "Running pytest on the new year/country/all_filters tests fails (RED) because build_filter_clauses does not yet accept year_from/year_to/country"
    - "Running pytest on the new API forwarding tests fails (RED) because the api_search route does not yet parse year_from/year_to/country"
  artifacts:
    - path: "tests/test_search/test_query_builder.py"
      provides: "FILT-01/02/03 unit tests for build_filter_clauses range + term clauses"
      contains: "def test_build_filter_clauses_year_from"
    - path: "tests/test_search/test_api_search.py"
      provides: "FILT-03 API param forwarding tests"
      contains: "def test_year_from_filter_forwarded"
  key_links:
    - from: "tests/test_search/test_query_builder.py"
      to: "nitrofind.search.query_builder.build_filter_clauses"
      via: "import + direct call with new kwargs"
      pattern: "build_filter_clauses\\(.*year_from"
    - from: "tests/test_search/test_api_search.py"
      to: "nitrofind.server.app"
      via: "test_client GET /api/search with new query params"
      pattern: "year_from=1960"
---

<objective>
Create the failing-test scaffold (Wave 0 / RED) for Phase 11 extended filtering. Add unit tests to the existing query-builder test file covering year-range and country filter clause construction (FILT-01, FILT-02, FILT-03), and add API forwarding tests to the existing API test file covering the new `year_from`, `year_to`, and `country` query params (FILT-03).

Purpose: Establishes executable acceptance criteria BEFORE implementation so Plans 02 and 03 have a verifiable RED→GREEN target. Satisfies the Nyquist Rule — every downstream `<verify>` resolves to a concrete test that exists.
Output: New test functions in `tests/test_search/test_query_builder.py` and `tests/test_search/test_api_search.py` that fail today and pass after Plan 02 ships.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extended-filtering/11-RESEARCH.md
@.planning/phases/11-extended-filtering/11-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces

This plan (11-01) creates these new test symbols:
- `tests/test_search/test_query_builder.py`: `test_build_filter_clauses_year_from`, `test_build_filter_clauses_year_to`, `test_build_filter_clauses_year_both_bounds`, `test_build_filter_clauses_year_none_produces_no_clause`, `test_build_filter_clauses_country`, `test_build_filter_clauses_country_empty_string_ignored`, `test_build_filter_clauses_all_filters`
- `tests/test_search/test_api_search.py`: `test_year_from_filter_forwarded`, `test_year_to_filter_forwarded`, `test_country_filter_forwarded`, `test_year_invalid_string_coerced_to_none`

These tests reference symbols created later in this phase (Plan 02): the `year_from`, `year_to`, `country` keyword params of `build_filter_clauses`, and the `_safe_int_param` helper / param parsing in `api_search`. They are expected to FAIL until Plan 02 lands.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add FILT-01/02/03 unit tests to test_query_builder.py</name>
  <files>tests/test_search/test_query_builder.py</files>
  <read_first>
    - tests/test_search/test_query_builder.py (existing test patterns; the `test_build_filter_clauses_*` functions around lines 193-222 are the exact analog to copy; confirm `build_filter_clauses` is already imported in the import block lines 24-33)
    - nitrofind/search/query_builder.py (current `build_filter_clauses` signature at line 158 — confirm it does NOT yet accept year_from/year_to/country, which is why these tests will RED)
    - .planning/phases/11-extended-filtering/11-PATTERNS.md (test analog section for test_query_builder.py — exact assertion shapes)
  </read_first>
  <action>
    Append seven new test functions to `tests/test_search/test_query_builder.py`, one assertion per function, each docstring referencing its requirement ID, matching the existing `test_build_filter_clauses_manufacturer` style (call `build_filter_clauses(...)`, assert on the returned list of dicts):

    1. `test_build_filter_clauses_year_from` [FILT-01]: call `build_filter_clauses(year_from=1960)`; assert result equals `[{"range": {"production_end": {"gte": 1960}}}]`.
    2. `test_build_filter_clauses_year_to` [FILT-01]: call `build_filter_clauses(year_to=1975)`; assert result equals `[{"range": {"production_start": {"lte": 1975}}}]`.
    3. `test_build_filter_clauses_year_both_bounds` [FILT-01]: call `build_filter_clauses(year_from=1960, year_to=1975)`; assert `len(result) == 2` and both `{"range": {"production_end": {"gte": 1960}}}` and `{"range": {"production_start": {"lte": 1975}}}` are members.
    4. `test_build_filter_clauses_year_none_produces_no_clause` [FILT-01]: call `build_filter_clauses(year_from=None, year_to=None)`; assert result equals `[]`.
    5. `test_build_filter_clauses_country` [FILT-02]: call `build_filter_clauses(country="Germany")`; assert result equals `[{"term": {"country_of_origin": "Germany"}}]`.
    6. `test_build_filter_clauses_country_empty_string_ignored` [FILT-02]: call `build_filter_clauses(country="")`; assert result equals `[]`.
    7. `test_build_filter_clauses_all_filters` [FILT-03]: call `build_filter_clauses(manufacturer="BMW", era_bucket="1960s", body_style="coupe", year_from=1960, year_to=1975, country="Germany")`; assert `len(result) == 6` and each of the six expected clause dicts is a member (the three existing term clauses plus the two range clauses plus the country term clause).

    The exact field names that must appear in assertions: range key `production_end` with operator `gte` for year_from; range key `production_start` with operator `lte` for year_to; term key `country_of_origin` for country. Do not invent helper functions — keep each test self-contained like the existing analogs.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && python -m pytest tests/test_search/test_query_builder.py -q -k "year or country or all_filters" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_search/test_query_builder.py` contains `def test_build_filter_clauses_year_from(`
    - `tests/test_search/test_query_builder.py` contains `def test_build_filter_clauses_country(`
    - `tests/test_search/test_query_builder.py` contains `def test_build_filter_clauses_all_filters(`
    - The seven new tests FAIL (RED) when run today because `build_filter_clauses` does not yet accept `year_from`/`year_to`/`country` (expect TypeError: unexpected keyword argument). Command `pytest tests/test_search/test_query_builder.py -q -k "year or country or all_filters"` exits non-zero.
    - No pre-existing test in the file is modified or deleted.
  </acceptance_criteria>
  <done>Seven FILT test functions exist and fail with an unexpected-keyword-argument error against the current build_filter_clauses signature.</done>
</task>

<task type="auto">
  <name>Task 2: Add FILT-03 API forwarding tests to test_api_search.py</name>
  <files>tests/test_search/test_api_search.py</files>
  <read_first>
    - tests/test_search/test_api_search.py (existing `test_manufacturer_filter_forwarded` around lines 153-183 — the exact monkeypatch + assert-on-filter analog to copy; confirm imports of MagicMock and server)
    - nitrofind/server.py (current `api_search` at line 128 and the `build_filter_clauses(...)` call at lines 151-155 — confirm it does NOT yet read year_from/year_to/country, which is why these tests will RED)
    - .planning/phases/11-extended-filtering/11-PATTERNS.md (test analog section for test_api_search.py — exact filter-extraction path through function_score.query.bool.filter)
  </read_first>
  <action>
    Append four new test functions to `tests/test_search/test_api_search.py`, each using the inline monkeypatch style (NOT the fixture functions) matching `test_manufacturer_filter_forwarded`: build a `MagicMock` ES client whose `.search.return_value` is `{"took": 3, "hits": {"total": {"value": 0}, "hits": []}}`, set `server.state["ready"] = True` and `server.state["es_client"] = mock_es` via `monkeypatch.setitem`, then `server.app.test_client()`.

    1. `test_year_from_filter_forwarded` [FILT-03]: GET `/api/search?q=test&year_from=1960`; assert status 200; extract `filters = mock_es.search.call_args.kwargs["query"]["function_score"]["query"]["bool"]["filter"]`; assert `{"range": {"production_end": {"gte": 1960}}} in filters`.
    2. `test_year_to_filter_forwarded` [FILT-03]: GET `/api/search?q=test&year_to=1975`; same extraction; assert `{"range": {"production_start": {"lte": 1975}}} in filters`.
    3. `test_country_filter_forwarded` [FILT-03]: GET `/api/search?q=test&country=Germany`; same extraction; assert `{"term": {"country_of_origin": "Germany"}} in filters`.
    4. `test_year_invalid_string_coerced_to_none` [FILT-03]: GET `/api/search?q=test&year_from=abc`; assert status 200; assert the string `"production_end"` does NOT appear anywhere in `str(call_kwargs["query"]["function_score"]["query"])` — proving a non-integer year is coerced to None and emits no range clause.

    The filter-extraction path is fixed by the existing query shape: `query → function_score → query → bool → filter`. Mirror it exactly; do not reshape the query.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && python -m pytest tests/test_search/test_api_search.py -q -k "year or country" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_search/test_api_search.py` contains `def test_year_from_filter_forwarded(monkeypatch):`
    - `tests/test_search/test_api_search.py` contains `def test_country_filter_forwarded(monkeypatch):`
    - `tests/test_search/test_api_search.py` contains `def test_year_invalid_string_coerced_to_none(monkeypatch):`
    - The four new tests FAIL (RED) today because `api_search` does not yet forward the new params (the asserted range/term clauses are absent from the ES call). Command `pytest tests/test_search/test_api_search.py -q -k "year or country"` exits non-zero.
    - No pre-existing test in the file is modified or deleted.
  </acceptance_criteria>
  <done>Four FILT-03 API forwarding tests exist and fail (RED) because the route does not yet read year_from/year_to/country.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → server.app | In-process test client; no real network or ES; MagicMock stands in for ES |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Test fixtures asserting on ES call shape | accept | Tests are local dev artifacts, not runtime-reachable; no untrusted input enters the test scaffold |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — additive test functions only; RESEARCH.md Package Legitimacy Audit confirms zero new packages |
</threat_model>

<verification>
- `pytest tests/test_search/test_query_builder.py -q -k "year or country or all_filters"` exits non-zero (RED — implementation absent).
- `pytest tests/test_search/test_api_search.py -q -k "year or country"` exits non-zero (RED — implementation absent).
- `pytest tests/test_search/ -q` shows the new tests failing and ALL pre-existing tests still passing (no regressions introduced by the scaffold).
</verification>

<success_criteria>
- 11 new test functions exist across the two existing test files.
- All 11 fail today for the right reason (missing params / missing clauses), not import errors or syntax errors.
- Pre-existing test suite remains green.
</success_criteria>

<output>
Create `.planning/phases/11-extended-filtering/11-01-SUMMARY.md` when done.
</output>
