---
phase: 11-extended-filtering
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - templates/index.html
  - static/js/app.js
  - static/css/style.css
autonomous: false
requirements: [FILT-01, FILT-02, FILT-03]

must_haves:
  truths:
    - "The filter row shows a Year From input, a Year To input, and a Country input alongside the existing manufacturer/era/body-style selects (FILT-01, FILT-02)"
    - "Entering a year range or country and blurring the field re-runs the search with the new filters applied (FILT-01, FILT-02)"
    - "Year and country filters combine with manufacturer/era/body-style — all active filters apply simultaneously (FILT-03)"
    - "Empty filter fields are stripped from the request URL (no year_from=&year_to=&country=)"
  artifacts:
    - path: "templates/index.html"
      provides: "Three new filter controls in .filter-row"
      contains: "filter-year-from"
    - path: "static/js/app.js"
      provides: "currentFilters extended with year_from/year_to/country + change-event wiring"
      contains: "filterYearFrom"
    - path: "static/css/style.css"
      provides: "Styling for .filter-row number/text inputs consistent with existing selects"
      contains: ".filter-row input"
  key_links:
    - from: "static/js/app.js onFilterChange"
      to: "GET /api/search query string"
      via: "currentFilters spread into URLSearchParams in runSearch"
      pattern: "currentFilters.year_from"
    - from: "templates/index.html filter inputs"
      to: "static/js/app.js DOM cache + change listeners"
      via: "getElementById on filter-year-from/filter-year-to/filter-country"
      pattern: "getElementById\\(.filter-year-from.\\)"
---

<objective>
Build the browser UI for Phase 11 extended filtering. Add three controls to the filter row (Year From, Year To, Country), extend the `currentFilters` state and wire the new inputs to `onFilterChange` using `change` events, and style the new inputs consistently with the existing filter selects. Then a human verifies the end-to-end filtering behavior in the running app.

Purpose: Completes FILT-01/FILT-02 from the user's perspective — the year-range and country filters are usable in the live UI and combine with the existing filters (FILT-03). The backend (Plan 02) already accepts the params; this plan makes them reachable.
Output: Updated `index.html`, `app.js`, `style.css`; a human-verified working filter row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-extended-filtering/11-RESEARCH.md
@.planning/phases/11-extended-filtering/11-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces

This plan (11-03) creates these new symbols:
- `templates/index.html`: three new elements with ids `filter-year-from` (`<input type="number">`), `filter-year-to` (`<input type="number">`), `filter-country` (`<input type="text">`) inside `.filter-row`.
- `static/js/app.js`: three new keys on `currentFilters` (`year_from`, `year_to`, `country`); three new DOM cache constants `filterYearFrom`, `filterYearTo`, `filterCountry`; three new `change` event listeners; three new assignments inside `onFilterChange`.
- `static/css/style.css`: new selector rules `.filter-row input[type="number"]` and `.filter-row input[type="text"]` (plus their `:focus` variants).

The `runSearch` empty-param strip loop and the `URLSearchParams({ q, ...currentFilters })` spread are pre-existing and require NO change — they already handle the new keys generically.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add year + country controls to the filter row and style them</name>
  <files>templates/index.html, static/css/style.css</files>
  <read_first>
    - templates/index.html lines 26-55 (the `.filter-row` block: existing selects filter-manufacturer/filter-era/filter-body and the `.sort-controls` div — the new inputs go after filter-body and before sort-controls)
    - static/css/style.css lines 169-191 (the `.filter-row` container rule and `.filter-row select` / `.filter-row select:focus` rules — the token-based styling pattern to mirror; new rules go immediately after line 191)
    - .planning/phases/11-extended-filtering/11-PATTERNS.md (index.html and style.css sections — exact element markup and CSS token list)
  </read_first>
  <action>
    In `templates/index.html`, insert three controls inside `.filter-row` immediately after the closing tag of the `filter-body` select (after line 49) and before the `sort-controls` div: a number input with id `filter-year-from`, placeholder `Year from`, attributes `min="1900" max="2099" step="1"`; a number input with id `filter-year-to`, placeholder `Year to`, same min/max/step; and a text input with id `filter-country`, placeholder `e.g. Germany`.

    Use input elements not select elements (year ranges and country are free-form, unlike the fixed era/body vocabularies). The min/max attributes provide soft client-side validation matching the scraper 1900-2099 bounds. The country placeholder `e.g. Germany` signals the case-sensitive keyword-match convention (per RESEARCH.md Pitfall 3 — keyword field, no normalizer).

    In `static/css/style.css`, add new rules immediately after the `.filter-row select:focus` rule (after line 191). Add a combined selector `.filter-row input[type="number"], .filter-row input[type="text"]` that mirrors every property of the existing `.filter-row select` rule: padding `0.375rem 0.625rem`, font-size `0.85rem`, background-color `var(--bg-input)`, color `var(--text-primary)`, border `1px solid var(--border)`, border-radius `var(--radius)`, outline `none`, transition `border-color var(--transition)`, plus `width: 7rem`. Then add a `.filter-row input[type="text"]` override setting `width: 9rem`. Then add a focus rule `.filter-row input[type="number"]:focus, .filter-row input[type="text"]:focus` setting `border-color: var(--accent)`. All color and metric values MUST use the existing CSS custom-property tokens — no raw hex values (file convention, comment at lines 1-6).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && grep -q 'id="filter-year-from"' templates/index.html && grep -q 'id="filter-year-to"' templates/index.html && grep -q 'id="filter-country"' templates/index.html && grep -q 'filter-row input\[type="number"\]' static/css/style.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `templates/index.html` contains `id="filter-year-from"`, `id="filter-year-to"`, and `id="filter-country"`, all located between the `filter-body` select and the `sort-controls` div.
    - The two year inputs are `type="number"` with `min="1900"` and `max="2099"`; the country input is `type="text"` with placeholder `e.g. Germany`.
    - `static/css/style.css` contains a `.filter-row input[type="number"]` rule and a `.filter-row input[type="text"]` rule; every property value references a `var(--token)` (no raw hex).
    - The verify grep command prints PASS.
  </acceptance_criteria>
  <done>The filter row markup has three new controls and the stylesheet styles them consistently using existing tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Extend currentFilters state and wire new inputs with change events</name>
  <files>static/js/app.js</files>
  <read_first>
    - static/js/app.js line 28 (the `currentFilters` declaration), lines 45-47 (the filterMfr/filterEra/filterBody DOM cache lines), lines 91-95 (the `URLSearchParams` spread + empty-param strip loop — confirm it needs NO change), lines 192-201 (the `onFilterChange` function and the three existing `addEventListener("change", ...)` lines)
    - templates/index.html (the element ids created in Task 1 — confirm filter-year-from/filter-year-to/filter-country match the getElementById calls)
    - .planning/phases/11-extended-filtering/11-PATTERNS.md (app.js section — exact state keys, DOM cache names, and the change-not-input rule from Pitfall 4)
  </read_first>
  <action>
    Extend `currentFilters` (line 28) from `{ manufacturer: "", era_bucket: "", body_style: "" }` to also include `year_from: ""`, `year_to: ""`, and `country: ""`.

    Add three DOM cache constants after the `filterBody` line (after line 47): `filterYearFrom` from `getElementById("filter-year-from")`, `filterYearTo` from `getElementById("filter-year-to")`, `filterCountry` from `getElementById("filter-country")`.

    In `onFilterChange` (lines 192-201), add three assignments after the existing body_style assignment: `currentFilters.year_from = filterYearFrom.value`, `currentFilters.year_to = filterYearTo.value`, `currentFilters.country = filterCountry.value`. Keep the existing `if (currentQuery) runSearch(currentQuery);` line.

    Add three event listeners after the existing three filter listeners (after line 201): bind `filterYearFrom`, `filterYearTo`, and `filterCountry` each to `onFilterChange` on the `change` event. Use `change` (fires on blur/Enter) NOT `input` for the year number inputs — `input` would fire on every keystroke of a 4-digit year producing spurious partial-value queries (RESEARCH.md Pitfall 4). Use `change` for the country input too, for consistency.

    Do NOT modify the `URLSearchParams({ q, ...currentFilters })` spread or the empty-param strip loop (lines 91-95) — they already handle the new keys generically. Do NOT add any `currentSort` reset (Phase 10 decision: sort persists across filter changes).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && node --check static/js/app.js && grep -q 'filterYearFrom' static/js/app.js && grep -q 'currentFilters.country' static/js/app.js && grep -c 'addEventListener("change", onFilterChange)' static/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check static/js/app.js` exits 0 (no syntax error).
    - `static/js/app.js` `currentFilters` object contains keys `year_from`, `year_to`, and `country`.
    - File contains `const filterYearFrom`, `const filterYearTo`, `const filterCountry` bound via `getElementById`.
    - `onFilterChange` assigns `currentFilters.year_from`, `currentFilters.year_to`, `currentFilters.country` from the new inputs' `.value`.
    - The new listeners use the `change` event (grep count of `addEventListener("change", onFilterChange)` is 6 — three existing plus three new).
    - The `URLSearchParams` spread and strip loop (lines ~91-95) are unchanged.
  </acceptance_criteria>
  <done>currentFilters carries the three new keys, the inputs are wired via change events, and app.js parses without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — year-range and country filters work end-to-end</name>
  <action>This is a human-verification checkpoint — no autonomous implementation. The executor must ensure Elasticsearch is running with the indexed corpus and the Flask server is started, then present the verification steps below to the user and pause for the resume signal before marking the phase complete.</action>
  <what-built>
    A Year From input, Year To input, and Country input were added to the search filter row. They are wired to the existing filter pipeline: changing any of them re-runs the active search with `year_from`, `year_to`, and `country` appended to `GET /api/search`, where the backend (Plan 02) applies them as ES range and term filter clauses combined with the existing manufacturer/era/body-style filters.
  </what-built>
  <how-to-verify>
    Prerequisite: the Elasticsearch node must be running with the indexed automotive corpus, and the Flask server started (e.g. `python scripts/server.py` or the project's documented launch command). Then open the app in a browser at the served localhost URL.

    1. Type a broad query that returns many results (e.g. `Porsche` or `Ford`). Confirm results appear and the filter row now shows two number inputs ("Year from", "Year to") and a text input placeholdered "e.g. Germany", styled consistently with the existing dropdowns.
    2. FILT-01 (year range): In "Year from" type `1960`, press Tab/Enter to blur, then in "Year to" type `1975` and blur. Confirm the result list narrows to articles whose production period overlaps 1960–1975 (e.g. a classic-era model that started before 1960 but ran into it should still appear; clearly out-of-era-only models should drop out). Open the browser dev-tools Network tab and confirm the request URL contains `year_from=1960&year_to=1975`.
    3. FILT-02 (country): Clear the year fields. In the Country input type `Germany` (capital G — case-sensitive) and blur. Confirm results narrow to German-origin articles. Confirm the request URL contains `country=Germany`. (Optional negative check: typing `germany` lowercase may yield zero results — this is the documented case-sensitive keyword behavior, not a bug.)
    4. FILT-03 (combination): With a query active, set a manufacturer dropdown value AND a year range AND a country simultaneously. Confirm all filters apply together and the request URL carries every active param. Clear all filter fields and confirm the URL drops the empty params (no `year_from=&year_to=&country=`) and the unfiltered result set returns.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what failed (which filter, expected vs actual results, or the request URL observed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → DOM → query string | Year/country values typed into filter inputs are appended to the /api/search URL |
| browser → /api/search | The server-side coercion/guarding (Plan 02 T-11-02/T-11-03) is the authoritative validation layer; client min/max is convenience only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | client-side min/max on year inputs | accept | Client validation is cosmetic; the authoritative guard is `_safe_int_param` in server.py (T-11-02). A user bypassing the input still hits the server coercion |
| T-11-06 | Information Disclosure | filter values in query string | accept | Local single-user offline tool; query string carries only car-spec filter terms, no secrets or PII; no logging concern |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs — vanilla browser APIs only (no npm/CDN/framework per app.js header); RESEARCH.md Package Legitimacy Audit confirms zero new packages |
</threat_model>

<verification>
- `node --check static/js/app.js` exits 0.
- `grep` confirms the three new ids in index.html, the three new state keys + DOM caches + change listeners in app.js, and the new input rules in style.css.
- `pytest tests/test_search/ -q` remains fully green (this plan touches no Python, but confirm no incidental breakage).
- Human checkpoint (Task 3) approves all four end-to-end behaviors (FILT-01 range overlap, FILT-02 country, FILT-03 combination, empty-param stripping).
</verification>

<success_criteria>
- The filter row exposes working Year From, Year To, and Country controls.
- Changing any new filter re-runs the search with the param appended; empty fields are stripped.
- Year and country filters combine correctly with the existing filters (FILT-03).
- Human verification approved.
</success_criteria>

<output>
Create `.planning/phases/11-extended-filtering/11-03-SUMMARY.md` when done.
</output>
