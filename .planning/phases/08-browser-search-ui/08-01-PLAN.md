---
phase: 08-browser-search-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - nitrofind/server.py
  - templates/index.html
  - static/css/style.css
autonomous: true
requirements: [UIPL-01, SRCH-02]
must_haves:
  truths:
    - "GET / serves a rendered HTML document (not a raw string), beginning with <!DOCTYPE html> and containing <title>NitroFind</title>"
    - "Flask resolves templates/ and static/ at the project root despite app being defined in nitrofind/server.py"
    - "The page renders a dark off-black background by default via CSS custom properties on :root"
    - "The HTML contains three view containers (home-view, results-view, article-view) and <body data-state=\"home\"> at parse time"
  artifacts:
    - path: "templates/index.html"
      provides: "Three-state SPA HTML skeleton with stable element IDs consumed by app.js"
      min_lines: 40
      contains: "data-state=\"home\""
    - path: "static/css/style.css"
      provides: "Dark teal theme via CSS custom properties + data-state attribute selectors"
      min_lines: 60
      contains: ":root"
    - path: "nitrofind/server.py"
      provides: "render_template-backed GET / route + explicit template_folder/static_folder"
      contains: "render_template"
  key_links:
    - from: "nitrofind/server.py"
      to: "templates/index.html"
      via: "render_template(\"index.html\")"
      pattern: "render_template\\(\"index.html\"\\)"
    - from: "templates/index.html"
      to: "static/css/style.css"
      via: "<link rel=\"stylesheet\">"
      pattern: "static/css/style.css"
---

<objective>
Wire Flask to serve a real HTML page from `templates/index.html` and establish the dark-teal visual foundation. Replace the `GET /` placeholder string with `render_template("index.html")`, fixing the Flask template-root resolution pitfall, and create the three-state SPA HTML skeleton plus the CSS custom-properties dark theme.

Purpose: Deliver the static structural and visual shell (UIPL-01 dark theme, SRCH-02 excerpt highlight styling) that the Wave 2 JavaScript controller and Wave 2 test plan build on. No client-side behavior is implemented here — only markup, styling, and the Flask wiring.

Output: Modified `nitrofind/server.py`, new `templates/index.html`, new `static/css/style.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-browser-search-ui/08-CONTEXT.md
@.planning/phases/08-browser-search-ui/08-RESEARCH.md
@.planning/phases/08-browser-search-ui/08-PATTERNS.md
@nitrofind/server.py
@.agents/skills/design-taste-frontend/SKILL.md
</context>

<artifacts_this_phase_produces>
Symbols and files created or modified across Phase 8 (used by the source-grounding drift pass — these are new, not pre-existing):

- `templates/index.html` (new file) — element IDs: `search-input`, `status-line`, `search-input-results`, `filter-manufacturer`, `filter-era`, `filter-body`, `stats-line`, `results-list`, `back-btn`, `article-title`, `article-source`, `article-body`; CSS classes: `home-view`, `results-view`, `article-view`, `top-bar`, `logo`, `logo-small`, `search-wrap`, `filter-row`, `result-item`, `result-title`, `result-meta`, `result-domain`, `result-excerpt`, `selected`; attribute `data-state` on `<body>`
- `static/css/style.css` (new file) — CSS custom properties: `--bg-primary`, `--bg-surface`, `--bg-input`, `--text-primary`, `--text-secondary`, `--accent`, `--accent-hover`, `--border`, `--radius`, `--transition`
- `static/js/app.js` (new file, Plan 02) — functions: `transitionTo`, `runSearch`, `renderResults`, `openArticle`, `updateSelection`, `onFilterChange`, `startWarmupPolling`; module-level vars: `uiState`, `selectedIndex`, `currentQuery`, `currentFilters`, `currentResults`, `debounceTimer`, `abortController`
- `nitrofind/server.py` — modified: `render_template` import, explicit `template_folder`/`static_folder` Flask constructor args, `_pkg_dir` module-level var, `index()` returns `render_template("index.html")`
- `tests/test_server.py` (Plan 03) — modified `test_root_returns_html`; optional new `test_root_uses_template`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Rewire Flask GET / to render_template with explicit template/static roots</name>
  <files>nitrofind/server.py</files>
  <read_first>
    - nitrofind/server.py (current import line 34, Flask instantiation line 47, index() route lines 67-70, module docstring lines 9 and 20)
    - .planning/phases/08-browser-search-ui/08-PATTERNS.md (section "nitrofind/server.py (modify)" — exact before/after snippets)
    - .planning/phases/08-browser-search-ui/08-RESEARCH.md (Pitfall 1: Flask template_folder resolution)
  </read_first>
  <action>
    Make three edits to nitrofind/server.py:
    1. Change the import on line 34 from `from flask import Flask, jsonify, request` to add `render_template`, producing `from flask import Flask, jsonify, render_template, request` (alphabetical order between jsonify and request).
    2. Replace the bare `app = Flask(__name__)` on line 47. First compute `_pkg_dir = os.path.dirname(os.path.abspath(__file__))` (the `os` module is already imported at line 29). Then construct the app with explicit `template_folder=os.path.join(_pkg_dir, "..", "templates")` and `static_folder=os.path.join(_pkg_dir, "..", "static")` keyword arguments. This is mandatory because Flask(__name__) inside nitrofind/server.py resolves root_path to the nitrofind/ package dir, not the project root where templates/ and static/ live (per D-08 and Pitfall 1).
    3. Replace the body of the `index()` route (lines 67-70) so it returns `render_template("index.html")` instead of the raw placeholder string. Update its docstring one-liner from the D-13 placeholder wording to describe the Phase 8 search UI route (short phrase, no trailing period, matching the existing docstring style in this file).
    4. Update the module docstring: line 9 (`index` export description) and line 20 (`API-04` coverage note) to say GET / serves the rendered index.html search UI instead of "placeholder HTML".
    Do not modify any other route, the state dict, or the ES poller logic. Do not add new imports beyond render_template.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind &amp;&amp; grep -q 'render_template' nitrofind/server.py &amp;&amp; grep -q 'template_folder=' nitrofind/server.py &amp;&amp; grep -q 'static_folder=' nitrofind/server.py &amp;&amp; python -c "import ast; ast.parse(open('nitrofind/server.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `nitrofind/server.py` line with `from flask import` contains all four of: Flask, jsonify, render_template, request
    - `nitrofind/server.py` contains `template_folder=os.path.join(_pkg_dir, "..", "templates")` and `static_folder=os.path.join(_pkg_dir, "..", "static")`
    - `index()` function body contains exactly `return render_template("index.html")`
    - `python -c "import ast; ast.parse(open('nitrofind/server.py').read())"` exits 0 (file parses)
    - No remaining occurrence of the literal string `Search UI coming in Phase 8` in nitrofind/server.py
  </acceptance_criteria>
  <done>GET / route calls render_template("index.html"); Flask app constructed with explicit template_folder/static_folder pointing at project-root templates/ and static/; file parses without syntax error.</done>
</task>

<task type="auto">
  <name>Task 2: Create templates/index.html three-state SPA skeleton</name>
  <files>templates/index.html</files>
  <read_first>
    - .planning/phases/08-browser-search-ui/08-PATTERNS.md (section "templates/index.html (create)" — full HTML shell pattern with exact element IDs and filter option values)
    - .planning/phases/08-browser-search-ui/08-CONTEXT.md (D-03 home state, D-04 results state, D-05 article state, D-06 filters, D-07 warmup disabled input)
  </read_first>
  <action>
    Create templates/index.html at the project root (NOT inside nitrofind/). Build a single HTML5 document with these required structural elements, IDs, and attributes exactly as named (app.js in Plan 02 binds to these IDs; renaming any breaks the contract):
    - `<!DOCTYPE html>` declaration, `<html lang="en">`, `<head>` with `<meta charset="UTF-8">`, viewport meta, `<title>NitroFind</title>`, and `<link rel="stylesheet" href="/static/css/style.css">`.
    - `<body data-state="home">` — the data-state attribute MUST be hardcoded to "home" in the markup to prevent flash of unstyled content before JS runs (Pitfall 6).
    - Home view `<div class="home-view">`: an `<h1 class="logo">NitroFind</h1>`, a `<div class="search-wrap">` containing `<input id="search-input" type="search" placeholder="Search cars…" autocomplete="off" disabled>` (disabled at parse time per D-07; JS enables after warmup) and `<p id="status-line">Starting up…</p>`.
    - Results view `<div class="results-view">`: a `<header class="top-bar">` with `<span class="logo-small">NitroFind</span>` and `<input id="search-input-results" type="search" placeholder="Search cars…" autocomplete="off">`; a `<div class="filter-row">` with three `<select>` elements id=`filter-manufacturer` (single `<option value="">All manufacturers</option>`), id=`filter-era` (option value="" "All eras" plus static decade options "1950s" through "2020s"), id=`filter-body` (option value="" "All body styles" plus coupe/sedan/hatchback/convertible/SUV/truck); a `<p id="stats-line"></p>`; and `<div id="results-list"></div>`.
    - Article view `<div class="article-view">`: a `<button id="back-btn" type="button">← Back</button>`, `<h2 id="article-title"></h2>`, `<p id="article-source"></p>`, `<div id="article-body"></div>`.
    - `<script src="/static/js/app.js"></script>` as the last element before `</body>`.
    Do not add any CDN `<link>` or CDN `<script>` — offline constraint (D-08). Static decade and body-style option values come from RESEARCH.md Open Question 1 recommendation; manufacturer select stays "All"-only (no aggregation endpoint exists).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind &amp;&amp; test -f templates/index.html &amp;&amp; grep -q 'data-state="home"' templates/index.html &amp;&amp; grep -q 'id="search-input"' templates/index.html &amp;&amp; grep -q 'id="results-list"' templates/index.html &amp;&amp; grep -q 'id="article-body"' templates/index.html &amp;&amp; grep -q '/static/js/app.js' templates/index.html &amp;&amp; ! grep -qiE 'https?://[^"]*\.(js|css)' templates/index.html</automated>
  </verify>
  <acceptance_criteria>
    - File `templates/index.html` exists at project root (not under nitrofind/)
    - Contains `<!DOCTYPE html>` and `<title>NitroFind</title>`
    - `<body` tag contains `data-state="home"`
    - All of these IDs present: search-input, status-line, search-input-results, filter-manufacturer, filter-era, filter-body, stats-line, results-list, back-btn, article-title, article-source, article-body
    - Contains all three class containers: `home-view`, `results-view`, `article-view`
    - `#search-input` carries the `disabled` attribute
    - References `/static/css/style.css` and `/static/js/app.js`
    - No external (http/https) .js or .css URLs present (offline constraint)
  </acceptance_criteria>
  <done>templates/index.html renders the three-state skeleton with all required IDs, body data-state="home", disabled search input, static filter options, and links to local CSS/JS only.</done>
</task>

<task type="auto">
  <name>Task 3: Create static/css/style.css dark teal theme and state-machine layout</name>
  <files>static/css/style.css</files>
  <read_first>
    - .planning/phases/08-browser-search-ui/08-PATTERNS.md (section "static/css/style.css (create)" — color tokens, state-machine CSS, typography, warmup state, selected highlight, excerpt bold)
    - .planning/phases/08-browser-search-ui/08-CONTEXT.md (D-09 dark teal CSS custom properties, D-07 warmup disabled state, D-11 keyboard nav selected style)
    - .agents/skills/design-taste-frontend/SKILL.md (dark-mode quality rules: off-black not pure #000, one accent color, WCAG AA, one radius scale, no neon glows)
  </read_first>
  <action>
    Create static/css/style.css at the project root. Implement:
    1. A `:root` block defining ALL color/structure tokens as CSS custom properties (no inline hex anywhere else in the file): `--bg-primary: #0f1117` (off-black, never pure #000 per taste-skill dark-mode rule), `--bg-surface: #161b22`, `--bg-input: #1c2128`, `--text-primary: #e6edf3`, `--text-secondary: #8b949e`, `--accent: #2dd4bf` (single locked dark-teal accent), `--accent-hover: #5eead4`, `--border: #30363d`, `--radius: 6px` (one radius scale, reused on every interactive element), `--transition: 200ms ease`.
    2. A `body` rule using a system-font stack (`ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif` — offline-safe, no CDN font), `background-color: var(--bg-primary)`, `color: var(--text-primary)`, `margin: 0`.
    3. State-machine visibility via data-state attribute selectors: default `.results-view, .article-view { display: none; }`; then `body[data-state="results"] .home-view { display: none; }` + show results-view; `body[data-state="article"]` hides home-view and results-view and shows article-view. This makes CSS (not JS) own the show/hide of each view.
    4. Layout for the three views: home-view centers logo + search box vertically/horizontally (e.g. flfrom a full-viewport flex column centered); results-view top-bar is a horizontal flex row; filter-row is a horizontal flex row of selects; results-list is a vertical stack.
    5. Warmup disabled state: `#search-input:disabled { cursor: not-allowed; opacity: 0.5; }` and `#status-line` with `transition: opacity var(--transition)` so JS can fade it out by toggling opacity.
    6. `.result-item` styling using var tokens (padding, border via `--border`, `--radius`), a hover/`.selected` state: `.result-item.selected { background-color: var(--bg-surface); border-left: 2px solid var(--accent); }` (D-11 keyboard cursor highlight).
    7. `.result-excerpt b { color: var(--text-primary); font-weight: 600; }` so ES `<b>` highlight tags render visibly bold (SRCH-02).
    8. All interactive elements (inputs, selects, button) reuse `--radius` and `--border`; use `--accent` for focus/active states only. No box-shadow neon glows, no gradients on text. Ensure text colors meet WCAG AA on `--bg-primary`.
    Every color and the radius MUST be referenced via `var(--token)` after the :root declaration — do not repeat raw hex values in component rules.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind &amp;&amp; test -f static/css/style.css &amp;&amp; grep -q ':root' static/css/style.css &amp;&amp; grep -q -- '--accent:' static/css/style.css &amp;&amp; grep -q -- '--bg-primary: #0f1117' static/css/style.css &amp;&amp; grep -q 'body\[data-state="results"\]' static/css/style.css &amp;&amp; grep -q '.result-item.selected' static/css/style.css &amp;&amp; grep -q '.result-excerpt b' static/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - File `static/css/style.css` exists at project root
    - Contains a `:root` block defining `--bg-primary`, `--bg-surface`, `--bg-input`, `--text-primary`, `--text-secondary`, `--accent`, `--accent-hover`, `--border`, `--radius`, `--transition`
    - `--bg-primary` value is `#0f1117` (off-black, not `#000000`)
    - File contains no occurrence of `#000000` and no occurrence of `#000;` (pure black banned)
    - Contains `body[data-state="results"]` and `body[data-state="article"]` attribute selectors that toggle view visibility
    - Contains `#search-input:disabled` rule with `cursor: not-allowed`
    - Contains `.result-item.selected` rule referencing `var(--accent)`
    - Contains `.result-excerpt b` rule with `font-weight: 600`
  </acceptance_criteria>
  <done>static/css/style.css provides a complete dark-teal theme driven by :root custom properties, data-state-attribute view switching, disabled-input warmup styling, keyboard-selected highlight, and bold excerpt rendering — all using var() tokens with no pure black.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Flask GET / | Static template served; no user input reaches this route |
| disk (templates/static) → Flask response | Files authored in-repo; trusted content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | static_folder path traversal | accept | Flask's static handler normalizes paths and rejects `..` traversal; static_folder is a fixed project-relative dir; no user-supplied filename reaches it |
| T-08-02 | Tampering | index.html offline integrity | mitigate | No CDN `<link>`/`<script>` references — all CSS/JS served from local /static/; verify gate asserts no external http(s) .js/.css URLs |
| T-08-03 | Denial of Service | GET / template render | accept | render_template caches the compiled template; no per-request ES call; localhost single-user tool |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH.md Package Legitimacy Audit confirms zero new packages |
</threat_model>

<verification>
- `pytest tests/test_server.py -x` passes (existing tests still green after render_template switch; templates/ now resolvable). Note: test assertion update lives in Plan 03 — if Plan 03 has not run, `test_root_returns_html` still passes because `b"NitroFind"` remains in the rendered output.
- `python -c "from nitrofind import server; c = server.app.test_client(); r = c.get('/'); assert r.status_code == 200; assert b'<!DOCTYPE html>' in r.data; assert b'data-state=\"home\"' in r.data"` exits 0.
- Manual: `python main.py`, open http://localhost:5000 — page shows dark off-black background, centered NitroFind logo, disabled search box with "Starting up…" line.
</verification>

<success_criteria>
- GET / returns a rendered HTML document (200) starting with `<!DOCTYPE html>` and containing `<title>NitroFind</title>` and `data-state="home"`.
- Flask resolves templates/ and static/ at the project root (no TemplateNotFound).
- The page background is the off-black `--bg-primary`; the dark-teal `--accent` is the only accent hue.
- All structural element IDs required by Plan 02's app.js are present in the rendered HTML.
</success_criteria>

<output>
Create `.planning/phases/08-browser-search-ui/08-01-SUMMARY.md` when done.
</output>
