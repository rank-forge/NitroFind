---
phase: 08-browser-search-ui
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/test_server.py
  - .planning/phases/08-browser-search-ui/08-HUMAN-UAT.md
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, UIPL-01, UIPL-02, UIPL-03]
must_haves:
  truths:
    - "pytest tests/test_server.py passes with the GET / assertion updated to validate template rendering (not the old raw placeholder string)"
    - "A test asserts GET / returns text/html content-type and contains <!DOCTYPE html>, proving render_template resolved the template"
    - "A HUMAN-UAT checklist exists enumerating the seven browser success criteria for manual smoke testing"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Updated test_root_returns_html + new test_root_uses_template covering the render_template route (API-04 updated)"
      contains: "<!DOCTYPE html>"
    - path: ".planning/phases/08-browser-search-ui/08-HUMAN-UAT.md"
      provides: "Manual smoke-test checklist for the seven browser-side success criteria"
      min_lines: 20
  key_links:
    - from: "tests/test_server.py"
      to: "nitrofind.server GET /"
      via: "client.get('/')"
      pattern: "client_not_ready.get\\(\"/\"\\)"
---

<objective>
Update the server test suite to match the new `render_template`-backed `GET /` route and author a human smoke-test checklist for the browser-side behaviors that cannot be automated under the no-npm constraint.

Purpose: The existing `test_root_returns_html` asserted against the raw placeholder string; after Plan 01 switches to `render_template("index.html")`, the test must validate that the template actually rendered (not just that "NitroFind" appears). The six browser requirements (SRCH-01..04, UIPL-02..03) are vanilla-JS DOM behaviors with no Python-testable surface and no browser runtime available (jsdom/playwright excluded by the no-npm constraint), so they are captured as a structured manual UAT checklist for the phase gate.

Output: Modified `tests/test_server.py`, new `.planning/phases/08-browser-search-ui/08-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-browser-search-ui/08-RESEARCH.md
@.planning/phases/08-browser-search-ui/08-PATTERNS.md
@nitrofind/server.py
@tests/test_server.py
</context>

<artifacts_this_phase_produces>
See `08-01-PLAN.md` "artifacts_this_phase_produces" for the full Phase 8 symbol list. This plan modifies `tests/test_server.py` (updates `test_root_returns_html`, adds `test_root_uses_template`) and creates `.planning/phases/08-browser-search-ui/08-HUMAN-UAT.md`. It depends on `templates/index.html` and the `render_template` route from Plan 01 being in place.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update test_root_returns_html and add test_root_uses_template</name>
  <files>tests/test_server.py</files>
  <read_first>
    - tests/test_server.py (current test_root_returns_html lines 115-119; fixtures client_not_ready lines 27-32 and client_ready lines 35-43; module docstring API-04 line)
    - .planning/phases/08-browser-search-ui/08-PATTERNS.md (section "tests/test_server.py (modify)" — updated assertion pattern and fixture-reuse note)
    - nitrofind/server.py (index() route after Plan 01 — returns render_template("index.html"))
    - templates/index.html (the rendered output the test asserts against — confirm <!DOCTYPE html> and NitroFind title are present)
  </read_first>
  <action>
    Edit tests/test_server.py:
    1. Update `test_root_returns_html` so it validates that the template rendered, not the old raw string. Keep the `client_not_ready` fixture. Keep `assert resp.status_code == 200` and `assert b"NitroFind" in resp.data`, and ADD `assert b"<!DOCTYPE html>" in resp.data`. Update the docstring to "GET / returns HTTP 200 with rendered index.html template".
    2. Add a new test `test_root_uses_template(client_not_ready)` with: `resp = client_not_ready.get("/")`; `assert resp.status_code == 200`; `assert resp.content_type.startswith("text/html")`; `assert b'data-state="home"' in resp.data` (structural marker only present in the template file, not in any raw string).
    3. Update the module docstring line about API-04 to mention the template route and the new test name, matching the existing docstring style.
    Do not modify other tests or fixtures.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind &amp;&amp; grep -q 'test_root_uses_template' tests/test_server.py &amp;&amp; grep -q 'DOCTYPE html' tests/test_server.py &amp;&amp; python -m pytest tests/test_server.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_server.py` contains `test_root_uses_template` function
    - `test_root_returns_html` contains `assert b"<!DOCTYPE html>" in resp.data`
    - `test_root_uses_template` checks `resp.content_type.startswith("text/html")` and `b'data-state="home"' in resp.data`
    - `python -m pytest tests/test_server.py -x -q` exits 0 (all tests pass, including the updated and new tests)
  </acceptance_criteria>
  <done>test_root_returns_html updated to assert <!DOCTYPE html> in response; test_root_uses_template added to prove render_template resolves the template at the explicit path.</done>
</task>

<task type="auto">
  <name>Task 2: Create 08-HUMAN-UAT.md browser smoke-test checklist</name>
  <files>.planning/phases/08-browser-search-ui/08-HUMAN-UAT.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 8 success criteria — the seven items are the source of truth for the checklist)
    - .planning/phases/08-browser-search-ui/08-CONTEXT.md (D-03 through D-11 — implementation details for each behavior)
  </read_first>
  <action>
    Create `.planning/phases/08-browser-search-ui/08-HUMAN-UAT.md`. Include:
    - A one-line setup instruction: `python main.py` then open http://localhost:5000 in a browser.
    - A checklist of 7 items mapping directly to the 7 ROADMAP success criteria:
      1. Type slowly in search box — results appear after 300ms pause without pressing Enter (SRCH-01)
      2. Each result row shows title, source domain, and excerpt with matching words bolded in the excerpt (SRCH-02)
      3. Click a result row — article text fills the page, no new browser tab opens; click the Back button — results list returns with same query/filters (SRCH-03)
      4. Select a filter dropdown (e.g. era "1960s"), type a query — results are filtered; retype a different query — filter selection remains (SRCH-04)
      5. Page loads with dark off-black background and teal accent; no bright white background; no neon glows (UIPL-01)
      6. After searching, a line below the search box reads "N results (X.XXs)" (UIPL-02)
      7. In the results state: ArrowDown/ArrowUp highlight individual result rows; Enter on highlighted row opens article; Escape clears the search input and returns to home (UIPL-03)
    - An additional ES warmup check: on fresh app start, search box is disabled with "Starting up…" text, which fades out and enables the input when ES becomes ready (D-07).
    Format each item as a markdown checkbox `- [ ]`.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind &amp;&amp; test -f .planning/phases/08-browser-search-ui/08-HUMAN-UAT.md &amp;&amp; grep -c '^\- \[ \]' .planning/phases/08-browser-search-ui/08-HUMAN-UAT.md | awk '{exit ($1 >= 7)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/08-browser-search-ui/08-HUMAN-UAT.md` exists
    - Contains at least 7 `- [ ]` checkbox items
    - Each of the 7 ROADMAP success criteria (SRCH-01..04, UIPL-01..03) is represented by at least one checkbox
    - Includes setup instruction referencing `python main.py` and `localhost:5000`
  </acceptance_criteria>
  <done>08-HUMAN-UAT.md provides a structured manual checklist for all seven success criteria plus the ES warmup behavior, ready for use as the phase gate before /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → Flask test client | pytest test client uses server.app internally; no external network calls |
| templates/index.html → Flask test client | Template file read from disk by Flask; render_template must resolve the correct path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Information Disclosure | test assertions | accept | Tests are development-only, not shipped; no sensitive data in assertions |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs — test modifications only; no new test framework |
</threat_model>

<verification>
- `python -m pytest tests/test_server.py -x -q` exits 0 (all tests pass after render_template switch).
- `test_root_uses_template` passes — confirming template_folder resolves correctly at test time.
- `.planning/phases/08-browser-search-ui/08-HUMAN-UAT.md` exists with at least 7 checkboxes.
- Manual: run through the UAT checklist against `python main.py` at http://localhost:5000.
</verification>

<success_criteria>
- pytest test suite remains green after the GET / route switch from raw string to render_template.
- A structural test (data-state="home" in response) proves template resolution, not just string presence.
- A complete manual UAT checklist exists for all seven browser-side success criteria.
</success_criteria>

<output>
Create `.planning/phases/08-browser-search-ui/08-03-SUMMARY.md` when done.
</output>
