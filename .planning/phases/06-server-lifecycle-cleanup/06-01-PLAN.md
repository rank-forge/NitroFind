---
phase: 06-server-lifecycle-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.in
  - requirements.txt
  - nitrofind/es_manager.py
  - tests/test_es_manager.py
autonomous: true
requirements: [CLEN-01, SRVR-04]
must_haves:
  truths:
    - "pip install -r requirements.txt installs no Qt package"
    - "es_manager.py exposes resolve_es_home, inject_es_config, validate_es_home, shutdown_es, _es_binary_path, ES_URL with no PyQt6 import"
    - "shutdown_es terminates the ES subprocess on Linux with SIGTERM then kill fallback"
  artifacts:
    - path: "requirements.in"
      provides: "Flask added; PyQt6 and qt-material removed"
      contains: "flask"
    - path: "requirements.txt"
      provides: "Regenerated lockfile with no Qt packages"
    - path: "nitrofind/es_manager.py"
      provides: "Qt-free, Linux-only subprocess/path utilities"
      contains: "def shutdown_es"
    - path: "tests/test_es_manager.py"
      provides: "Updated unit tests with ESHealthWorker tests removed"
  key_links:
    - from: "requirements.in"
      to: "requirements.txt"
      via: "pip-compile --generate-hashes"
      pattern: "flask"
---

<objective>
Add Flask to the dependency tree, remove all Qt packages, and strip `nitrofind/es_manager.py` down to its reusable Linux-only subprocess and path utilities (removing the `ESHealthWorker` QThread class, the PyQt6 import, and all Windows-specific branches). Update `tests/test_es_manager.py` to match the simplified module.

Purpose: Establish a Qt-free foundation. `server.py` (Plan 02) imports `ES_URL` and `shutdown_es` from this module, and the regenerated `requirements.txt` is what `pip install` reads — both must be clean before downstream plans run.
Output: `requirements.in`, `requirements.txt`, `nitrofind/es_manager.py`, `tests/test_es_manager.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-server-lifecycle-cleanup/06-CONTEXT.md
@.planning/phases/06-server-lifecycle-cleanup/06-RESEARCH.md
@.planning/phases/06-server-lifecycle-cleanup/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols and paths newly created or changed across Phase 6 (so source-grounding does not flag them as drift):

- `requirements.in` — `flask>=3.1` added; `PyQt6==6.11.0` and `qt-material==2.17` removed
- `nitrofind/es_manager.py` — `ESHealthWorker` class **removed**; `resolve_es_home`, `_es_binary_path`, `shutdown_es` simplified to Linux-only; `from PyQt6.QtCore import ...` removed
- `nitrofind/server.py` — NEW: `app` (Flask), `state` (module dict), `index()`, `api_status()`, `_start_es_process()`, `_es_health_poller()`, `_fetch_index_stats()`, `start_es_background()`
- `main.py` — rewritten: `main()` Flask lifecycle entry point (no Qt)
- `tests/test_server.py` — NEW test file
- `tests/test_es_manager.py` — `test_worker_emits_ready`, `test_worker_emits_failed` removed
- `nitrofind/ui/` — entire directory **deleted** (5 files)
- `tests/test_loading_window.py`, `tests/test_ui/*` — **deleted**
- `tests/test_search/test_engine.py` — PyQt6 skip guard added (engine.py PyQt6 removal deferred to Phase 7)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Swap Qt dependencies for Flask and regenerate the lockfile</name>
  <files>requirements.in, requirements.txt</files>
  <read_first>
    - requirements.in (current contents — lines to remove/add)
    - requirements.txt (current locked file — header shows pip-compile invocation)
    - .planning/phases/06-server-lifecycle-cleanup/06-RESEARCH.md (Pitfall 5: never hand-edit requirements.txt; Package Legitimacy Audit: flask [OK])
  </read_first>
  <action>
    In `requirements.in`: remove the line `PyQt6==6.11.0` and the line `qt-material==2.17`; add the line `flask>=3.1`. Leave all other lines (`elasticsearch==8.*`, `requests>=2.32,<3`, `mediawikiapi==1.3`, `beautifulsoup4>=4.12,<5`, `lxml>=5,<7`, `pyyaml>=6.0,<7`) unchanged.
    Regenerate the lockfile by running `pip-compile --generate-hashes requirements.in` (pip-tools 7.5.3 is installed). Do NOT hand-edit `requirements.txt`. The regenerated file must contain a `flask==` pin with hashes and must contain no `pyqt6`, `pyqt6-qt6`, `pyqt6-sip`, or `qt-material` entries.
    Flask is approved per the RESEARCH.md Package Legitimacy Audit (slopcheck [OK]) — no human verification checkpoint required for this install.
  </action>
  <verify>
    <automated>grep -qi '^flask' requirements.in && ! grep -qi 'pyqt6\|qt-material' requirements.in && grep -qi 'flask==' requirements.txt && ! grep -qi 'pyqt6\|qt-material' requirements.txt && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.in` contains a line matching `flask` (case-insensitive)
    - `grep -i 'pyqt6\|qt-material' requirements.in` returns no matches (exit non-zero)
    - `requirements.txt` contains a `flask==` pinned entry with `--hash` lines
    - `grep -i 'pyqt6\|qt-material' requirements.txt` returns no matches (exit non-zero)
  </acceptance_criteria>
  <done>Flask is in both requirements files with hashes; no Qt package appears in either file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strip es_manager.py to Qt-free Linux-only utilities and update its tests</name>
  <files>nitrofind/es_manager.py, tests/test_es_manager.py</files>
  <read_first>
    - nitrofind/es_manager.py (current full module — the source being simplified)
    - tests/test_es_manager.py (current tests — which to keep, which to delete)
    - .planning/phases/06-server-lifecycle-cleanup/06-PATTERNS.md (es_manager.py section: exact simplified forms of resolve_es_home, _es_binary_path, shutdown_es; test_es_manager.py section)
    - .planning/phases/06-server-lifecycle-cleanup/06-CONTEXT.md (D-01, D-07)
  </read_first>
  <behavior>
    - test_missing_es_home: `validate_es_home(None)` and `validate_es_home("")` each raise `ValueError` matching "ES_HOME is not set" (unchanged, kept).
    - test_shutdown_graceful: with a live mock process (`poll()` returns None), `shutdown_es` calls `process.terminate()` once and `process.wait(timeout=10)` once; `send_signal` is NOT called (no win32 branch remains).
    - test_shutdown_kills_on_timeout: when `wait` raises `TimeoutExpired` then succeeds, `shutdown_es` calls `process.kill()` once and `wait` twice (kept).
  </behavior>
  <action>
    Edit `nitrofind/es_manager.py` per D-01 and D-07:
    - Remove the import line `from PyQt6.QtCore import QThread, pyqtSignal`.
    - Simplify `resolve_es_home()` to a single body: `return os.environ.get("ES_HOME")` — delete the entire `if getattr(sys, "frozen", False):` frozen-mode branch and its `Path(sys.executable).parent.glob(...)` logic.
    - Simplify `_es_binary_path(es_home)` to `return os.path.join(es_home, "bin", "elasticsearch")` — delete the `if sys.platform == "win32":` branch and the `.bat` path.
    - Simplify `shutdown_es(process)`: keep the idempotent `if process.poll() is not None: return` guard, then call `process.terminate()` unconditionally (delete the `if sys.platform == "win32": process.send_signal(signal.CTRL_BREAK_EVENT)` branch and its comment block), then keep the `wait(timeout=10)` / `TimeoutExpired` → `kill()` + `wait()` fallback.
    - Delete the entire `ESHealthWorker(QThread)` class (the `es_ready`/`es_failed` signals, `__init__`, `run`, `shutdown_es` method, and `_start_process`).
    - Keep `inject_es_config`, `validate_es_home`, and the `ES_URL = "http://localhost:9200"` constant unchanged.
    - Remove now-unused imports if they become dead: `signal` (only used by the deleted CTRL_BREAK_EVENT branch) and `Path` (only used by the deleted frozen branch). Keep `os`, `shutil`, `subprocess`, `sys` if still referenced; `time` is only used by the deleted `ESHealthWorker.run()` so remove it. Update the module docstring to drop `ESHealthWorker`, INFRA-04, and Windows references.
    Edit `tests/test_es_manager.py`:
    - Change the import to `from nitrofind.es_manager import shutdown_es, validate_es_home` (drop `ESHealthWorker`).
    - Delete `test_worker_emits_ready` and `test_worker_emits_failed`.
    - Update `test_shutdown_graceful` to remove the `if sys.platform == "win32":` branch: assert `mock_process.terminate.assert_called_once()`, `mock_process.send_signal.assert_not_called()`, and `mock_process.wait.assert_called_once_with(timeout=10)`.
    - Keep `test_missing_es_home` and `test_shutdown_kills_on_timeout` as-is. Remove now-unused imports (`signal` if no longer referenced).
  </action>
  <verify>
    <automated>grep -vE '^\s*#' nitrofind/es_manager.py | grep -c 'PyQt6\|QThread\|win32\|ESHealthWorker' | grep -qx 0 && python -c "import nitrofind.es_manager as m; assert not hasattr(m,'ESHealthWorker'); assert m.ES_URL=='http://localhost:9200'" && pytest tests/test_es_manager.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*#' nitrofind/es_manager.py | grep 'PyQt6\|QThread\|win32\|ESHealthWorker'` returns no matches (exit non-zero)
    - `python -c "import nitrofind.es_manager as m; print(hasattr(m,'ESHealthWorker'))"` prints `False`
    - `python -c "from nitrofind.es_manager import resolve_es_home, inject_es_config, validate_es_home, shutdown_es, _es_binary_path, ES_URL"` exits 0
    - `_es_binary_path('/x')` returns `/x/bin/elasticsearch` (no `.bat`)
    - `pytest tests/test_es_manager.py -x -q` exits 0 with `test_worker_emits_ready` and `test_worker_emits_failed` no longer collected
  </acceptance_criteria>
  <done>es_manager.py imports no PyQt6 and has no ESHealthWorker or Windows branches; the kept utilities import cleanly; test_es_manager.py passes with the two worker tests removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS env → process | `ES_HOME` env var controls which binary `validate_es_home`/`_es_binary_path` point at |
| package index → install | `pip install` reads `requirements.txt`; supply-chain trust crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | `validate_es_home` (malicious ES_HOME) | mitigate | Keep existing `isdir` + `isfile` guards before any exec (unchanged from v1.0) |
| T-06-SC | Tampering | pip install (flask) | accept | flask is slopcheck [OK] per RESEARCH.md Package Legitimacy Audit; 16-yr-old, tens-of-millions/mo downloads, official pallets repo — no [ASSUMED]/[SUS] package in this plan, so no blocking human checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_es_manager.py -x -q` passes.
- `grep -ri 'pyqt6\|qt-material' requirements.in requirements.txt` returns empty.
- `grep -vE '^\s*#' nitrofind/es_manager.py | grep 'PyQt6\|QThread\|win32'` returns empty.
</verification>

<success_criteria>
- Flask is the only new dependency; both Qt packages are gone from `requirements.in` and `requirements.txt`.
- `nitrofind/es_manager.py` is importable, Qt-free, Linux-only, and exposes `ES_URL`, `shutdown_es`, `resolve_es_home`, `inject_es_config`, `validate_es_home`, `_es_binary_path`.
- `tests/test_es_manager.py` passes with `ESHealthWorker` tests removed.
</success_criteria>

<output>
Create `.planning/phases/06-server-lifecycle-cleanup/06-01-SUMMARY.md` when done.
</output>
