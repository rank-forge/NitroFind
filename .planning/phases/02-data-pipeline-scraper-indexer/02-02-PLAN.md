---
phase: 02-data-pipeline-scraper-indexer
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - nitrofind/scraper/__init__.py
  - nitrofind/scraper/cleaner.py
  - nitrofind/scraper/state.py
  - nitrofind/scraper/indexer.py
  - tests/test_scraper/test_cleaner.py
  - tests/test_scraper/test_state.py
  - tests/test_scraper/test_indexer.py
autonomous: true
requirements: [SCRP-03, SCRP-04]
must_haves:
  truths:
    - "make_excerpt always returns a string of length <= 300 that ends at a word boundary"
    - "compute_era_bucket(year) returns '{decade}s' for valid years and 'Unknown' when year is None/0"
    - "SQLiteStateManager.mark_visited followed by is_visited returns True; calling mark_visited twice with same id does not raise"
    - "BulkIndexer.index_all halts and logs warning when index size reported by indices.stats reaches SIZE_HALT_BYTES (1.8 GB)"
    - "build_action(doc) returns dict with _index='car_articles' and _id=doc['article_id'] for upsert deduplication"
    - "BulkIndexer uses primaries.store.size_in_bytes (not total) for size guard (Pitfall 8)"
  artifacts:
    - path: "nitrofind/scraper/__init__.py"
      provides: "Package marker for nitrofind.scraper namespace"
    - path: "nitrofind/scraper/cleaner.py"
      provides: "make_excerpt, compute_era_bucket, parse_year — pure transform utilities"
      exports: ["make_excerpt", "compute_era_bucket", "parse_year"]
    - path: "nitrofind/scraper/state.py"
      provides: "SQLiteStateManager class with is_visited, mark_visited, close, __enter__/__exit__"
      exports: ["SQLiteStateManager"]
    - path: "nitrofind/scraper/indexer.py"
      provides: "BulkIndexer class with index_all + size guard; build_action helper; SIZE_HALT_BYTES constant"
      exports: ["BulkIndexer", "build_action", "SIZE_HALT_BYTES", "CHECK_EVERY_N_DOCS"]
  key_links:
    - from: "nitrofind/scraper/indexer.py"
      to: "nitrofind/es_manager.py"
      via: "from nitrofind.es_manager import ES_URL (never hardcode localhost:9200)"
      pattern: "from nitrofind.es_manager import ES_URL"
    - from: "nitrofind/scraper/indexer.py"
      to: "elasticsearch.helpers.streaming_bulk"
      via: "for ok, info in streaming_bulk(client, actions_generator, chunk_size=100, raise_on_error=False, raise_on_exception=False)"
      pattern: "streaming_bulk"
    - from: "nitrofind/scraper/state.py"
      to: "sqlite3 (stdlib)"
      via: "sqlite3.connect(db_path) with INSERT OR IGNORE for idempotency"
      pattern: "sqlite3.connect"
---

<objective>
Build the source-independent core of the scraper package: the pure-function text cleaner (excerpt, era_bucket, parse_year), the SQLite-backed resume state manager (D-06), and the ES bulk indexer with size guard (SCRP-03 dedup via _id, SCRP-04 halt at 1.8 GB). These three modules have no dependency on Wikipedia or blog scrapers — they expose interfaces that Plans 03 and 04 consume.

Purpose: Vertical-slice three foundational pieces in a single plan because they are tightly related (the indexer needs state for visit-tracking, both will be wired together by the CLI in Plan 05). Each is small (< 60 lines), has pure or near-pure semantics, and is fully unit-testable without network or live ES. Replacing the Wave 0 stubs in test_cleaner.py / test_state.py / test_indexer.py is part of this plan — the executor flips `pytest.importorskip` -> real imports and `pytest.skip` -> real assertions.

Output: A working `nitrofind/scraper/` package with three modules importable, fully unit-tested with `pytest tests/test_scraper/test_cleaner.py tests/test_scraper/test_state.py tests/test_scraper/test_indexer.py -x -m "not integration"` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-CONTEXT.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-VALIDATION.md
@nitrofind/es_schema.py
@nitrofind/es_manager.py

<interfaces>
<!-- Existing code these modules must integrate with -->

From nitrofind/es_manager.py:
- `ES_URL = "http://localhost:9200"` — single source of truth; import this, never hardcode
- Error-handling convention: `except Exception as exc: logger.warning("...: %s: %s", type(exc).__name__, exc)`
- Module-level constant pattern (see line 35)

From nitrofind/es_schema.py:
- `CAR_ARTICLES_MAPPING.properties` keys define every allowed field — see file for full list
- `ensure_index(client)` — idempotent index creator (indexer does NOT call this; Plan 05 CLI does at startup)
- Index name is literal string `"car_articles"`

From elasticsearch package (already pinned in requirements.txt at 8.19.3):
- `from elasticsearch import Elasticsearch`
- `from elasticsearch.helpers import streaming_bulk`
- `streaming_bulk(client, actions, chunk_size=100, raise_on_error=False, raise_on_exception=False)` yields `(ok: bool, info: dict)`
- `client.indices.stats(index="car_articles", metric="store")` returns dict; access `result["indices"]["car_articles"]["primaries"]["store"]["size_in_bytes"]` (Pitfall 8 — use primaries NOT total)

From tests/test_es_manager.py:
- Test module docstring style (lines 1-13): "Test strategy:" and "Requirement coverage:" sections
- Test grouping pattern: `# ---` 76-hyphen separator + section name comment
- Call class methods directly (synchronous) — never start QThread

From tests/test_es_schema.py:
- Pure-function test pattern: no mocks needed for stdlib + pure logic
- Mock pattern for ES client: `MagicMock()` then `mock_client.indices.stats.return_value = {...}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement nitrofind/scraper/cleaner.py with pure text utilities</name>
  <files>nitrofind/scraper/__init__.py, nitrofind/scraper/cleaner.py, tests/test_scraper/test_cleaner.py</files>
  <read_first>
    - nitrofind/__init__.py (package marker convention — single comment line)
    - nitrofind/es_schema.py (module docstring style; export list pattern)
    - tests/test_es_schema.py (pure-function test pattern, no mocks for stdlib)
    - tests/test_scraper/test_cleaner.py (Wave 0 stub — replace pytest.importorskip + pytest.skip bodies with real implementations)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md (nitrofind/scraper/cleaner.py section — docstring + transform implementations)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md (Pitfall 7 — excerpt mid-word cut; L-06 300 char cap; L-07 era_bucket formula)
  </read_first>
  <behavior>
    - Test: `make_excerpt("word " * 200)` returns string with `len(result) <= 300` AND result does not end mid-word (does not end with a partial word).
    - Test: `make_excerpt("short text")` returns `"short text"` unchanged (input below 300 chars passes through).
    - Test: `make_excerpt("a" * 295 + " boundary_word")` returns text ending on a space-delimited boundary (no `boundar` prefix at end).
    - Test: `compute_era_bucket(1965) == "1960s"`; `compute_era_bucket(2003) == "2000s"`; `compute_era_bucket(1999) == "1990s"`.
    - Test: `compute_era_bucket(None) == "Unknown"`; `compute_era_bucket(0) == "Unknown"`.
    - Test: `parse_year("1965 to 1972")` returns `1965`; `parse_year("manufactured in 1999")` returns `1999`; `parse_year("")` and `parse_year("no year here")` both return `None`.
  </behavior>
  <action>
    Create `nitrofind/scraper/__init__.py` as a one-line comment (`# NitroFind scraper package`) — matches `nitrofind/__init__.py` style.

    Create `nitrofind/scraper/cleaner.py` exporting three pure functions per L-05/L-06/L-07/Pitfall 7:
      - `make_excerpt(body_text: str) -> str` — when `len(body_text) <= 300` return as-is; otherwise return `body_text[:300].rsplit(" ", 1)[0]` (Pitfall 7 word-boundary trim). Handles empty string (returns `""`).
      - `compute_era_bucket(production_start: Optional[int]) -> str` — return `"Unknown"` when input is falsy (None, 0); otherwise return `f"{(production_start // 10) * 10}s"` (L-07 exact formula).
      - `parse_year(raw: str) -> Optional[int]` — return `None` for empty/None input; else `re.search(r"\b(19|20)\d{2}\b", raw)` and return `int(match.group())` or `None`. (Limits years to 1900-2099 per L-07 reasonable car-era range.)
    Module docstring follows `es_schema.py` style: Exports section, Requirement coverage (L-05, L-06, L-07, SCHEMA-03, Pitfall 7), Anti-patterns avoided. Imports: `import re`, `from typing import Optional`. No `logging` — pure functions.

    Update `tests/test_scraper/test_cleaner.py`: replace the Wave 0 `pytest.importorskip` with `from nitrofind.scraper.cleaner import make_excerpt, compute_era_bucket, parse_year`. Replace each `pytest.skip("Wave 1 implementation")` body with the real assertion described in `<behavior>` above. Add edge case `test_make_excerpt_empty_input` returning `""` and `test_make_excerpt_no_spaces` ensuring a 301-character no-space string returns `body_text[:300]` (rsplit returns full string on no-space input — verify this matches L-06 ≤300 by adjusting if needed: when `" "` not in `body_text[:300]`, `rsplit(" ", 1)` returns `[body_text[:300]]` so `[0]` is the full 300-char string, which is allowed).
  </action>
  <verify>
    <automated>pytest tests/test_scraper/test_cleaner.py -x -m "not integration" -v 2>&1 | grep -E "PASSED|FAILED" | wc -l | xargs -I{} test {} -ge 5 && python -c "from nitrofind.scraper.cleaner import make_excerpt, compute_era_bucket, parse_year; assert make_excerpt('a'*400).__len__() <= 300; assert compute_era_bucket(1965) == '1960s'; assert compute_era_bucket(None) == 'Unknown'; assert parse_year('made in 1999') == 1999; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `nitrofind/scraper/__init__.py` exists (single comment line acceptable)
    - `nitrofind/scraper/cleaner.py` contains `def make_excerpt(`, `def compute_era_bucket(`, `def parse_year(`
    - `nitrofind/scraper/cleaner.py` module docstring contains substrings `Exports:` and `Requirement coverage:` and references `L-06` and `L-07`
    - `from nitrofind.scraper.cleaner import make_excerpt, compute_era_bucket, parse_year` succeeds
    - `make_excerpt("a " * 200)` returns string with `len(result) <= 300`
    - `make_excerpt("hello")` returns `"hello"` unchanged
    - `compute_era_bucket(1965) == "1960s"`, `compute_era_bucket(None) == "Unknown"`, `compute_era_bucket(0) == "Unknown"`
    - `parse_year("made in 1999") == 1999`, `parse_year("") is None`, `parse_year("hello") is None`
    - `pytest tests/test_scraper/test_cleaner.py -x -m "not integration"` exits 0 with at least 5 PASSED tests
  </acceptance_criteria>
  <done>
    Cleaner module ships three pure functions exercising L-05/L-06/L-07 + Pitfall 7; test file flipped from stubs to real assertions; full unit suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement nitrofind/scraper/state.py with SQLiteStateManager</name>
  <files>nitrofind/scraper/state.py, tests/test_scraper/test_state.py</files>
  <read_first>
    - nitrofind/es_manager.py (class init + lifecycle pattern; error-handling convention)
    - nitrofind/es_schema.py (module docstring style with Exports + Requirement coverage + Security mitigations sections)
    - tests/test_scraper/test_state.py (Wave 0 stub — replace stubs with real tests)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md (nitrofind/scraper/state.py section — class init + context manager pattern)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md (Pattern 5 SQLite State Tracking; Security Domain — path traversal on db_path)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-CONTEXT.md (D-06 resume support)
  </read_first>
  <behavior>
    - Test: `SQLiteStateManager(":memory:").is_visited("page_123") is False` (unseen ID returns False)
    - Test: after `state.mark_visited("page_123", "wikipedia")`, `state.is_visited("page_123") is True`
    - Test: calling `state.mark_visited("page_123", "wikipedia")` twice in succession does not raise (INSERT OR IGNORE idempotency)
    - Test: `with SQLiteStateManager(":memory:") as state: ...` works (context manager); `state._conn` is closed after `__exit__` (assert via `state._conn.close()` raising `sqlite3.ProgrammingError` after exit, or check using `try: state._conn.execute('SELECT 1'); except sqlite3.ProgrammingError: pass`).
    - Test: passing a path containing `..` (e.g., `"../etc/passwd"`) raises `ValueError` from path-traversal guard.
  </behavior>
  <action>
    Create `nitrofind/scraper/state.py` exporting `SQLiteStateManager` class. Module docstring: Exports, Requirement coverage (D-06), Security mitigations (T-02-05 path traversal on db_path).
    Imports: `import os`, `import sqlite3`, `from pathlib import Path`.

    Class API:
      - `__init__(self, db_path: str) -> None`:
        1. Path traversal guard: if `db_path != ":memory:"`, resolve `Path(db_path).resolve()` and reject if it does NOT start with `Path.cwd().resolve()` (project working directory). Raise `ValueError(f"db_path must be inside project directory: {db_path}")` on rejection. The `:memory:` literal bypasses the check for tests.
        2. Open `self._conn = sqlite3.connect(db_path)` and create table `visited` with columns `id TEXT PRIMARY KEY, source TEXT NOT NULL, indexed_at TEXT NOT NULL` (verbatim from RESEARCH.md Pattern 5).
        3. Commit.
      - `is_visited(self, item_id: str) -> bool` — `SELECT 1 FROM visited WHERE id = ?` parameterized query; `fetchone() is not None`.
      - `mark_visited(self, item_id: str, source: str) -> None` — `INSERT OR IGNORE INTO visited (id, source, indexed_at) VALUES (?, ?, datetime('now'))` parameterized; commit.
      - `close(self) -> None` — `self._conn.close()`.
      - `__enter__(self)` returns `self`; `__exit__(self, *args)` calls `self.close()`.

    All SQL parameters MUST use `?` placeholders (parameterized queries) — never f-string interpolation into SQL (SQL injection mitigation, idiomatic Python sqlite3).

    Update `tests/test_scraper/test_state.py`: replace `pytest.importorskip` with `from nitrofind.scraper.state import SQLiteStateManager`. Replace stub bodies with the `<behavior>` test assertions. Use `:memory:` for all positive-path tests (no file I/O). Add `test_db_path_traversal_rejected` exercising `SQLiteStateManager("../etc/passwd")` raising `ValueError` (use pytest.raises). Add `test_visited_persists_across_reopen` using a tempfile fixture (`tmp_path` from pytest): open state, mark_visited, close; reopen at same path; assert is_visited returns True.
  </action>
  <verify>
    <automated>pytest tests/test_scraper/test_state.py -x -m "not integration" -v 2>&1 | grep -E "PASSED|FAILED" | wc -l | xargs -I{} test {} -ge 5 && python -c "from nitrofind.scraper.state import SQLiteStateManager; s = SQLiteStateManager(':memory:'); assert s.is_visited('x') is False; s.mark_visited('x', 'wikipedia'); assert s.is_visited('x') is True; s.mark_visited('x', 'wikipedia'); s.close(); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `nitrofind/scraper/state.py` defines `class SQLiteStateManager:` with methods `__init__`, `is_visited`, `mark_visited`, `close`, `__enter__`, `__exit__`
    - Module docstring contains `Exports:` and `Requirement coverage:` sections and references `D-06`
    - `SQLiteStateManager(":memory:")` instantiates without error
    - `SQLiteStateManager("../etc/passwd")` raises `ValueError` with message containing `db_path must be inside project directory`
    - All SQL statements in the file use `?` placeholders (no f-string SQL): grep for `f"INSERT` or `f"SELECT` in file returns 0 matches
    - `pytest tests/test_scraper/test_state.py -x -m "not integration"` exits 0 with at least 5 PASSED tests
    - File-based persistence test passes: state survives close + reopen at same db_path
  </acceptance_criteria>
  <done>
    SQLiteStateManager exports the D-06 resume API; path traversal guard rejects malicious db_paths; parameterized queries throughout; tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement nitrofind/scraper/indexer.py with BulkIndexer + size guard</name>
  <files>nitrofind/scraper/indexer.py, tests/test_scraper/test_indexer.py</files>
  <read_first>
    - nitrofind/es_manager.py (ES_URL constant; error-handling pattern lines 163-164; class structure)
    - nitrofind/es_schema.py (Elasticsearch import line 17)
    - tests/test_es_manager.py (MagicMock + patch pattern for ES client; section header style)
    - tests/test_scraper/test_indexer.py (Wave 0 stub — flip stubs to real tests)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md (nitrofind/scraper/indexer.py section — BulkIndexer class + build_action + size-guard implementation)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md (Pattern 3 ES Bulk Indexing with Size Guard; Pattern 4 Bulk Action Format with _id Deduplication; Pitfall 8 primaries vs total)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-CONTEXT.md (L-03, L-04, SCRP-03, SCRP-04)
  </read_first>
  <behavior>
    - Test: `build_action({"article_id": "12345", "title": "Ferrari 308", "body": "text"})` returns dict with `_index == "car_articles"` and `_id == "12345"` and includes all other fields from input doc.
    - Test: `SIZE_HALT_BYTES == 1_800_000_000` and `CHECK_EVERY_N_DOCS == 100` (module constants exported).
    - Test (unit, mocked): with `streaming_bulk` patched to yield 150 successful results and `client.indices.stats` returning size >= SIZE_HALT_BYTES, `BulkIndexer.index_all(actions)` returns a doc count <= 150 (halted early after the first size check at doc 100) and a warning is logged with substring "Halting scraper" and "SCRP-04".
    - Test (unit, mocked): with size always below halt threshold, all yielded results are counted; returned count equals the number of `(ok=True, info)` items.
    - Test (unit): `_index_size_bytes` correctly reads `stats["indices"]["car_articles"]["primaries"]["store"]["size_in_bytes"]` from a mocked stats dict (Pitfall 8 — uses primaries NOT total).
    - Test (integration, marked `@pytest.mark.integration`): with live ES, indexing same `_id` twice produces no duplicate count increase (SCRP-03). Skip if `ES_HOME` not set.
  </behavior>
  <action>
    Create `nitrofind/scraper/indexer.py` per RESEARCH.md Pattern 3 + Pattern 4. Module docstring with Exports, Requirement coverage (SCRP-03, SCRP-04, L-03, L-04), Anti-patterns avoided (per-document client.index() calls; Pitfall 8 total vs primaries).

    Imports: `import logging`, `from elasticsearch import Elasticsearch`, `from elasticsearch.helpers import streaming_bulk`, `from nitrofind.es_manager import ES_URL` (single source of truth — never hardcode localhost URL).

    Module constants:
      - `SIZE_HALT_BYTES = 1_800_000_000`  # 1.8 GB — SCRP-04 / L-04
      - `CHECK_EVERY_N_DOCS = 100`

    Module-level logger: `logger = logging.getLogger(__name__)`.

    Free function `build_action(doc: dict) -> dict`:
      - `action = {"_index": "car_articles", "_id": doc["article_id"]}` (L-03 — article_id is str(pageid) for Wikipedia, URL slug for blogs)
      - `action.update(doc)` then return action.

    Class `BulkIndexer`:
      - `__init__(self, client: Elasticsearch, state)` — store as `self._client` and `self._state`. (state is `SQLiteStateManager`; type hint as `"SQLiteStateManager" | None` if needed but plain attribute storage is acceptable.)
      - `index_all(self, actions_generator) -> int`: iterate `streaming_bulk(self._client, actions_generator, chunk_size=100, raise_on_error=False, raise_on_exception=False)`. For each `(ok, info)`:
        * If not ok: `logger.warning("Bulk index error: %s", info)`; continue.
        * Else: `doc_count += 1`; if `doc_count % CHECK_EVERY_N_DOCS == 0`: call `self._index_size_bytes()`; log INFO with current count + size in MB; if `size >= SIZE_HALT_BYTES`: log WARNING containing literal substring `"Halting scraper"` AND `"SCRP-04"` AND the human-readable size; `return doc_count`.
        * After loop end: return doc_count.
      - `_index_size_bytes(self) -> int`: `stats = self._client.indices.stats(index="car_articles", metric="store")`; return `stats["indices"]["car_articles"]["primaries"]["store"]["size_in_bytes"]`. (Pitfall 8 — uses `primaries` NOT `total`.)

    Error handling within `index_all`: wrap the streaming_bulk loop in `try/except Exception as exc: logger.warning("Bulk indexing failed: %s: %s", type(exc).__name__, exc); raise` so unexpected ES errors surface to the CLI (Plan 05) rather than being silently swallowed. Errors logged per-action via `raise_on_error=False` (info logged at warning level above).

    Update `tests/test_scraper/test_indexer.py`: replace `pytest.importorskip` with `from nitrofind.scraper.indexer import BulkIndexer, build_action, SIZE_HALT_BYTES, CHECK_EVERY_N_DOCS`. Replace stub bodies with the `<behavior>` assertions. For the size-guard test, patch `nitrofind.scraper.indexer.streaming_bulk` to return `iter([(True, {}) for _ in range(150)])` and configure mock client's `indices.stats` return value with `size_in_bytes = SIZE_HALT_BYTES + 1`. Use pytest's `caplog` fixture (set level INFO) to assert the warning message contains `"Halting scraper"` and `"SCRP-04"`. For the integration test `test_deduplication_no_duplicate_docs`, decorate `@pytest.mark.integration`; skip if `os.environ.get("ES_HOME") is None`. Inside: create live `Elasticsearch(ES_URL)`, call `ensure_index(client)` from es_schema, build a fixed doc with `article_id="test_dedup_xyz"`, index via `BulkIndexer.index_all` twice, then query `client.count(index="car_articles", query={"term": {"article_id": "test_dedup_xyz"}})` — assert count == 1.
  </action>
  <verify>
    <automated>pytest tests/test_scraper/test_indexer.py -x -m "not integration" -v 2>&1 | grep -E "PASSED|FAILED" | wc -l | xargs -I{} test {} -ge 4 && python -c "from nitrofind.scraper.indexer import BulkIndexer, build_action, SIZE_HALT_BYTES, CHECK_EVERY_N_DOCS; assert SIZE_HALT_BYTES == 1_800_000_000; assert CHECK_EVERY_N_DOCS == 100; a = build_action({'article_id': '42', 'title': 'X'}); assert a['_id'] == '42'; assert a['_index'] == 'car_articles'; print('ok')" && grep -q "from nitrofind.es_manager import ES_URL" nitrofind/scraper/indexer.py && ! grep -q '"http://localhost:9200"' nitrofind/scraper/indexer.py</automated>
  </verify>
  <acceptance_criteria>
    - `nitrofind/scraper/indexer.py` defines `class BulkIndexer:`, `def build_action(`, module constant `SIZE_HALT_BYTES = 1_800_000_000`, `CHECK_EVERY_N_DOCS = 100`
    - Module imports `from nitrofind.es_manager import ES_URL`
    - File contains NO occurrence of the literal `"http://localhost:9200"` (grep returns zero matches)
    - `_index_size_bytes` accesses `["primaries"]["store"]["size_in_bytes"]` and NOT `["total"]["store"]` (grep `total\b.*store` returns 0; grep `primaries.*store` returns ≥ 1)
    - `build_action({"article_id": "42", "title": "X"})` returns `{"_index": "car_articles", "_id": "42", "article_id": "42", "title": "X"}`
    - `pytest tests/test_scraper/test_indexer.py -x -m "not integration"` exits 0 with at least 4 PASSED tests
    - Size-guard test exercises caplog and asserts warning contains both `"Halting scraper"` AND `"SCRP-04"` literal substrings
    - Integration test is collected (decorator present); `pytest tests/test_scraper/test_indexer.py -m integration --collect-only` lists at least one test
  </acceptance_criteria>
  <done>
    BulkIndexer halts before 1.8 GB, build_action sets _id for deduplication, indexer imports ES_URL (no hardcoded localhost), Pitfall 8 honored (primaries not total), tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> SQLiteStateManager | Caller may pass arbitrary `db_path`. Project-rooted validation prevents traversal. |
| scraper documents -> Elasticsearch | Scraper feeds bulk actions to ES. `dynamic: "false"` on car_articles index (Phase 1 lockdown) drops any extra fields silently — defense-in-depth against injection via infobox/blog HTML. |
| BulkIndexer -> indices.stats response | ES stats response is trusted (localhost only) — no validation of size_in_bytes integer beyond key access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering (path traversal) | `SQLiteStateManager.__init__(db_path)` | mitigate | Validate `Path(db_path).resolve()` starts with `Path.cwd().resolve()` before `sqlite3.connect`. Special case `:memory:` for tests. Raise `ValueError` on rejection. ASVS V5 (input validation). |
| T-02-06 | Tampering (SQL injection) | SQLiteStateManager queries | mitigate | All SQL uses `?` parameterized placeholders. Acceptance criteria grep-asserts no f-string SQL. |
| T-02-07 | Denial of Service (unbounded index) | `BulkIndexer.index_all` | mitigate | `SIZE_HALT_BYTES = 1_800_000_000` halt threshold logged with WARNING and `SCRP-04` tag (SCRP-04 requirement). Generator abandoned on halt — no further indexing. |
| T-02-08 | Tampering (ES index poisoning via doc fields) | `build_action` -> ES | accept | `dynamic: "false"` on the index mapping (Phase 1) discards any keys not in `CAR_ARTICLES_MAPPING.properties`. Scraper-side extra fields are silently dropped — no further mitigation needed here. Plan 03/04 must still construct doc dicts that match the schema; mismatched fields will be silently lost (not a security issue, but a data-loss bug). |
</threat_model>

<verification>
1. `pytest tests/test_scraper/test_cleaner.py tests/test_scraper/test_state.py tests/test_scraper/test_indexer.py -x -m "not integration"` exits 0 with at least 14 PASSED tests
2. `pytest tests/ -x -m "not integration"` — full unit suite green (Phase 1 + new Phase 2 unit tests)
3. `grep -n "http://localhost:9200" nitrofind/scraper/*.py` returns no matches (ES_URL imported, never hardcoded)
4. `grep -n 'f"INSERT\|f"SELECT\|f"UPDATE\|f"DELETE' nitrofind/scraper/state.py` returns no matches (parameterized queries only)
5. `python -c "from nitrofind.scraper.cleaner import make_excerpt, compute_era_bucket, parse_year; from nitrofind.scraper.state import SQLiteStateManager; from nitrofind.scraper.indexer import BulkIndexer, build_action, SIZE_HALT_BYTES; print('all imports ok')"`
</verification>

<success_criteria>
- nitrofind/scraper/ package exists with __init__.py + cleaner.py + state.py + indexer.py
- All three modules importable, exported symbols match the contract
- cleaner.py: make_excerpt always ≤300 chars at word boundary; compute_era_bucket maps year->decade; parse_year extracts 4-digit year
- state.py: SQLiteStateManager supports is_visited, mark_visited (idempotent via INSERT OR IGNORE), context manager; path traversal guard rejects malicious db_path
- indexer.py: BulkIndexer halts at 1.8 GB (SIZE_HALT_BYTES) with logged warning containing "SCRP-04"; build_action sets _id=article_id for dedup (SCRP-03); uses primaries not total (Pitfall 8); imports ES_URL (no hardcoded localhost)
- Wave 0 test stubs in test_cleaner.py / test_state.py / test_indexer.py converted to real assertions; all unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline-scraper-indexer/02-02-SUMMARY.md` documenting:
- New module file paths + line counts
- Exported symbols per module
- Test counts: PASSED unit tests in test_cleaner / test_state / test_indexer
- Confirmation: pytest tests/ -x -m "not integration" exits 0
- Confirmation: no `http://localhost:9200` literal in any nitrofind/scraper/*.py file
</output>
