---
phase: 02-data-pipeline-scraper-indexer
plan: 04
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
files_modified:
  - config/scraper.yaml
  - nitrofind/scraper/blogs.py
  - tests/test_scraper/test_blogs.py
autonomous: false
requirements: [SCRP-02]
must_haves:
  truths:
    - "Developer has manually verified that Hagerty (or fallback target) responds with HTTP 200 to a NitroFind User-Agent and captured the current article-container CSS selector"
    - "config/scraper.yaml article_selector and listing_selector for the verified target reflect actual current site HTML (no longer marked ASSUMED)"
    - "BlogScraper reads target config from config dict (selectors NOT hardcoded — Pitfall 4)"
    - "BlogScraper.yield_documents handles HTTP 403 / Cloudflare challenge by logging warning and returning None (Pitfall 3)"
    - "Blog body field contains only plain text (no `<` or `>` characters from BeautifulSoup.get_text)"
    - "Each blog document has has_infobox=False, source_domain set from target.name->base_url host, article_id derived from URL slug"
    - "Honest User-Agent string: NitroFind/1.0 (...) — no browser impersonation per project ethic"
    - "If all blog targets fail (all 403), scraper logs warning + completes with Wikipedia-only mode (does not raise — phase success criterion 2 requires at least one blog OR Wikipedia-only when blogs inaccessible)"
  artifacts:
    - path: "config/scraper.yaml"
      provides: "Updated blog target selectors verified against live HTML (or marked still-ASSUMED with a fallback selector)"
      contains: "article_selector"
    - path: "nitrofind/scraper/blogs.py"
      provides: "BlogScraper class — article list fetch, article page fetch, BS4 parse, doc dict generation"
      exports: ["BlogScraper"]
  key_links:
    - from: "nitrofind/scraper/blogs.py"
      to: "bs4.BeautifulSoup"
      via: "BeautifulSoup(resp.text, 'lxml'); container.get_text(separator=' ', strip=True)"
      pattern: "from bs4 import BeautifulSoup"
    - from: "nitrofind/scraper/blogs.py"
      to: "nitrofind.scraper.cleaner (Plan 02)"
      via: "make_excerpt(body)"
      pattern: "from nitrofind.scraper.cleaner import make_excerpt"
    - from: "nitrofind/scraper/blogs.py"
      to: "nitrofind.scraper.state.SQLiteStateManager (Plan 02)"
      via: "self._state.is_visited(url); self._state.mark_visited(url, target_name)"
      pattern: "self._state.is_visited"
    - from: "config/scraper.yaml blogs.targets"
      to: "BlogScraper._config['blogs']['targets']"
      via: "Iterate enabled targets in order; first 200-OK target wins (fallback chain)"
      pattern: "for target in self._config['targets']"
---

<objective>
Build the automotive blog data source. SCRP-02 requires at least one blog target to be indexed; RESEARCH.md confirms all four targets (Hagerty, Car and Driver, Hemmings, Road & Track) return non-200 to automated WebFetch, making selector + accessibility verification a live, human-required step. This plan splits the work: a human-verify checkpoint captures current CSS selectors and confirms accessibility, then BlogScraper implements the BS4 parsing + fallback chain + graceful 403 handling using the verified config.

Purpose: Plan must be `autonomous: false` because Task 1 is a `checkpoint:human-verify` — Claude cannot run a real browser to inspect Hagerty's HTML, and `requests.get` returns 403 (verified at research time). After the checkpoint completes, Tasks 2-3 are fully automated: BlogScraper code + unit tests with mocked HTTP responses.

Output: Updated config/scraper.yaml selectors, a BlogScraper module yielding clean blog document dicts, and unit tests covering 403 handling, missing-container fallback, and dedup-via-state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-CONTEXT.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md
@.planning/phases/02-data-pipeline-scraper-indexer/02-VALIDATION.md
@nitrofind/es_schema.py

<interfaces>
<!-- Modules and config that blogs.py depends on -->

From nitrofind.scraper.cleaner (Plan 02 contract):
- make_excerpt(body_text: str) -> str — returns 300-char-or-less excerpt at word boundary

From nitrofind.scraper.state (Plan 02 contract):
- SQLiteStateManager.is_visited(item_id: str) -> bool
- SQLiteStateManager.mark_visited(item_id: str, source: str) -> None

From config/scraper.yaml (Plan 01) — blogs section schema:
- blogs.targets: list of dicts each with: name, enabled, base_url, article_list_url, article_selector, listing_selector

From requests library (already pinned at 2.34.0):
- requests.Session() with .headers / .get(url, timeout=15)
- requests.HTTPError, requests.RequestException
- response.status_code, response.text, response.raise_for_status()

From bs4 (added in Plan 01):
- BeautifulSoup(html, "lxml")
- soup.select(css_selector) -> list, soup.select_one(css_selector) -> Tag|None
- tag.decompose(), tag.get_text(separator=" ", strip=True)

From nitrofind/es_schema.py — Allowed fields (same as Wikipedia, but has_infobox=False; production_start/manufacturer/specs etc. may be empty strings or omitted):
- title, url, source_domain, article_id, scraped_at, body, excerpt, word_count, has_infobox, image_count
- Blog docs typically omit production_start/production_end/manufacturer/body_style/era_bucket/country_of_origin/specs (those become empty/missing — set has_infobox=False and era_bucket="Unknown" to satisfy schema; do not invent values)

Verified at research time (RESEARCH.md Blog Target Assessment + Pitfall 3):
- Hagerty: confirmed 403 on direct WebFetch
- Car and Driver: connection error
- Hemmings: connection error
- Road & Track: not verified; high risk
- All four return non-200 — manual inspection required at implementation time
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual blog inspection — capture current CSS selectors and confirm accessibility</name>
  <files>config/scraper.yaml</files>
  <what-built>
    Claude already shipped Plan 01's config/scraper.yaml with three blog targets pre-populated:
      - hagerty (enabled, ASSUMED selectors)
      - caranddriver (disabled, ASSUMED selectors)
      - hemmings (disabled, ASSUMED selectors)
    Selectors are marked `# ASSUMED — verify at implementation` because RESEARCH.md confirms automated WebFetch was blocked on all four targets. CSS class names rotate on these sites and cannot be confirmed without a live browser.
  </what-built>
  <how-to-verify>
    DEVELOPER STEPS — perform in this order; stop at the first target that succeeds:

    1. Open a real browser (Firefox/Chrome) and visit `https://www.hagerty.com/media/all-articles/`. Confirm the page loads (no Cloudflare challenge, no login wall). If 403/blocked, skip to step 5.
    2. Open DevTools (F12) → Elements tab. Right-click a single article link on the listing page → Inspect. Identify the most stable CSS selector that uniquely identifies article links (typically a class like `a.article-card__link`, `article a[href*="/media/"]`, or similar). Note this as `listing_selector`.
    3. Click one of the article links. On the article page, right-click the main article body text → Inspect. Identify the container element that wraps the full article body (typically `<article>`, `<div class="article-body">`, `<main>`, or similar). Note this as `article_selector`. Prefer semantic selectors (`article`, `main`, `[role="main"]`) when class names look hash-generated.
    4. Confirm `requests.get(article_url, headers={"User-Agent": "NitroFind/1.0 (nullsecurity1337@gmail.com; personal offline research tool)"}, timeout=15)` from a Python REPL returns status 200 and the response body length > 5000 chars. Run this from the project venv:
       ```
       python -c "import requests; r = requests.get('<paste article url>', headers={'User-Agent': 'NitroFind/1.0 (nullsecurity1337@gmail.com; personal offline research tool)'}, timeout=15); print(r.status_code, len(r.text))"
       ```
       Expect: `200 <some-number > 5000>`.
    5. If Hagerty fails (403, Cloudflare challenge, or step 4 returns non-200): repeat steps 1-4 for Car and Driver (`https://www.caranddriver.com/news/`), then Hemmings (`https://www.hemmings.com/stories/`), then Road & Track (`https://www.roadandtrack.com/news/`). Mark the first working target as `enabled: true` and disable the rest.
    6. If ALL FOUR fail: this is acceptable per RESEARCH.md Blog Target Assessment fallback strategy and phase success criterion (Wikipedia-only completes the minimum 1,000 articles). Document the failure in config/scraper.yaml with a YAML comment `# All four blog targets returned 403/connection error during manual inspection on <date>. Phase will run Wikipedia-only.` and leave all `enabled: false`.
    7. Update `config/scraper.yaml`:
       - Replace `# ASSUMED — verify at implementation` comments on the working target with `# VERIFIED <YYYY-MM-DD>` and paste the captured CSS selector value
       - Set `enabled: true` only on the working target
       - Add a top-level `blogs.verified_at` field with today's ISO date for traceability
    8. Re-run validation: `python -c "import yaml; cfg = yaml.safe_load(open('config/scraper.yaml')); print(cfg['blogs'])"` to confirm the YAML still parses and the verified target is visible.

    EXPECTED OUTCOME for Claude to proceed: At least one of:
      (a) `config/scraper.yaml` has exactly one blogs.targets entry with enabled: true AND that target's selectors comment is `# VERIFIED <date>` AND step-4 curl returned 200 from the developer's machine.
      (b) All four targets are `enabled: false` AND a comment in `blogs:` documents the failure (Wikipedia-only mode).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
      - `"approved hagerty"` (or caranddriver, hemmings, roadandtrack) — confirms the named target verified at step 4 with 200 response, selectors updated in config
      - `"approved wikipedia-only"` — all four blog targets failed manual inspection; phase will run Wikipedia-only
      - `"blocked: <reason>"` — verification could not complete; Claude pauses for further input
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BlogScraper class in nitrofind/scraper/blogs.py</name>
  <files>nitrofind/scraper/blogs.py</files>
  <read_first>
    - nitrofind/es_manager.py (class structure, error-handling convention with exception type name in log message)
    - nitrofind/es_schema.py (allowed fields in CAR_ARTICLES_MAPPING.properties)
    - config/scraper.yaml (post-Task-1 — verified target and selectors)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md (nitrofind/scraper/blogs.py section — full class + HTTP fetch + BS4 parse with noise removal)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md (Pattern 6 Blog Scraping with BeautifulSoup4; Pitfalls 3, 4; Blog Target Assessment fallback chain)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-CONTEXT.md (D-05, D-06, L-02, L-05, L-06)
  </read_first>
  <behavior>
    - Behavior: BlogScraper(config, state).yield_documents() is a generator. For each enabled target in config["blogs"]["targets"]: fetch listing page, extract article URLs via listing_selector, then fetch each article page and yield doc dict.
    - Behavior: HTTP 403 on listing page logs warning containing "403" and target name, then moves to the next enabled target (fallback chain). If no enabled target succeeds, yield nothing (no exception) and log a warning "All blog targets returned non-200 — Wikipedia-only mode".
    - Behavior: HTTP 403 on an article page (after listing succeeded) logs warning and skips just that article — does not abandon the target.
    - Behavior: When BeautifulSoup.select_one(article_selector) returns None (selector no longer matches): log warning "Article container not found at <url> (selector: <selector>)" and skip that article (Pitfall 4).
    - Behavior: Body text uses BeautifulSoup.get_text(separator=" ", strip=True) AFTER decomposing script/style/nav/footer/aside/.ad/.advertisement (RESEARCH.md Pattern 6). After get_text, collapse multiple whitespace with `re.sub(r"\s+", " ", text).strip()` so body is plain text only (L-05). Body MUST not contain `<` or `>` characters.
    - Behavior: Each yielded dict has:
      - article_id = URL slug (last non-empty path component of the URL; e.g., "https://www.hagerty.com/media/ferrari-history" -> "ferrari-history"). NOT a Wikipedia pageid — blog dedup is by URL slug per L-03 generalization.
      - source_domain = urlparse(target["base_url"]).netloc — e.g., "www.hagerty.com"
      - has_infobox = False
      - era_bucket = "Unknown" (no production_start data from blog articles)
      - title = parsed from <title> tag or first <h1> in the article container (fallback chain)
      - body = cleaned plain text
      - excerpt = make_excerpt(body)
      - word_count = len(body.split())
      - image_count = len(soup.select("img")) within the article container (or 0)
      - scraped_at = datetime.now(timezone.utc).isoformat()
    - Behavior: URL already in state (state.is_visited(url) is True) is skipped before fetch (D-06).
    - Behavior: After successful yield, state.mark_visited(url, target["name"]) is called.
    - Behavior: User-Agent header is HONEST: "NitroFind/1.0 (nullsecurity1337@gmail.com; personal offline research tool)" — no browser impersonation (RESEARCH.md security domain).
    - Behavior: Rate limit: time.sleep(self._rate_limit_seconds) — default 1.0 — between article fetches.
  </behavior>
  <action>
    Create nitrofind/scraper/blogs.py exporting class BlogScraper.

    Module docstring (es_manager.py style): Exports (BlogScraper), Requirement coverage (SCRP-02, L-05, L-06, D-05, D-06), Anti-patterns avoided (Pitfall 3 — graceful 403 + fallback chain; Pitfall 4 — selectors from config not hardcoded; honest User-Agent — no browser impersonation).

    Imports (canonical order): stdlib (`logging`, `re`, `time`, `datetime.datetime`, `datetime.timezone`, `typing.Generator`, `typing.Optional`, `urllib.parse.urlparse`, `urllib.parse.urljoin`), third-party (`requests`, `from bs4 import BeautifulSoup`), local (`from nitrofind.scraper.state import SQLiteStateManager`, `from nitrofind.scraper.cleaner import make_excerpt`).

    Module-level: `logger = logging.getLogger(__name__)`; `HONEST_USER_AGENT = "NitroFind/1.0 (nullsecurity1337@gmail.com; personal offline research tool)"`.

    BlogScraper API:
      - __init__(self, config: dict, state: SQLiteStateManager) -> None:
        * self._targets = [t for t in config["blogs"]["targets"] if t.get("enabled")]
        * self._state = state
        * self._rate_limit = float(config["blogs"].get("rate_limit_seconds", 1.0))
        * self._session = requests.Session(); self._session.headers["User-Agent"] = HONEST_USER_AGENT
      - yield_documents(self) -> Generator[dict, None, None]:
        * If self._targets is empty: logger.warning("No blog targets enabled — skipping blog scraping"); return.
        * For each target in self._targets:
          - article_urls = self._fetch_article_urls(target)
          - If article_urls is None (listing fetch failed): logger.warning("Target %r listing unavailable, trying next", target["name"]); continue (fallback chain — Pitfall 3).
          - Log INFO "Target %r: discovered %d article URLs" (D-05).
          - For each url in article_urls:
            * If self._state.is_visited(url): continue (D-06).
            * doc = self._fetch_article(url, target); if doc is None: continue.
            * yield doc
            * self._state.mark_visited(url, target["name"])
            * time.sleep(self._rate_limit)
          - Break after first successful target (fallback chain — only need one per SCRP-02 minimum).
        * If loop finished without yielding anything: logger.warning("All blog targets returned non-200 — Wikipedia-only mode").
      - _fetch_article_urls(self, target: dict) -> Optional[list[str]]:
        * try: resp = self._session.get(target["article_list_url"], timeout=15); resp.raise_for_status()
          except requests.HTTPError as exc: logger.warning("HTTP %s fetching listing %s: %s", exc.response.status_code, target["article_list_url"], exc); return None
          except requests.RequestException as exc: logger.warning("Request failed for listing %s: %s: %s", target["article_list_url"], type(exc).__name__, exc); return None
        * soup = BeautifulSoup(resp.text, "lxml")
        * links = soup.select(target["listing_selector"])
        * urls = []
        * For link in links: href = link.get("href"); if href: urls.append(urljoin(target["base_url"], href))
        * Return deduplicated list (preserve order): `list(dict.fromkeys(urls))`.
      - _fetch_article(self, url: str, target: dict) -> Optional[dict]:
        * try: resp = self._session.get(url, timeout=15); resp.raise_for_status()
          except requests.HTTPError as exc: logger.warning("HTTP %s fetching article %s: %s", exc.response.status_code, url, exc); return None
          except requests.RequestException as exc: logger.warning("Request failed for article %s: %s: %s", url, type(exc).__name__, exc); return None
        * soup = BeautifulSoup(resp.text, "lxml")
        * For noise_tag in soup.select("script, style, nav, footer, aside, .ad, .advertisement"): noise_tag.decompose()
        * container = soup.select_one(target["article_selector"])
        * If container is None: logger.warning("Article container not found at %s (selector: %r)", url, target["article_selector"]); return None  # Pitfall 4
        * raw_text = container.get_text(separator=" ", strip=True)
        * body_text = re.sub(r"\s+", " ", raw_text).strip()  # L-05 plain text only
        * If len(body_text) < 100: logger.warning("Skipping suspiciously short article (%d chars) at %s", len(body_text), url); return None  # Pitfall 4 warning sign
        * title_tag = container.select_one("h1") or soup.select_one("title")
        * title = title_tag.get_text(strip=True) if title_tag else url
        * url_slug = self._url_slug(url)
        * Return doc dict with EXACTLY these keys (per CAR_ARTICLES_MAPPING):
          - title, url, source_domain (= urlparse(target["base_url"]).netloc), article_id (= url_slug), scraped_at, body, excerpt (= make_excerpt(body_text)), word_count (= len(body_text.split())), has_infobox=False, image_count (= len(container.select("img"))), era_bucket="Unknown"
        * Do NOT include manufacturer/production_start/production_end/body_style/country_of_origin/specs keys — blogs don't have structured data, so omit (ES will store nothing for those fields; schema fields are not required).
      - _url_slug(self, url: str) -> str:
        * Parse url; take the path; strip trailing slash; take the last non-empty path segment as the slug. If empty, fall back to a hash of the URL: `hashlib.sha1(url.encode()).hexdigest()[:16]`. Import hashlib at top.

    Error handling convention (per es_manager.py): every `except Exception as exc:` logs `type(exc).__name__, exc`. No bare except. No silent pass.
  </action>
  <verify>
    <automated>python -c "from nitrofind.scraper.blogs import BlogScraper; import inspect; sig = inspect.signature(BlogScraper.__init__); assert 'config' in sig.parameters and 'state' in sig.parameters; print('ok')" && grep -q "NitroFind/1.0" nitrofind/scraper/blogs.py && grep -q "raise_for_status" nitrofind/scraper/blogs.py && grep -q 'select("script, style, nav, footer, aside' nitrofind/scraper/blogs.py && grep -q "make_excerpt" nitrofind/scraper/blogs.py && ! grep -q "Mozilla/" nitrofind/scraper/blogs.py</automated>
  </verify>
  <acceptance_criteria>
    - nitrofind/scraper/blogs.py defines `class BlogScraper:` with methods `__init__`, `yield_documents`, `_fetch_article_urls`, `_fetch_article`, `_url_slug`
    - Module docstring contains substrings `Exports:`, `Requirement coverage:`, `SCRP-02`, `Pitfall 3`, `Pitfall 4`
    - File contains literal `"NitroFind/1.0 (nullsecurity1337@gmail.com; personal offline research tool)"` (honest UA)
    - File does NOT contain `"Mozilla/"` substring (no browser impersonation)
    - File contains literal call `raise_for_status()` (HTTP error escalation)
    - File contains literal `select("script, style, nav, footer, aside` (noise removal per Pattern 6)
    - File contains import `from bs4 import BeautifulSoup` and import `from nitrofind.scraper.cleaner import make_excerpt`
    - File contains literal `BeautifulSoup(resp.text, "lxml")` (lxml parser backend)
    - `_fetch_article` returns None for missing container (Pitfall 4) and for short bodies (< 100 chars)
    - `python -c "from nitrofind.scraper.blogs import BlogScraper"` exits 0
    - File has NO bare `except:` (grep `except:\\s*$` returns 0)
    - File has NO occurrence of `"http://localhost:9200"` (blog scraper does not touch ES URL)
  </acceptance_criteria>
  <done>
    BlogScraper class committed: implements graceful 403 fallback chain (Pitfall 3), config-driven selectors with missing-container guard (Pitfall 4), BS4 noise removal + plain-text extraction (L-05), honest User-Agent, URL-slug-based article_id, has_infobox=False.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert Wave 0 stubs into real unit tests for BlogScraper</name>
  <files>tests/test_scraper/test_blogs.py</files>
  <read_first>
    - tests/test_es_manager.py (MagicMock + patch convention; section header style; result-capture pattern)
    - tests/test_scraper/test_blogs.py (Wave 0 stub — flip pytest.importorskip + pytest.skip to real assertions)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-PATTERNS.md (tests/test_scraper/test_blogs.py section — 403 handling test pattern with `with patch("nitrofind.scraper.blogs.requests.Session")`)
    - .planning/phases/02-data-pipeline-scraper-indexer/02-RESEARCH.md (Pitfalls 3, 4 — expected error behaviors)
    - nitrofind/scraper/blogs.py (the file produced by Task 2)
  </read_first>
  <behavior>
    - Test test_fetch_article_returns_none_on_403: Mock requests.Session.get to return a response with raise_for_status raising requests.HTTPError with response.status_code=403. _fetch_article returns None and logs warning containing "403".
    - Test test_fetch_article_returns_none_on_missing_container: Mock get to return HTML where select_one(article_selector) returns None (no matching tag). _fetch_article returns None and logs warning containing "Article container not found".
    - Test test_fetch_article_returns_none_on_short_body: Mock get to return HTML with article container but body text shorter than 100 characters. _fetch_article returns None and logs warning containing "suspiciously short".
    - Test test_extract_plain_text_removes_html_tags: Mock get to return HTML with `<p>Real <strong>body</strong> text about Ferrari</p>` inside the article container. Result doc's body field contains "Real body text about Ferrari" with no `<` or `>` characters (L-05).
    - Test test_doc_has_correct_shape: Successful fetch yields doc with required keys (title, url, source_domain, article_id, scraped_at, body, excerpt, word_count, has_infobox, image_count, era_bucket). has_infobox is False, era_bucket is "Unknown", article_id is the URL slug, source_domain is the urlparse netloc of base_url.
    - Test test_fallback_chain_advances_on_403: Configure two enabled targets; mock the first target's listing URL to return 403; mock the second target's listing to return 200 with one article link; mock the article URL to return 200 with valid HTML. Consume generator. Assert second target's articles are yielded; first target's articles are not. Assert logger emitted a warning about the first target's 403.
    - Test test_state_visited_url_is_skipped: Configure one enabled target; mock listing to return 200 with one article URL "https://www.hagerty.com/media/foo"; configure mock_state.is_visited to return True for that exact URL. Assert the session's get is NOT called for the article URL (only the listing URL). No documents yielded.
    - Test test_honest_user_agent_not_mozilla: After construction, assert BlogScraper._session.headers["User-Agent"] equals the HONEST_USER_AGENT module constant and does NOT contain "Mozilla".
  </behavior>
  <action>
    Update tests/test_scraper/test_blogs.py:
      1. Replace `pytest.importorskip("nitrofind.scraper.blogs", ...)` with `from nitrofind.scraper.blogs import BlogScraper, HONEST_USER_AGENT`.
      2. Replace each `pytest.skip("Wave 1 implementation")` body with the test logic from <behavior>.
      3. Extend module docstring with the new test names and "Pitfall 3" / "Pitfall 4" coverage references.
      4. Use a sample config dict:
         ```
         sample_config = {
           "blogs": {
             "rate_limit_seconds": 0,
             "targets": [
               {"name": "hagerty", "enabled": True, "base_url": "https://www.hagerty.com/media/",
                "article_list_url": "https://www.hagerty.com/media/all-articles/",
                "article_selector": "article.post", "listing_selector": "a.article-link"}
             ]
           }
         }
         ```
         For fallback-chain test, add a second disabled-then-enabled pair (first enabled returns 403, second enabled returns 200).
      5. Use `with patch.object(BlogScraper, "_session", new=mock_session):` is messy because session is per-instance — instead patch on the instance after construction: `scraper = BlogScraper(sample_config, mock_state); scraper._session = mock_session`. Mock_session.get.side_effect = lambda url, **kw: produce_mock_response(url).
      6. For HTML mock responses, use a small literal HTML string like `"<html><body><article class='post'><h1>Test Title</h1><p>Real body text about Ferrari that is sufficiently long to clear the 100-char threshold for the test of valid article content.</p></article></body></html>"`. For the missing-container test, return HTML without the `article.post` element.
      7. Use pytest's `caplog` fixture (level=WARNING) to assert warning message content.
      8. Group tests with `# ---` 76-hyphen section headers:
         - SCRP-02: HTTP error handling (test_fetch_article_returns_none_on_403)
         - SCRP-02: Selector mismatch (test_fetch_article_returns_none_on_missing_container, test_fetch_article_returns_none_on_short_body)
         - SCRP-02: HTML stripping (test_extract_plain_text_removes_html_tags)
         - SCRP-02: Document shape (test_doc_has_correct_shape)
         - SCRP-02: Fallback chain (test_fallback_chain_advances_on_403)
         - D-06: Skip-if-visited (test_state_visited_url_is_skipped)
         - Security/etiquette: User-Agent (test_honest_user_agent_not_mozilla)
  </action>
  <verify>
    <automated>pytest tests/test_scraper/test_blogs.py -x -m "not integration" -v 2>&1 | grep -E "PASSED|FAILED" | wc -l | xargs -I{} test {} -ge 7 && pytest tests/test_scraper/test_blogs.py -x -m "not integration" 2>&1 | grep -q "passed" && ! grep -q "pytest.importorskip" tests/test_scraper/test_blogs.py && ! grep -q 'pytest.skip("Wave 1' tests/test_scraper/test_blogs.py && pytest tests/ -x -m "not integration"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_scraper/test_blogs.py imports `from nitrofind.scraper.blogs import BlogScraper, HONEST_USER_AGENT` (no longer pytest.importorskip)
    - File contains NO `pytest.skip("Wave 1` substring
    - File contains all eight test functions listed above
    - `pytest tests/test_scraper/test_blogs.py -x -m "not integration"` exits 0 with at least 7 PASSED tests (`test_fallback_chain_advances_on_403` may rely on slightly different infrastructure if patched-session pattern is hard; ensure at least 7 pass)
    - `pytest tests/ -x -m "not integration"` exits 0 (no regressions in Phase 1 or sibling Phase 2 plans)
    - test_doc_has_correct_shape body asserts `doc["has_infobox"] is False` AND `doc["source_domain"] == "www.hagerty.com"` (or the verified target's netloc) AND `doc["article_id"]` is a non-empty string AND `doc["era_bucket"] == "Unknown"`
    - test_extract_plain_text_removes_html_tags body asserts `"<" not in doc["body"]` AND `">" not in doc["body"]`
    - test_fallback_chain_advances_on_403 body asserts that documents from the second target appear in yielded results AND warning log mentions the first target name
    - test_honest_user_agent_not_mozilla asserts `"Mozilla" not in scraper._session.headers["User-Agent"]`
  </acceptance_criteria>
  <done>
    Wave 0 stubs converted to eight real assertions covering 403 fallback (Pitfall 3), selector mismatch (Pitfall 4), HTML stripping (L-05), honest UA, dedup-via-state (D-06), and document shape; full unit suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> BlogScraper | Hagerty/Car&Driver/Hemmings/Road&Track are external untrusted content. HTML may contain script, style, iframe, or malformed markup. |
| BS4 parse -> ES indexing | get_text(strip=True) removes all tags; regex collapse of whitespace ensures plain text. dynamic:"false" on ES index blocks any field injection from infobox-shaped data (irrelevant for blogs — has_infobox=False — but defense in depth). |
| config/scraper.yaml -> BlogScraper | Config is project-controlled (verified by developer in Task 1); selectors are not user-input at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering (HTML injection into ES body) | _fetch_article -> body field | mitigate | BS4 get_text(separator=" ", strip=True) removes ALL tags; subsequent `re.sub(r"\s+", " ", text).strip()` normalizes whitespace. Acceptance criteria: body field contains no `<` or `>` characters (test_extract_plain_text_removes_html_tags). |
| T-02-15 | Spoofing (User-Agent impersonation) | BlogScraper._session.headers | accept | We deliberately use an HONEST User-Agent identifying NitroFind + project owner email. No browser impersonation. RESEARCH.md security domain mandates this. If Hagerty returns 403 to honest UA, Plan logs warning and falls back to the next target — we do not bypass bot protection. |
| T-02-16 | Information Disclosure (article URL in SQLite state) | state.mark_visited(url, target_name) | accept | Article URLs are public; storing them locally is not sensitive. data/scraper_state.db is gitignored (Plan 01). |
| T-02-17 | Denial of Service (slow/hanging blog response) | requests.Session.get | mitigate | timeout=15 on every HTTP call; HTTPError + RequestException both caught and logged. No infinite retries — single-shot fetch per URL. |
| T-02-18 | Tampering (XML/XXE via lxml parser) | BeautifulSoup(html, "lxml") | accept | lxml in BS4 mode treats input as HTML, not XML, so XXE attack surface is minimal. BeautifulSoup does not expand external entities by default. RESEARCH.md confirms lxml as the recommended BS4 backend for performance. |
</threat_model>

<verification>
1. After Task 1 checkpoint: config/scraper.yaml has at least one enabled target with VERIFIED selectors, OR all targets disabled with documented fallback comment
2. `pytest tests/test_scraper/test_blogs.py -x -m "not integration"` exits 0 with at least 7 PASSED tests
3. `pytest tests/ -x -m "not integration"` — full unit suite green
4. `grep -n "Mozilla\|browser" nitrofind/scraper/blogs.py` returns no matches (no browser impersonation)
5. `grep -n "http://localhost:9200" nitrofind/scraper/blogs.py` returns no matches (blogs do not touch ES URL directly)
6. `python -c "from nitrofind.scraper.blogs import BlogScraper; print('ok')"` succeeds
</verification>

<success_criteria>
- Task 1 checkpoint: developer confirms either (a) one blog target verified with 200 response + updated selectors, or (b) all four targets blocked → Wikipedia-only documented in config
- nitrofind/scraper/blogs.py defines BlogScraper with __init__(config, state) and yield_documents() generator
- BlogScraper handles HTTP 403 on listing by advancing to next enabled target (fallback chain — Pitfall 3); handles 403 on article by skipping just that article
- Missing article container triggers warning and skip (Pitfall 4); short body (<100 chars) also skipped
- Body text contains only plain text (no `<` or `>` characters) via BS4 get_text + whitespace collapse (L-05)
- User-Agent is HONEST_USER_AGENT module constant (no "Mozilla/" substring — project ethic enforced)
- yield_documents yields docs with article_id=URL slug, source_domain=urlparse(base_url).netloc, has_infobox=False, era_bucket="Unknown"
- D-06 honored: state.is_visited(url) skips already-indexed URLs; state.mark_visited(url, target_name) called after each successful yield
- All Wave 0 stubs replaced; at least seven unit tests pass; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline-scraper-indexer/02-04-SUMMARY.md` documenting:
- Task 1 checkpoint outcome: name of verified target OR Wikipedia-only mode
- Updated config/scraper.yaml diff (selector values, verified_at date, enabled flags)
- nitrofind/scraper/blogs.py: line count + exported symbols
- Methods on BlogScraper and what each does
- Unit test names + PASSED count
- Confirmation: pytest tests/ -x -m "not integration" exits 0
- Confirmation: HONEST_USER_AGENT module constant present, "Mozilla" string absent
</output>
