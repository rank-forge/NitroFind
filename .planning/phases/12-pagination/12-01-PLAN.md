---
phase: 12-pagination
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_search/test_api_search.py
autonomous: true
requirements: [PAGE-01, PAGE-02]
must_haves:
  truths:
    - "Six new pagination tests exist in test_api_search.py and are collected by pytest"
    - "Four existing response-shape tests assert the new wrapper shape, not the flat array"
    - "The new tests are RED (fail) against the current flat-array api_search implementation"
  artifacts:
    - path: "tests/test_search/test_api_search.py"
      provides: "RED pagination test scaffold + updated response-shape assertions"
      contains: "def test_pagination_page_2"
  key_links:
    - from: "tests/test_search/test_api_search.py"
      to: "nitrofind.server.api_search"
      via: "mock_es.search.call_args.kwargs assertions on from_/size and wrapper response keys"
      pattern: "call_args\\.kwargs"
---

<objective>
Create the Wave 0 failing-test scaffold for Phase 12 pagination. Update the four existing tests that inspect the `/api/search` response body to expect the new wrapper shape `{"results": [...], "total", "took_ms", "page"}`, and add six new pagination unit tests that pin the `page`→`from_` offset math and the wrapper response contract.

Purpose: Lock the pagination behavior contract in tests BEFORE the implementation exists (RED), so the backend plan (12-02) has an executable definition of done.
Output: `tests/test_search/test_api_search.py` with 4 updated tests and 6 new pagination tests. All 6 new tests plus the 4 updated tests are RED against the current implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pagination/12-RESEARCH.md
@.planning/phases/12-pagination/12-PATTERNS.md
@.planning/phases/12-pagination/12-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols introduced across Phase 12 (excluded from source-drift verification):
- Backend: `PAGE_SIZE` constant (nitrofind/server.py); reshaped `_result_to_api_dict(result)` signature (took_ms param removed); wrapper response keys `results`, `total`, `took_ms`, `page`
- Frontend: `currentPage` state var, `renderPagination(total, page)` function, `prevBtn`/`nextBtn` DOM refs (static/js/app.js); `#prev-btn`, `#next-btn`, `.pagination-row`/`#pagination-row` (templates/index.html); `.pagination-row` and `.pagination-row button` CSS rules (static/css/style.css)
- Tests (this plan): `test_pagination_default`, `test_pagination_page_2`, `test_pagination_page_zero`, `test_pagination_invalid_page`, `test_pagination_total`, `test_pagination_wrapper`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Update the 4 existing response-shape tests for the wrapper response</name>
  <files>tests/test_search/test_api_search.py</files>
  <read_first>
    - tests/test_search/test_api_search.py (current assertions on the flat array response)
    - .planning/phases/12-pagination/12-RESEARCH.md (Pitfall 5 — the exact 4 tests and new assertions)
    - .planning/phases/12-pagination/12-PATTERNS.md (test file section — line-by-line current→new assertion table)
  </read_first>
  <action>
    Update the four tests in `tests/test_search/test_api_search.py` that read the `/api/search` response body so they expect the new wrapper object instead of a flat list. Exact target assertions:
    - `test_search_returns_result_array`: change `assert isinstance(data, list)` to `assert isinstance(data["results"], list)` and `assert len(data) == 1` to `assert len(data["results"]) == 1`.
    - `test_search_result_shape`: change `item = data[0]` to `item = data["results"][0]`; change the expected key set to exclude `"took_ms"` so it is exactly `{"title", "url", "source_domain", "excerpt", "body", "body_html", "score"}`; change `assert item["took_ms"] == 12` to `assert data["took_ms"] == 12`; add `assert data["total"] == 1`; add `assert data["page"] == 1`.
    - `test_excerpt_uses_highlight`: change `item = resp.get_json()[0]` to `item = resp.get_json()["results"][0]`.
    - `test_excerpt_fallback`: change `item = resp.get_json()[0]` to `item = resp.get_json()["results"][0]`.
    Do NOT change `test_search_empty_q_returns_empty` — the blank-q guard still returns `[]`, so `resp.get_json() == []` remains correct. Do NOT change the filter/sort tests (`test_manufacturer_filter_forwarded`, `test_sort_*`, `test_year_*`, `test_country_*`) — they inspect `call_args.kwargs["query"]`, not the response body.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && python -m pytest tests/test_search/test_api_search.py -q -k "search_returns_result_array or search_result_shape or excerpt" 2>&1 | grep -E "failed|error" </automated>
  </verify>
  <acceptance_criteria>
    - `test_api_search.py` contains `data["results"][0]` in `test_search_result_shape`
    - `test_search_result_shape` expected key set excludes `"took_ms"` and asserts `data["took_ms"] == 12`, `data["total"] == 1`, `data["page"] == 1`
    - `test_excerpt_uses_highlight` and `test_excerpt_fallback` both use `resp.get_json()["results"][0]`
    - `test_search_empty_q_returns_empty` still asserts `resp.get_json() == []` (unchanged)
    - The 4 updated tests FAIL (RED) against the current flat-array implementation — `pytest -k "search_returns_result_array or search_result_shape or excerpt"` reports failures
  </acceptance_criteria>
  <done>The 4 response-shape tests assert the wrapper contract and are RED against the current implementation. `test_search_empty_q_returns_empty` and the filter/sort tests are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add 6 new pagination unit tests</name>
  <files>tests/test_search/test_api_search.py</files>
  <read_first>
    - tests/test_search/test_api_search.py (fixture and inline-monkeypatch patterns to reuse)
    - .planning/phases/12-pagination/12-PATTERNS.md (test section — `test_pagination_page_2` inline monkeypatch example, call_args.kwargs inspection pattern)
    - .planning/phases/12-pagination/12-VALIDATION.md (Wave 0 Requirements — the 6 test names and expected behavior)
  </read_first>
  <action>
    Add six new pagination tests to `tests/test_search/test_api_search.py`, each using the inline `monkeypatch.setitem(server.state, "ready", True)` + `monkeypatch.setitem(server.state, "es_client", mock_es)` pattern (mirroring `test_manufacturer_filter_forwarded`). Mock ES return value: `{"took": 3, "hits": {"total": {"value": 25}, "hits": []}}` (adjust `total.value` per test). Inspect `mock_es.search.call_args.kwargs` for `from_`/`size`, and `resp.get_json()` for wrapper keys. Tests:
    - `test_pagination_default`: GET `/api/search?q=ferrari` (no page param) → `call_kwargs["from_"] == 0` and `call_kwargs["size"] == 10`.
    - `test_pagination_page_2`: GET `/api/search?q=ferrari&page=2` → `call_kwargs["from_"] == 10` and `call_kwargs["size"] == 10`.
    - `test_pagination_page_zero`: GET `/api/search?q=ferrari&page=0` → clamped to page 1 → `call_kwargs["from_"] == 0`.
    - `test_pagination_invalid_page`: GET `/api/search?q=ferrari&page=abc` → non-integer coerced → `call_kwargs["from_"] == 0`.
    - `test_pagination_total`: mock `total.value` = 248 → `resp.get_json()["total"] == 248`.
    - `test_pagination_wrapper`: assert `resp.get_json()` is a dict containing keys `results`, `total`, `took_ms`, `page`; `data["took_ms"] == 3` (wrapper level); and NO item in `data["results"]` contains a `took_ms` key.
    Each test name must be uniquely selectable via `pytest -k`. Keep q non-blank in every request (blank q short-circuits before ES is called).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/Leonardo/Desktop/codes/Claude/NitroFind && python -m pytest tests/test_search/test_api_search.py -k pagination --collect-only -q 2>&1 | grep -c "test_pagination"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_search/test_api_search.py -k pagination --collect-only -q` collects exactly 6 tests
    - `test_api_search.py` contains `def test_pagination_default`, `def test_pagination_page_2`, `def test_pagination_page_zero`, `def test_pagination_invalid_page`, `def test_pagination_total`, `def test_pagination_wrapper`
    - `test_pagination_page_2` asserts `call_kwargs["from_"] == 10` and `call_kwargs["size"] == 10`
    - `test_pagination_wrapper` asserts the response is a dict with keys `results`, `total`, `took_ms`, `page`
    - All 6 new tests FAIL (RED) against the current flat-array implementation
  </acceptance_criteria>
  <done>Six pagination tests exist, are collected by pytest, pin the `page`→`from_` math and wrapper contract, and are RED against the current implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → Flask `/api/search` | Untrusted `page` query-string value crosses here (validated in 12-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `page` query param → ES `from_` | mitigate | `test_pagination_invalid_page` pins that a non-integer `page` coerces to page 1 (`from_=0`); enforced in 12-02 via `_safe_int_param` |
| T-12-02 | Denial of Service | out-of-range `page` (e.g. `page=0`) | mitigate | `test_pagination_page_zero` pins clamp to page 1; verifies `from_` is never negative |
| T-12-SC | Tampering | package installs | accept | This plan installs no packages; edits an existing test file only — no supply-chain surface |
</threat_model>

<verification>
- `pytest tests/test_search/test_api_search.py -k pagination --collect-only -q` collects 6 pagination tests.
- Running the full `test_api_search.py` shows the 4 updated + 6 new tests RED (implementation not yet done); the untouched tests (`test_search_empty_q_returns_empty`, filter/sort tests) remain GREEN.
</verification>

<success_criteria>
- 6 new pagination tests present and collectable.
- 4 existing response-shape tests updated to the wrapper contract.
- New/updated tests are RED against the current implementation; no unrelated test broken.
</success_criteria>

<output>
Create `.planning/phases/12-pagination/12-01-SUMMARY.md` when done.
</output>
